# Windows 배포용 의존성 패키지
# Windows 환경에서 안정적으로 실행되도록 버전 조정

# GUI 프레임워크 (CustomTkinter로 전환)
customtkinter>=5.2.0

# 키움증권 OpenAPI+ (Windows 전용, PyQt5 기반)
# pykiwoom은 PyQt5의 QAxWidget을 상속하므로 PyQt5가 반드시 필요
# PyPI 최신 배포 버전: 0.1.6 (0.1.9는 존재하지 않음)
pykiwoom>=0.1.6
PyQt5>=5.15.0

# HTTP 요청 및 웹소켓
requests==2.31.0
websockets==11.0.3
websocket-client==1.6.4

# 거래소 API
python-binance==1.0.19
ccxt>=4.0.0

# 로깅 및 유틸리티
loguru==0.7.2

# 데이터 처리
numpy==1.24.3
pandas==2.0.3

# 환경 변수 관리
python-dotenv==1.0.0

# OpenAI API
openai>=1.35.0

# 테스트/배포 게이트
pytest>=8.0.0

# Windows 호환성을 위한 추가 패키지
# pywin32는 PyInstaller가 자동으로 포함하므로 제외
psutil==5.9.8
aiohttp==3.9.5
ujson==5.10.0
dateparser==1.2.0
colorama==0.4.6
win32-setctime==1.1.0

# 음성 (AI 어시스턴트 STT/TTS)
# pyaudio는 Windows에서 pipwin 또는 pip install pyaudio 로 설치
SpeechRecognition>=3.10.0
pyaudio>=0.2.14
pipwin>=0.5.2

# 이미지 처리 및 OCR (차트 분석 기능)
Pillow>=10.0.0
opencv-python-headless>=4.10.0
rapidocr-onnxruntime
paddlepaddle
paddleocr>=2.7.0
