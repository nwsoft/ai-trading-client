# 📋 NoahAI 릴리스 노트

<!-- markdownlint-disable MD007 MD013 MD024 -->

## v3.9.0.8 AI Custom Update Fix 1 · 테스터 추가 피드백 보강 (2026-08-11)

- 통합자산은 현재 거래소·증권사 잔고를 사용합니다. 종료 거래 누적 체결액을 자산으로 표시하지 않으며, 레거시 DB 열 차이 때문에 0원으로 고정되던 문제를 수정했습니다.
- KRW와 USDT는 환율 기준시각 없이 합산하지 않습니다. 통화별 금액을 그대로 표시하고 비교 가능한 단일 통화에서만 비중·집중도·손실 시나리오를 계산합니다.
- AI 애널리스트 심층분석은 파괴된 입력창 참조를 버리고 살아 있는 AI 어시스턴트를 확인·재생성한 뒤 요청합니다.
- AI 탭만 부분 처리하지 않고 시장 트렌드·금융 인텔리전스·AlphaArena·코인/종목 정보·거래 통계·AI 애널리스트까지 공통 화면 소유권에 포함했습니다. 탭 삭제와 재구성 때 예전 위젯 참조·콜백·native 메뉴가 함께 정리됩니다.
- 실제 AI 질문과 심층분석 요청은 같은 활동 ID로 하단 기록에 남고 `응답 생성 중 → 완료/오류` 상태가 갱신됩니다.
- 현재 잔고 단일 저장소와 구형/신형 거래 DB 공통 어댑터를 적용해 통합자산뿐 아니라 시나리오·보안 경고의 같은 스키마 재발 경로도 제거했습니다.
- 공통 원인 집중 회귀 48건과 전체 자동 회귀 `1,432 passed, 6 skipped`를 통과했습니다. Windows 설치본 검증 전 상태는 `pending_windows_rebuild`입니다.

## v3.9.0.8 AI Custom Update Fix 1 · Windows UI 생명주기 안정화 (2026-08-10)

- 설정창을 대시보드 수명 동안 한 인스턴스로 재사용해 열고 닫을 때마다 30개 이상의 native 드롭다운 메뉴를 재할당하던 구조를 제거했습니다.
- 저장·폐기 확인을 별도 CustomTkinter Toplevel 대신 OS 표준 3상태 확인창으로 바꿔 자원 고갈 시 빈 흰색 창이 겹치는 경로를 제거했습니다.
- CustomTkinter ComboBox·OptionMenu의 비시각 `tk.Menu`를 탭 정리 때 명시적으로 파괴합니다.
- 블록체인/주식 전환의 중복 탭 삭제·재생성 경로를 하나로 합치고, 설정 저장 시 현재 서비스만 갱신하도록 바꿔 거래소와 증권사 탭 혼합을 차단했습니다.
- 코인/종목 정보와 거래소/증권사 탭의 정본 순서를 프레임 재생성 없이 복구합니다.
- 포지션 카드의 중첩 Canvas 프레임을 제거해 Windows 자원 압박 상황의 빈 카드 렌더링 가능성을 줄였습니다.
- 전체 자동 회귀 `1,421 passed, 6 skipped`와 native 메뉴 100회 생성/정리, 실제 설정창 25회 재사용 스모크를 통과했습니다.
- 소스·회귀 검증과 Windows 설치본 검증을 분리합니다. 새 EXE·SHA-256·제보 PC 100회 전환 전 manifest는 `pending_windows_rebuild`입니다.

## v3.9.0.8 AI Custom Update · 전략 IR·검증·설명·공유 고도화 (2026-08-10)

- 원본 근거와 실행 규칙을 Noah Strategy IR v1로 연결하고 `supported / needs_clarification / unsupported`를 저장·승인·활성화 전에 재검증합니다.
- 같은 전략을 Level 1 요약, Level 2 핵심값, Level 3 전체 IR·안전 DSL로 제공하고 초보자·일반·고급·실험실 프로필과 기능별 ON/OFF를 추가했습니다.
- 총 PnL·총 수익률·MDD·승률·Profit Factor·월별/연별 표를 최소 품질 필터로 제공하되 과거 재생만으로 자동 승격하지 않고 PAPER 전진검증을 우선합니다.
- 중첩 Expression Graph, 제한형 사용자 지표 언어, 명시적 주문 상태 계획과 HMAC·시간창·nonce·delivery ID webhook 게이트를 추가했습니다.
- `.noahstrategy` 내보내기/가져오기, SHA-256 변조 검사, 비활성 검토 가져오기, 전략 여권·품질·감사 보고서를 추가했습니다.
- 대시보드 업데이트·AI 커스텀 매뉴얼과 AI 어시스턴트의 프로필·백테스트/PAPER·XAI·패키지·webhook 설명을 같은 정본으로 동기화했습니다.
- 집중 회귀 `94 passed`, 전체 회귀 `1,411 passed, 6 skipped`, 문서 정합·빌드 포함·활성 소스 감사를 통과했습니다.
- 새 Windows EXE와 SHA-256, 실제 TradingView/webhook, 장시간 PAPER/LIVE, 서버 팀 공유와 인증 B2B API는 외부 검증 전이며 manifest는 `pending_windows_rebuild`입니다.

## v3.9.0.7 Fix Patch 3 · Windows VC 런타임·UI 생명주기·거래소/현물 원장 안정화 (2026-08-08)

- 세 덤프의 동일 네이티브 충돌(`0xc0000005`, `MSVCP140.dll+0x12ea0`, `onnxruntime_pybind11_state.pyd`)을 기준으로 패키징 정책을 수정했습니다.
- PyQt5는 키움 OpenAPI+의 `QAxWidget`/COM을 위해 유지합니다. PyQt5 자체나 `pyi_rth_pyqt5`를 제거하는 패치가 아닙니다.
- PyQt5·pandas 등이 포함한 중첩 `MSVCP140*.dll`/`VCRUNTIME140*.dll`을 빌드 수집 단계에서 제거하고, 공식 Microsoft VC143 x64 CRT의 검증된 단일 세트만 EXE 루트에 수집합니다.
- ONNX Runtime PE 링커 버전보다 오래된 런타임, 필수 DLL 누락, 서로 다른 VC 릴리스 혼합을 발견하면 빌드를 실패시킵니다. 완성 EXE도 하위 중복과 원본 SHA-256을 다시 검사합니다.
- RapidOCR/ONNX는 대시보드 import 시 로드하지 않고 실제 차트 OCR 요청 시에만 초기화해 Binance PAPER 시작 경로의 불필요한 네이티브 로드를 제거했습니다.
- `CTkTabview.delete()`가 동적 탭 프레임을 파괴하지 않는 동작을 공통 생명주기 함수로 보완했습니다. 모든 자식 콜백을 정리하고 프레임을 실제 파괴해 거래소/서비스 전환 뒤 메뉴·위젯이 누적되는 원인을 제거했습니다.
- Windows 설정창·서비스 전환 전후 USER/GDI 리소스 수를 기록해 `No more menus can be allocated` 재현 여부를 수치로 확인할 수 있게 했습니다.
- Evaluator·MarketSentiment·런타임 생성 정책에 명시적 거래소 경계를 적용했습니다. 비바이낸스 심볼은 Binance K라인·티커·펀딩비·OI로 폴백하지 않고, Binance가 활성 범위에 없으면 전용 REST/WebSocket도 생성하지 않습니다.
- Bitget 후보군에서 확인된 토큰화 주식 심볼을 코인 후보에서 제외했습니다. 시장 메타데이터와 관리형 제외목록을 함께 사용합니다.
- Upbit·Bithumb LIVE 주문 전 실제 잔고를 확인하고 동일 종목 중복 진입·원장 밖 중요 보유자산 재진입을 차단합니다. 기존 중요 보유자산은 최대 포지션 수에 합산하고 dust는 슬롯에서 분리하며, 청산 시 앱이 진입한 수량만 매도합니다.
- PAPER·LEARNING은 개인 잔고/체결 API를 호출하지 않으며 회원 권한, 선물 거래소, 증권사 경로는 기존 게이트를 유지합니다.
- Fix Patch 2의 로그 개인정보·Windows 파일 회전·설정창 안정화와 Fix Patch 1의 레퍼럴 승인 게이트는 그대로 유지합니다.
- 통합 소스 전체 회귀 `1,377 passed, 6 skipped`를 통과했습니다. Windows USER/GDI 장시간 전환, 제보 PC 네이티브 크래시, 실제 거래소 소액 체결은 재빌드 후 외부 E2E 게이트로 남습니다.
- 새 Windows EXE를 아직 만들지 않았으므로 manifest는 `pending_windows_rebuild`입니다. Windows 10/11 깨끗한 PC와 제보 PC에서 Kiwoom, 차트 OCR, 승인된 레퍼럴 Binance PAPER 장시간 검증 후 배포합니다.

## v3.9.0.7 Fix Patch 2 · 로그 개인정보·Windows 파일 잠금·안정성 진단 보강 (2026-08-08)

- 로그인 성공 응답과 `user_info`를 원문으로 출력하지 않습니다. 이메일·세션 ID·Bearer 토큰·UID·레퍼럴 코드/URL·검증 오류는 파일과 콘솔 로그에 기록하지 않습니다.
- 진단에 필요한 회원등급·정책 버전·허용 거래소 개수·귀속 상태별 건수만 allowlist 요약으로 남깁니다.
- Windows에서 열려 있는 `trading_*.log`를 시작 중 직접 삭제하던 경로를 제거했습니다. 메인·거래소 로그는 25MB 회전, 7개 백업, 14일 보관으로 통일합니다.
- Windows 이전 실행 PID 확인은 `os.kill(pid, 0)`을 사용하지 않고 `psutil.pid_exists()`를 사용해 런타임 진단 초기화 `SystemError`를 방지합니다.
- 설정창의 상단 진단 상세를 기본 접고, 하단 저장·취소 버튼을 탭보다 먼저 예약해 800px 창에서 설정 본문이 아래로 밀리지 않게 했습니다.
- 하단 전체 저장·취소·백업 복구 버튼을 38px 높이로 줄여 설정 본문 공간을 추가 확보했습니다.
- 설정창 생성 완료 후에만 모달 잠금을 걸고 생성 실패 시 빈 창을 즉시 제거합니다. Tk interpreter에 묶이는 `CTkImage` 대신 PIL 원본만 캐시해 재진입 시 `pyimage` 오류도 방지합니다.
- 레퍼럴 UID 미신청 사용자의 로그인·API 입력 준비는 허용하되 `allowed_exchanges=[]`, `can_select_exchange=false`, `can_start_trading=false`로 LEARNING/PAPER/LIVE를 계속 차단합니다.
- 설정창 집중 회귀 `5 passed`, 전체 회귀 `1,354 passed, 6 skipped`, 문서·빌드 포함·활성 소스 감사를 통과했습니다. 새 Windows EXE와 manifest SHA-256은 Windows 재빌드·설치 후 확정하며, 그전까지 Fix Patch 2는 `pending_windows_rebuild`입니다.

## v3.9.0.7 Fix Patch 1 · 레퍼럴 첫 실행·자동확인 운영 안정화 (2026-08-07)

- OpenAI 등 AI 공급자 키를 앱 로그인 필수조건에서 분리했습니다. 키가 없으면 AI 기능만 비활성 안내를 표시하고 대시보드와 거래소 API 설정은 정상적으로 사용할 수 있습니다.
- 첫 설정창을 키 없이 닫았을 때 `sys.exit()`로 프로그램이 종료되던 경로를 제거했습니다.
- daltrading 상태 확인이 네트워크 장애·5xx 등으로 한 번 실패했다고 앱을 강제 종료하지 않습니다. 서버가 계정 비활성, 세션 충돌 또는 401/403을 명시한 경우에만 종료 게이트를 적용합니다.
- 레퍼럴 회원은 활성 제휴 거래소의 API 키 검증 후 UID를 자동 제출합니다. UID가 변경되면 기존 승인도 `pending`으로 되돌린 뒤 서버 Affiliate 조회 결과에 따라 재승인 또는 불일치 차단됩니다.
- 목적별 전략 허브가 로그인 쿠키를 인식해 로그인 사용자에게 `로그인 중`과 `내 대시보드`를 표시하도록 daltrading 웹을 수정했습니다.
- 운영자 Affiliate 조회 전용 키는 SSH나 `.env` 편집 없이 daltrading `관리자 → 설정`에서 저장·교체·삭제할 수 있습니다. 서버 DB에는 암호화하고 화면에는 끝 4자리만 표시하며, 사용자 거래 키와는 완전히 분리합니다.
- Bybit·Bitget·OKX 키 저장 시 기존 `pending` UID도 재검증 대기열에 넣습니다. 실제 Affiliate 조회 권한이 없거나 거래소가 조회를 거부하면 자동 승인하지 않고 `pending`으로 유지합니다. Binance는 Partner/API-Agent 조회 권한 확인 전까지 수동 예외 처리입니다.
- daltrading 전체 회귀 `52 passed, 3 subtests passed`와 클라이언트 전체 회귀 `1,344 passed, 6 skipped`를 통과했습니다. Windows EXE는 사용자의 v3.9.0.7 Fix Patch 1 재빌드 전이며 manifest는 `pending_windows_rebuild`입니다.

## v3.9.0.7 Source Candidate 2 · Affiliate 자동 귀속 확인·주기 재검증 (2026-08-06)

- 레퍼럴 회원은 승인 전에도 활성 제휴 거래소 API 키를 입력·검증할 수 있습니다. 거래소 선택과 LEARNING/PAPER/LIVE 시작은 서버 `verified` 전까지 계속 차단됩니다.
- Bybit·Bitget·OKX API 검증 성공 직후 사용자 PC에서 본인 UID와 제공 가능한 inviter/affiliate/channel 값을 조회합니다. 사용자 API Key·Secret·Passphrase는 PC 밖으로 전송하지 않고 UID와 비밀이 아닌 관계 결과만 daltrading에 보냅니다.
- daltrading은 운영자 Affiliate 조회 전용 키로 같은 UID를 재조회합니다. 일치하면 `auto_verified`, 불일치하면 차단, 자격증명 미설정이나 일시 장애면 `pending`으로 유지합니다.
- 자동 판정 건은 systemd timer로 주기 재검증합니다. 귀속 해제는 `expired`로 자동 차단하고, 거래소 장애만으로 기존 승인을 해제하지 않습니다.
- 관리자 레퍼럴 화면에 거래소별 자동 확인 준비 상태·검증 방식·오류를 표시하며, 수동 승인은 API 미설정·장애·불일치 예외 처리로 남깁니다.
- Binance 일반 추천 코드는 공식 Partner/API-Agent 조회 계약과 권한이 확인되기 전까지 자동 승인하지 않고 수동 예외 처리합니다.
- daltrading 전체 회귀 `44 passed, 3 subtests passed`, 클라이언트 집중 회귀 `11 passed`를 통과했습니다. Windows EXE와 실제 Affiliate 자격증명 E2E는 아직 남아 있습니다.

## v3.9.0.7 Source Candidate 1 · 레퍼럴 UID 귀속 검증·거래소 권한 (2026-08-05)

- daltrading에 사용자·거래소별 `referral_attributions` 원본을 추가했습니다. UID는 암호화 저장하고 사용자 화면에는 끝 4자리만 표시합니다.
- 레퍼럴 사용자는 대시보드에서 UID를 제출하며, 운영자는 Affiliate Portal과 대사한 뒤 관리자 `레퍼럴 귀속 관리`에서 `verified`, `rejected`, `expired`, `pending`을 판정합니다.
- 레퍼럴 회원의 `allowed_exchanges`는 전역 활성 거래소와 사용자별 `verified` 거래소의 교집합입니다. 단순히 `referral` 등급이거나 API 키가 유효하다는 이유만으로 실행 권한을 부여하지 않습니다.
- 클라이언트 설정에서 미승인 거래소의 API 입력·연결 확인·분석 선택·실주문 선택을 비활성화하고 상태와 가입/승인 동선을 표시합니다. 시작 직전과 약 60초 정책 갱신에서도 허용 목록을 다시 적용합니다.
- 유료 `pro_coin`·`premium` 사용자는 레퍼럴 귀속 게이트를 적용하지 않습니다. 주문이 사용자 PC에서 거래소로 직접 전달되는 구조라 변조된 비공식 클라이언트까지 절대 차단한다고 주장하지 않습니다.
- 거래소 가입 URL은 HTTPS와 각 거래소 공식 도메인만 관리자 저장을 허용합니다. API Key·Secret·Passphrase·KYC 자료는 daltrading이 수집하지 않습니다.
- 서버 전체 회귀 `41 passed, 3 subtests passed`, 클라이언트 전체 회귀 `1,336 passed, 6 skipped`를 통과했습니다. 레퍼럴·설정·실행 안전 집중 회귀도 `32 passed`입니다.
- Windows EXE는 `pending_windows_rebuild`입니다. 운영 daltrading 배포, 실제 OKX·Bitget·Bybit Affiliate Portal UID E2E, 새 Windows 설치본 SHA·로그인·설정·PAPER/LIVE 검증 전에는 배포 완료로 보지 않습니다.
- 통합 운영 정본: `daltrading/REFERRAL_MEMBERSHIP_OPERATIONS_20260727.md`

## v3.9.0.6 Source Candidate 2 · 거래소 체결 동기화 예산·전략 표본 정합 (2026-08-04)

- KRW·USDT 통화별 챔피언–챌린저 문장을 추가하는 과정에서 `ai_report_widget.py` import를 막던 f-string 구문 오류를 수정했습니다. 전체 활성 Python 구문 검사와 실제 `main.py` 로그인·대시보드·AI 리포트 위젯 초기화까지 다시 확인했습니다.
- AI 커스텀을 숨은 부가기능이 아니라 `배우기 → 만들기 → 검증하기 → 실행하기 → 개선하기 → 공유하기`를 연결하는 NoahAI의 대표 `AI 전략 운영체제`로 정본화했습니다.
- 인앱 `NoahAI 소개`·`AI 커스텀` 매뉴얼과 전략 화면에서 현재 공개 배포본, v3.9.0.6 소스 후보, 배포 후 운영 게이트, 향후 생태계를 구분합니다. AI 멘토·고급 주문 표현·워크포워드는 Source Candidate 2 구현으로, 실제 공유·다운로드·마켓은 후속 단계로 표시합니다.
- NoahAI는 `AI 금융 의사결정 인프라`, AI 커스텀은 `AI 전략 운영체제`, 전략 허브는 `검증 기반 허브`로 대외 설명을 통일했습니다. TradingView 완전 대체·모든 Pine 지원·수익 보장 표현은 사용하지 않습니다.
- 전체 제품 로드맵을 `docs/AI_CUSTOM_STRATEGY_OS_PRODUCT_ROADMAP.md`에 정리하고 기존 주문 표현력 항목을 AI 멘토·제작소·안전 DSL·검증 연구소·국면 오케스트레이터·실행 OS·전략 여권·신뢰/법무의 8개 계층 아래로 재배치했습니다.
- 분석 루프가 10초마다 `force=True`로 최근 체결 최대 200건을 다시 호출해 거래소별 30초 제한까지 우회하던 API 낭비를 제거했습니다.
- 수익성 검증은 거래소 API 응답이 아니라 로컬의 진입-청산 완료 거래를 우선 사용하고, 완료 거래가 없을 때만 확정 체결 원장을 보조 표본으로 사용합니다.
- LIVE의 전체 체결 대조는 시작·사용자 수동 가져오기·기본 5분 저빈도 복구로 제한하고, 평상시에는 마지막 체결 시각/거래 ID 이후만 증분 조회합니다.
- 신규 주문은 로컬에 보존한 주문 ID만 기본 10초 간격으로 최종 상태까지 확인합니다. PAPER·LEARNING은 미확정 실주문이 없으면 개인 체결 API를 호출하지 않습니다.
- 취소·거절·만료·실패 주문은 terminal 상태로 종료해 다음 주기마다 같은 주문 ID를 영구 재조회하지 않습니다.
- 어댑터가 인증 사용자 체결 스트림을 명시적으로 제공하면 스트림 이벤트를 우선 저장하고, 스트림이 정상인 동안 주기 REST 이력 호출을 생략하는 공통 계약을 추가했습니다. 현재 Binance 공개 시세 WebSocket을 개인 체결 스트림으로 오인하지 않으며, 거래소별 인증 스트림 실연결은 후속 외부 검증 대상입니다.
- 공통 CCXT 이력 조회와 Binance 계정 체결 조회가 `since`/`startTime` 및 마지막 거래 ID 커서를 받을 수 있도록 확장했습니다. 증분 인자를 지원하지 않는 레거시 어댑터만 저빈도 전체 조회로 호환합니다.
- 실제 체결 원장에 `realized_pnl`을 보존하고 로컬 조회·증분 커서 계약을 추가했습니다. 기존 DB는 행 삭제나 재작성 없이 컬럼만 멱등 보강합니다.
- AI 커스텀은 TradingView·Pine·텍스트·PDF·OCR·영상·YouTube에서 전략을 AI로 구조화하는 목적을 유지합니다. 공개 배포본의 안전 실행 범위와 Source Candidate 2의 고급 부분청산·추적손절·재진입 계획을 구분하며, 임의 Pine/Python·모든 사용자 정의 함수와 거래소별 인증 스트림·서명 webhook은 지원 완료로 표시하지 않습니다.

- 실제 사용자 `data/260802_Teayu`의 DB·학습 저장소·거래 로그·안정성 로그를 Binance·Upbit·Bithumb·Bybit·OKX·Bitget별로 다시 대조했습니다.
- KRW 현물 `H/KRW`와 CCXT 파생상품 `AVAX/USDT:USDT`를 `coin + USDT`로 다시 만들던 공통 최근 거래 조회를 제거하고 거래소 원본 심볼을 정확히 조회합니다.
- 학습 데이터가 쌓인 뒤 완료 거래 최소값이 3~5건으로 상승하면서, 완료 이력이 없는 심볼의 첫 거래를 영구 차단하던 순환 조건을 제거했습니다. 시장 적합성·신호·AI 신뢰도 가드는 유지합니다.
- 완료 거래가 없을 때의 승률 0%·손실률 0%를 실제 저성과·저손실로 동시에 해석하던 신뢰도 왜곡을 제거했습니다.
- 오늘·주간·월간·실시간 AI 리포트와 챔피언–챌린저 산출물은 KRW·USDT 등 환산되지 않은 통화를 합산하지 않습니다. 통화 구성이 다르면 금액 우열 판정은 `inconclusive_currency_boundary`로 보류합니다.
- Binance 거래내역 가져오기의 `200건`은 API 최근 조회 상한임을 표시하고, `API 조회 / 체결원장 신규 / 청산 성과 신규`를 분리해 반복 조회가 중복 저장처럼 보이지 않게 했습니다.
- Binance 개별 체결을 공통 실제 체결 원장에 저장한 뒤 `trade_log` 청산 성과로도 다시 넣던 레거시 이중 기록을 중단했습니다. 기존 `reason=binance_import` 행은 삭제하지 않고 승률·PnL·학습·AI 리포트에서 제외합니다.
- 동시에 실행된 다른 PID를 이전 비정상 종료로 오인하지 않고 `parallel_runtime_session_detected`로 구분하며, 한 프로세스의 정상 종료가 다른 프로세스의 활성 마커를 삭제하지 않습니다.
- Source Candidate 2 전체 자동 회귀는 `1,332 passed, 6 skipped`이며 문서 정합·빌드 포함·활성 소스/격리 감사도 PASS입니다.
- 이번 변경은 macOS 소스·사용자 데이터 재현 기준입니다. Windows EXE는 `pending_windows_rebuild`이고 실제 API 주문·장시간 실행 검증 전이므로 배포 완료 또는 6개 거래소 실거래 완료로 표현하지 않습니다.

## v3.9.0.5 Fix Patch 5 · 갱신 생존성·체결 기록 연결 (2026-08-01)

- 탭 전환 중 `winfo_viewable=False`가 한 번 발생하면 반복 예약이 사라져, 탭을 다시 눌렀을 때만 한 번 갱신되고 재정지하던 생명주기 결함을 수정했습니다. 숨김 중 API 호출은 하지 않지만 2초 생존 확인 예약은 유지합니다.
- 포지션 카드의 건수 옆에 마지막 적용 시각을 표시해 사용자가 갱신 정지 여부를 직접 확인할 수 있습니다.
- Binance 네이티브 주문도 CCXT 거래소와 같은 `exchange_execution_log`에 주문 접수·확정 체결을 기록합니다.
- Upbit·Bithumb·Bybit·OKX·Bitget의 LIVE 진입 시 실제 체결 원장뿐 아니라 `trade_log` 진입 행도 멱등 생성합니다. 과거에는 청산 시 존재하지 않는 진입 행을 UPDATE하여 거래통계와 AI 리포트에서 거래가 누락될 수 있었습니다.
- `NEW/PENDING` 접수를 체결 성공으로 간주해 내부 포지션을 만드는 모순을 제거했습니다. 실제 체결 확정 후에만 포지션·성과 생명주기를 시작합니다.
- 공통 거래 엔진은 UI 탭/가져오기 버튼과 독립적으로 30초 간격으로 거래소 이력과 미확정 주문 ID를 대조합니다.
- 실제 사용자 `Teayu` 데이터 점검에서 앱 시작 마이그레이션이 `trade_log`를 삭제한 뒤 청산 완료 행만 복원하고 `order_id` 등 신규 컬럼을 누락하는 결함을 확인했습니다. 테이블 재생성을 제거하고 행 수를 보존하는 증분 컬럼 보강으로 교체했습니다.
- 스키마 검증이 실패하면 경고 후 계속 거래하던 경로도 제거했습니다. 거래 기록 DB가 준비되지 않으면 거래 엔진 초기화를 실패 폐쇄해 기록 없는 실체결을 방지합니다.
- 거래소 대시보드 첫 KPI는 실제 체결 건수로 표시하고, 승률·PnL 분모는 청산 완료 성과로 유지합니다.
- AI 리포트의 오늘 거래에 실제 체결 목록을 별도로 표시합니다. 청산 전 체결은 내역에는 나오지만 승률·PnL로 계산하지 않습니다.
- 전체 자동 회귀는 `1,298 passed, 6 skipped`이며, 사용자 DB형 마이그레이션 보존·초기화 실패 차단을 포함한 Fix Patch 5 집중 회귀는 `6 passed`입니다.
- 이번 수정은 소스와 자동 회귀 기준입니다. 새 Windows EXE, 실제 계정, 재시작, 장시간 갱신은 배포 후 검증 전까지 완료로 표현하지 않습니다.

## v3.9.0.5 Fix Patch 4 · 계좌 상태·소스·문서 최종 정합 (2026-08-01)

- 업데이트 후 처음 실행하면 `실거래 시작 전 필수 안내`가 한 번 표시됩니다. 이후에도 대시보드 하단과 설정 상단, `메뉴얼 → 실거래 준비`에서 다시 열 수 있습니다.
- 창 제목·인앱 매뉴얼·대시보드 하단에 `v3.9.0.5 Fix Patch 4`를 표시해 같은 버전 번호의 이전 설치본과 구분합니다.
- 실거래 준비 화면에서 거래소·증권사별 표시 차이, 회원등급, PAPER/LIVE 조건, 증권 API 계약, 최소 주문 검증 순서를 한 번에 확인할 수 있습니다.

- Upbit·Bithumb 현물 계좌는 `보유자산`, Binance·Bybit·OKX·Bitget 선물 계좌는 `포지션`으로 표시합니다. 현물 보유 코인을 빈 선물 포지션으로 오인하던 화면 계약을 바꾼습니다.
- PAPER 모드에서는 엔진 로그에만 남던 가상 포지션을 대시보드에 표시합니다. 포지션 카드는 `PAPER`를 표시하고 가상 미실현 PnL을 갱신합니다.
- PAPER 계좌 카드는 가상 원금을 임의로 만들지 않습니다. `실잔고 미사용`, 가상 포지션 수, 가상 미실현 PnL만 보여 주어 실계정과 혼동하지 않게 했습니다.
- 선물 포지션 조회는 정상 0건·실시간·캐시·조회 오류를 구분합니다. Binance API 오류를 `0개 활성`으로 숨기지 않습니다.
- Bithumb 포함 5개 CCXT 거래소의 주문 접수·확정 체결·NoahAI 성과 통계 분리와 주문 ID 복구 계약을 유지했습니다.
- 증권 4개 공식 계약·설정 자동 이전·어댑터·LIVE AND 권한을 통합 점검했습니다.
- 대시보드 정의 전용 메서드 23개와 레거시 테마 소스 6개를 활성 패키지 밖에 복구 가능하게 격리했습니다. 사용자 거래 데이터·로그·인증정보는 해당 격리에 포함되지 않습니다. 정본은 `docs/SOURCE_QUARANTINE_MANIFEST_20260801.md`입니다.
- 최소 주문 규격 검증 예외는 주문 차단으로 처리하고, TP/SL 감시·복구 오류를 명시적으로 기록합니다.
- 전체 소스 회귀는 `1,292 passed, 6 skipped`이고 활성 소스·빌드 포함·문서 정합 검사가 PASS입니다.
- Windows EXE는 재빌드 전이며 manifest는 `pending_windows_rebuild`입니다. 공개 Fix Patch 1 EXE에 Fix Patch 4가 들어 있다고 판단하면 안 됩니다.

## v3.9.0.5 Fix Patch 3 · 증권사 공식 API 계약·LIVE 권한 통일 (2026-08-01)

- 키움·신한·미래에셋·한국투자 API 선택지를 공식 계약 기준으로 다시 분류했습니다. XingAPI는 LS증권 API이므로 신한에서 제거했고, 한국투자 KIS 경로는 미래에셋에서 분리했습니다.
- 과거 설정은 앱 시작 시 새 계약명으로 자동 이전됩니다. API 키와 계좌값은 보존하며, XingAPI 오분류 계정은 잘못된 신한 주문을 막기 위해 신한 활성/LIVE 값만 꺼집니다.
- 한국투자 KIS의 공식 토큰·헤더·TR ID·실전/모의 서버·주문 성공 판정을 구현했고, 키움 OpenAPI+의 Windows OCX/로그인/계좌/주문 함수 준비상태를 실행 직전에 확인합니다.
- 신한과 미래에셋은 추정 URL을 사용하지 않고 증권사 제휴 계약에서 발급된 운영 프로필을 사용합니다. 신한은 공식 `dataHeader/dataBody`, HMAC `hsKey` 형식을 지원합니다.
- PAPER가 켜져 있으면 외부 주문은 항상 나가지 않습니다. PAPER가 꺼진 뒤에도 전역 LIVE와 해당 증권사 LIVE를 모두 켜고 API 연결·계좌·가드레일이 정상이어야 실제 주문이 실행됩니다.
- Windows EXE 재빌드와 실제 증권 계정 운영 검증은 배포 단계에서 진행합니다. 현재 소스 테스트는 Windows OCX 또는 실계정 체결을 증명하지 않습니다.

## v3.9.0.5 Fix Patch 2 · 확정 체결·통계 복구·Binance 포지션 갱신 (2026-08-01)

- 비바이낸스 거래소의 잔고·포지션·주문이 같은 공통 어댑터를 사용하도록 연결 정본을 통합했습니다. Bithumb 주문내역이 정상 0건일 때 이후 조회를 영구 중단하던 오류도 수정했습니다.
- Binance 포지션 화면은 현재 정본 클라이언트의 시간 동기화된 조회를 사용합니다. 설정 저장 뒤 Trader 참조도 함께 교체하며, 조회 오류를 빈 포지션으로 숨기지 않습니다.
- Upbit·Bithumb·Bybit·OKX·Bitget의 주문 접수와 실제 체결을 분리했습니다. `NEW/PENDING/OPEN`은 주문 접수 원장에 남기고, 개별 주문 조회에서 양수 체결수량이 확인된 경우에만 실제 체결 원장에 기록합니다.
- 전체 거래목록 API를 제공하지 않는 Bithumb도 NoahAI가 제출해 주문 ID를 보존한 주문은 `fetchOrder`로 다시 확인할 수 있습니다. `거래 통계 가져오기`가 이 주문별 복구를 먼저 실행합니다.
- 거래 통계의 실제 체결 건수·금액은 확정 체결 원장을 사용합니다. 승률·PnL은 진입부터 청산까지 연결된 완료 거래만 사용하며 서로 다른 분모를 섞지 않습니다.
- 청산 연결 키를 심볼 하나에서 거래소·방향·진입 주문 ID로 강화하고, 진입·청산 DB 기록 실패를 주문 성공 뒤 조용히 숨기지 않습니다.
- 당시에는 증권 API 이름 등록과 실거래 준비 상태를 분리하고 구 `kiwoom_api`·`xingapi` 경로를 차단했습니다. 이 임시 전면 차단 정책은 Fix Patch 3의 공식 계약명·설정 이전·전역/증권사/런타임 AND 권한으로 대체되었습니다.
- PyInstaller spec을 `build_safe.py` 단일 생성기로 통합하고 Python 소스 폴더 이중 번들, 폐기 테마 hidden import, Windows 대상 `win32_setctime` 누락을 정리했습니다.
- 구조 감사 수정 포함 전체 자동 회귀 1,269개가 경고 없이 통과했고 6개가 환경 의존으로 건너뛰었습니다. 새 Windows EXE와 실제 계정 6개 거래소, 최소 매수·매도, 재시작, 24~72시간 안정성은 아직 검증 전입니다.
- 현재 manifest는 `pending_windows_rebuild`입니다. 공개 Fix Patch 1 EXE에는 이 변경이 포함되지 않습니다.

## v3.9.0.5 Update Patch 1 · 거래소 공통 원장·화면 조회·자동업데이트 (2026-07-31)

- Binance 최소금액을 맞춘 수량이 step 내림·SELL 참조가·주문 직전 시세 변화로 다시 5 USDT 아래가 되던 계산 순서를 수정했습니다. 목표금액에 안전폭을 적용하고 거래소 제출 직전 실제 `MIN_NOTIONAL`과 수량 step을 다시 검증합니다.
- Upbit·Bithumb·Bybit·OKX·Bitget 주문은 공통 CCXT 규격 계약을 사용합니다. 실제 market의 최소수량·최소금액과 정밀도를 적용하며, KRW 최소금액 정보 누락 시 5,000 KRW로 실패 폐쇄합니다. 다중 거래소 위험 분할 후 최소치 미달 시 승인 수량을 몰래 늘리지 않고 해당 주문을 건너뜁니다.
- 거래소 조회가 12초를 넘긴 뒤 전역 조회 키를 영구 점유하던 상태를 해제하고 늦은 이전 응답을 세대 ID로 폐기합니다. 거래소와 증권 포지션 카드 모두 같은 비동기 갱신 계약을 사용합니다.
- 설정을 저장한 뒤 이미 생성된 거래 엔진이 이전 `paper_trading`·실제 주문 거래소 값을 계속 사용하던 런타임 동기화 누락을 수정했습니다. 전역 PAPER가 켜져 있으면 모든 거래소에서 PAPER가 우선하며, 끈 뒤 Bybit를 실제 주문 대상으로 선택·저장하면 다음 실행 판정부터 LIVE 계약을 사용합니다.
- Bithumb의 정상 CCXT 체결 상태 `closed`를 실패로 오인하던 청산 판정을 수정했습니다. 정상 체결 뒤 포지션 종료·NoahAI 거래 기록·통계 갱신이 이어집니다.
- Binance 전용이던 `거래 통계 가져오기`를 Upbit·Bithumb·Bybit·OKX·Bitget까지 확장했습니다. 거래소의 실제 체결 내역은 별도 실행 원장에 중복 없이 저장하며, NoahAI가 진입부터 청산까지 추적한 완료 거래의 승률·손익과 섞어 만들지 않습니다.
- 5개 CCXT 거래소를 `새 주문 접수 원장·과거 체결 API·완료 주문 폴백·수동 체결 백필` 기능 계약으로 통일했습니다. 과거 API 미지원은 더 이상 `체결 0건`으로 오인하지 않으며, 새 NoahAI 실주문은 지원 여부와 관계없이 로컬 실제 체결 원장에 기록하고 실행 중 가능한 과거 원장도 중복 없이 정합화합니다.
- 블록체인에서 주식/증권·자산 통합 등 다른 서비스로 이동할 때 대시보드 공용 UI 전달 큐까지 취소하던 수명주기 오류를 수정했습니다. 다시 거래소 탭으로 돌아오면 잔고·포지션 결과가 화면에 정상 반영됩니다.
- 빠른 화면 왕복 시 파괴된 이전 화면과 새 화면이 같은 잔고 API를 겹쳐 호출하던 구조를 대시보드 전역 조회 ID로 통합했습니다. UI 전달 큐는 1,024개 상한과 동일 키 최신값 병합을 사용합니다.
- 자동업데이트 최초 확인은 초기 연결과 경쟁하지 않게 실행 약 15초 뒤 백그라운드에서 수행하고, 이후에는 사용자 설정 주기(기본 6시간, 1~72시간) 또는 수동 확인을 사용합니다. 같은 `3.9.0.5` Fix Patch도 manifest EXE SHA 차이로 감지합니다.
- 코드서명 인증서 요구를 제거했습니다. 무료 배포 정본은 GitHub HTTPS + manifest SHA-256 필수 검증이며, SHA 누락·불일치·GitHub 외 URL은 실패 폐쇄합니다. 무서명 EXE의 Windows 평판 경고는 이 방식으로 제거되지 않습니다.
- 설정 갱신 로그에서 전체 설정 사전과 비밀값이 노출될 수 있는 출력을 제거하고 변경 키 이름만 안전하게 기록합니다.
- 현재 공개된 v3.9.0.5 Fix Patch 1 EXE에는 위 변경이 포함되지 않습니다. Update Patch 1 소스는 `pending_windows_rebuild`이며 Windows 재빌드·manifest SHA 생성·설치·실계정 검증 후 배포합니다.

## v3.9.0.5 Fix Patch 1 · 거래소 잔고·설정 도움말 정합화 (2026-07-30)

- 잔고 3칸을 `KRW/USDT + BTC + ETH`로 고정하던 표시·어댑터 계약을 제거했습니다. 기준통화 다음에는 실제 0 초과 보유자산을 최대 2개 표시하며, 나머지 빈 칸은 오류가 아닌 `추가 자산 없음`으로 표시합니다.
- Upbit·Bithumb·Bybit·OKX·Bitget CCXT 어댑터가 BTC·ETH만 잘라 반환하지 않고 거래소가 반환한 모든 양수 보유자산을 공통 형식으로 전달합니다.
- 국내 현물은 KRW, 해외 선물은 USDT를 기준통화로 사용합니다. 거래소 카드의 3칸 한계는 `최대 3개 표시`로 명시하며 거래 판단·수량 계산용 전체 잔고 객체는 잘리지 않습니다.
- Binance 전용 클라이언트도 한 번의 계정 API 응답에서 전체 양수 자산과 계정 요약을 함께 만들어 중복 계정 조회를 제거했습니다.
- AI 어시스턴트가 `일반·거래소 선택·거래소 API·AI 엔진/API·고급 매매 계층·AlphaArena·AI 시스템 상태·업데이트` 8개 설정 탭의 v3.9.0.5 정본을 질문별로 검색합니다.
- 설정 질문에는 API 키 값을 제외한 현재 모드·관찰/주문 범위·다중 실행·포지션·최상단·AI 커스텀·고급 정책·증권 실행 상태를 함께 제공합니다. 외부 AI가 꺼져 있어도 설치된 설정 정본으로 로컬 답변하며, 설정 변경은 기존 허용 키·최종 확인 절차를 그대로 유지합니다.
- 설정 8개 탭 상단에 `AI에게 묻기`를 추가했습니다. 누르면 해당 탭의 목적·현재 저장 상태·변경 영향·권장값·주의사항을 질문하고, 설정 창은 입력값을 잃지 않은 채 숨겨졌다가 다시 열립니다.
- AI 어시스턴트의 `자주 하는 질문` 열을 독립 스크롤로 바꿔 질문이 늘어나도 하단 전송·음성입력·설정관리·차트분석 메뉴를 가리지 않습니다.
- AI 학습 자산 판정을 BTC·ETH/USDT 문자열 가정에서 거래소·자산유형·실제 심볼 형식 계약으로 바꿨습니다. KRW-XRP·SOL/KRW 같은 국내 알트와 모든 지원 증권사·ETF가 올바른 서비스 학습 화면에 포함됩니다.
- 데모 잔고의 BTC·ETH 0 고정 슬롯과 국내 거래소 로그의 USDT 고정 가정을 제거했습니다.
- 전체 자동 회귀 `1,233 passed, 6 skipped`.

## v3.9.0.5 AI 커스텀 역할·실행 파이프라인 정합화 (2026-07-30)

- 거래소 실행 루프가 시작됐는데 카드가 계속 초록색 `시작`과 `정지`로 남던 UI 상태 분리를 수정했습니다. 시작·정지 결과가 버튼, 상태 배지, 전체 제어에 한 번에 반영되며 실제 워커와 5초마다 재대조합니다.
- PAPER 실행을 `정지`로 잘못 표시하던 상태 문자열 판정을 교정해 LIVE·PAPER·LEARNING을 각각 구분합니다.
- Upbit·Bithumb·Bybit·OKX·Bitget 잔고 한 번 갱신에 인증 잔고 API를 두 번 호출하던 중복 경로를 제거했습니다. 같은 응답에서 화면용 계정 요약을 만들며 12초 안에 값 또는 조치 가능한 실패 상태를 표시합니다.
- AI 커스텀의 제품 목적을 “익숙한 시중 전략의 실행력 + NoahAI의 시장국면 판단·감독·보호·기록”으로 확정했습니다. 학습은 초기 사용의 전제조건이 아니라 전략 선택·국면 감지·비용·체결 품질을 개선하는 후행 기능입니다.
- 화면의 전략 역할을 `기본 AI 후보 재확인 (권장)`과 `사용자 전략 원형 독립 실행 (고급)`으로 구분했습니다. 검증 통과 운용/1배·1% 제한시험과 LEARNING/PAPER/LIVE는 별도 축으로 표시합니다.
- Binance·Bybit·OKX·Bitget·Upbit·Bithumb·주식/ETF 경로가 공통 `TradeCandidate`/`ExitPlan` 계약을 사용하며, AI 커스텀 활성 풀은 후보당 한 번만 평가합니다.
- 과거 단일 전략 규칙과 거래소별 글로벌 커스텀 설정을 실행 정본에서 제거했습니다. 사용자 전략 활성화는 더 이상 글로벌 TP/SL·레버리지·기본 AI 신호 임계값을 바꾸지 않습니다.
- `confirm`은 NoahAI 후보를 사용자 조건으로 재확인합니다. `independent`는 기본 AI가 HOLD여도 사용자 전략이 방향·조건을 결정하며, 후단 NoahAI 합의 임계값·패턴 재심사·전체 전략 수익성으로 다시 바꾸거나 차단하지 않습니다.
- 두 역할 모두 시장국면 적합성·데이터/API 상태·계좌 위험예산·포지션/집중도·최소수량·중복주문·실행권한·주문 규격 안전 경계를 통과합니다.
- 사용자 전략 TP/SL은 글로벌 기본값과 섞지 않으며 Binance 거래소 보험 주문에도 승인 전략의 요청값을 유지합니다. 위험예산 충돌은 SL 변경이 아니라 수량 축소 또는 명시적 주문 차단으로 처리합니다.
- LEARNING은 기본 AI·AI 커스텀·시장국면·수익성·후행 전략·계좌 위험·수량·청산계획·최종 주문 규격까지 평가하고, 외부 상태 변경과 신규 주문 제출만 차단합니다.
- 과거 adaptive 연결부가 사용자 전략의 국면·성과 조정값을 글로벌 TP/SL·레버리지·신호 임계값에 쓰던 중첩 경로를 제거했습니다. 선언된 조정은 선택된 전략 후보에만 적용됩니다.
- 주식 전용 커스텀 `signal_threshold` 재평가를 제거해 코인과 같은 후보 계약을 사용합니다. 고급 주식 전략도 NoahAI 전체 수익성·합의 임계값으로 재차단하지 않습니다.
- 코인 유니버스에서 알트코인을 메이저로 재분류하던 로직을 제거하고, 분류를 보존한 채 부족한 총수만 다음 고득점 후보로 채웁니다. 통합 거래소에는 현재 국면을 선정기에 전달하고 주식/ETF 기본 후보는 거래대금·유동성 정보 우선으로 정렬합니다.
- 일반 역할은 공통 `SelectionPolicy`로 사용자 고정 종목과 NoahAI 자동 후보를 합친 뒤 기본 AI 후보를 만들고 커스텀 조건으로 재확인합니다.
- 고급 역할은 별도 `StrategyUniversePolicy`로 사용자 고정·포함/제외 종목, 최소 거래대금, 최대 스프레드, 변동성 범위와 후보 수를 로컬에서 결정한 뒤 사용자 전략을 독립 평가합니다. 후보선정 때문에 LLM을 종목별 추가 호출하지 않습니다.
- AI 커스텀의 `거래소/증권사/전체 자산` 선택은 전략 적용 범위입니다. 각 거래소·증권사는 자기 지원 심볼과 시장 데이터로 후보를 만들며 다른 대상의 목록을 복사하지 않습니다.
- 전략별 `regime_scope`를 `market/symbol/both/none`으로 명시했습니다. 기본값은 전체 시장이며, 선택한 국면 데이터가 없을 때 다른 범위로 조용히 대체하지 않습니다.
- 여러 실제 주문 거래소·증권사는 기본적으로 각각 실행합니다. 같은 자산·방향·전략·시간창을 하나의 기회 ID로 묶어 통합 예상 노출·손실과 대상별 결과를 기록하며, 동일 대상·계좌·신호의 재제출만 중복 차단합니다.
- 설정에서 사용자가 선택한 경우에만 총위험 분할 또는 우선순위 한 곳 실행을 적용합니다. 한 곳 실행은 저장된 비용표·지정 대상이 없으면 실제 주문 목록의 첫 번째 대상을 사용합니다. 위험 초과 시 전략 SL을 변경하지 않고 수량을 줄이거나 주문을 차단합니다.
- 주식 정보의 `자동매매에 추가`가 실행부가 읽지 않던 `watchlist`에만 저장되던 오류를 수정했습니다. 실행 정본인 `stock_auto_trading.symbols`로 통합하고 기존 watchlist도 자동 호환합니다.
- 공통 `ExitPolicy`를 추가해 내부 계산 단위를 `fraction` 하나로 고정하고 `저장된 기본 폴백`, `현재 종목에 적용된 청산값·근거`, `거래소에 실제 제출·확인된 보험 TP/SL`을 별도 필드와 실시간 로그로 표시합니다. 전략값과 보험 주문 반올림값이 서로 덮어쓰이지 않습니다.
- Binance는 실제 거래소에서 확인한 보험 가격과 주문 ID 상태를 기록하고, Bybit·OKX·Bitget은 어댑터 제출 결과를 기록합니다. Upbit·Bithumb 현물과 현재 증권 경로처럼 서버 측 보험 주문을 사용하지 않는 경우에는 `모니터 청산·보험 주문 미제출` 상태를 명시합니다.
- 통합 거래소는 실제 어댑터와 지원 마켓을 확인할 수 없으면 기본 심볼을 억지로 분석하지 않고 후보 선정을 중단합니다. 이로써 다른 거래소 심볼 혼입과 반복 `Invalid symbol` 호출을 차단합니다.
- 증권 자동 후보는 KOSPI와 KOSDAQ을 함께 조회하고, `주식만/ETF만/통합` 모드에 따라 거래대금 우선 후보를 최대 8개 선정합니다. 통합은 주식 4개·ETF 4개를 기본으로 균형 배분하며 거래정지 종목은 제외합니다.
- 시장분석 AI는 기존 15분 상태 캐시와 영속 호출예산을 유지합니다. 별도 경로였던 포지션 크기 AI도 15분 캐시·방향/1% 가격/0.10 신뢰도 변화·실패 120초 쿨다운을 적용해 높은 신뢰도나 SHORT 신호만으로 반복 호출하지 않습니다.
- 진입 전 손실패턴 AI는 동일 신호·동일 손실표본을 5분 재사용하고 실패 시 120초 재시도를 제한합니다. 성과 기반 임계값 최적화도 10거래 이후 새 5거래마다 한 번만 역할별 Provider로 호출하며, 실패한 동일 표본을 매 사이클 다시 호출하지 않습니다.
- Provider 활성 상태를 기본 Provider 하나가 아니라 `신호 분석/패턴/포지션 크기/손익 복기/파라미터 최적화` 역할별 라우팅 자격증명으로 판정합니다.
- AI 조기청산 설정의 fraction과 포지션 PnL의 퍼센트 포인트 비교 단위를 통일했고, UnifiedTrader의 동적 기본 TP/SL도 저장 설정 정본을 사용합니다.

## v3.9.0.5 설정 정본·실행 모드 통합 (2026-07-30)

- Teayu_02 압축 로그에서 거래 직후 지연 잔고 갱신 `NameError` 90건, 비정상 종료 감지 4건, 파괴된 AI 설정 입력창 `TclError` 1건을 확인하고 스레드·창 수명주기를 수정했습니다.
- v3.9.0.3에서 AI 키에만 추가된 필수 `keyring`·Windows 보안 저장소 의존성이 기존 무설치 정책 및 거래소·증권사 키 저장 방식과 불일치함을 확인했습니다. v3.9.0.5는 이를 철회하고 사용자별 로컬 설정 정책으로 통일했으며, 보안 저장 실패가 일반 설정 저장을 차단하지 않습니다.
- API 키의 앞뒤 일부를 진단 로그에 남기던 출력도 제거하고 설정 여부·길이만 기록합니다. 설정과 백업은 지원용 로그 묶음에 포함하지 않습니다.
- Binance·Upbit·Bithumb·Bybit·OKX·Bitget 거래소 탭은 별도 UI adapter 대신 실거래 루프의 기존 ExchangeManager를 함께 사용합니다. 숨은 탭에서 소실되던 최초 조회를 탭 선택 시 즉시 다시 깨우고, 12초 조회 제한과 `API 키 없음/API 키 오류/연결 실패/시간 초과` 상태를 표시해 영구 `로딩 중`을 제거했습니다.
- 구버전 TP/SL의 퍼센트 포인트 `0.18/0.2`는 내부 fraction `0.0018/0.002`로 최초 1회 변환·저장합니다. 정상 마이그레이션은 WARNING이 아닌 INFO로 기록합니다.
- 시작할 때마다 균형 전략 프리셋이 `0.18/0.2`를 다시 Trader에 주입하던 별도 원인을 제거했습니다. 프리셋은 표시 단위를 `percent_points`, 주문 단위를 `fraction`으로 명시하며 Binance와 통합 거래소 엔진이 모두 최종 입력을 재검증합니다.
- 런타임 프로파일이 설정되지 않았는데도 `balanced`를 자동 적용해 저장된 TP/SL·레버리지를 덮어쓰던 숨은 기본 동작을 제거했습니다. 프리셋은 사용자가 명시적으로 선택한 경우에만 적용됩니다.
- 배열/dict OHLCV 혼용, OKX 문자열 종목, dict가 아닌 분석 결과를 정규화하고 비정상 분석은 HOLD로 격리했습니다. 최적화 datetime 저장과 포지션 동시 제거 오류도 수정했습니다.
- Binance TP/SL은 실제 열린 포지션 반영을 먼저 확인하고, 이미 포지션이 없으면 `-4509` 보호주문을 반복 제출하지 않습니다.
- 설정 탭을 운용 모드 → 운용 범위·주문 권한 → 거래 연결 → AI 엔진 → 고급 정책 → AlphaArena 실험 → 자동 관리 진단 → 업데이트 순으로 정리했습니다.
- `설정 → 일반` 상단에서 설정 정본 버전, LEARNING/PAPER/LIVE, 호환 보관 수와 모순 수를 바로 확인합니다.
- 실제 실행 코드가 소비하지 않던 과거 최상위 설정 20개를 신규 템플릿에서 제거했습니다. 기존 사용자 값은 `_legacy_settings_v3905`로 1회 보관하며 실행에는 사용하지 않습니다.
- AlphaArena의 `alpha_arena`와 `alphaarena_*` 이중 저장을 `alpha_arena` 하나로 통합했습니다. 기존 로컬 키는 보존하고 v3.9.0.3의 미완료 `credential_ref`만 남은 경우 새 키를 입력하기 전까지 참조를 삭제하지 않습니다.
- 활성 거래소 중복을 제거하고 학습 범위를 정본과 동기화하며 실제 주문 범위가 분석·학습 범위를 벗어나지 않도록 자동 교정합니다.
- v3.9.0.3 과도기 주문 목록은 명시적 사용자 확인 여부를 증명할 수 없어 v3.9.0.5 최초 실행에서 호환 보관 후 0개로 잠급니다. LIVE 사용자는 설정에서 주문 거래소를 다시 선택·저장해야 합니다.
- 증권 `stock_auto_trading.auto_start/enabled` 이중값은 `auto_start` 기준으로 정규화하고 호환 필드만 같은 값으로 유지합니다.
- 설정 파일을 비밀값 없이 검사하는 `scripts/audit_settings_contract.py`와 계약 회귀 테스트를 추가했습니다.
- 거래소 준비도와 AI Provider 진단은 `--account`로 대상을 명시하고 사용자 설정을 변경하지 않는 읽기 전용 방식으로 수정했습니다.
- 빈 잔고·계정 응답을 성공으로 캐시하던 문제, 인증·IP 오류를 일반 연결 실패로 흐리던 문제, 준비도 점검 프로세스 정리 경고를 수정했습니다.
- Binance 폴백 어댑터의 포지션·미체결·거래내역·레버리지·마진·펀딩 기능을 실제 BinanceClient 위임으로 연결했습니다.
- 커스텀 전략 파일은 로드·저장 시 사용자 전용 권한 `0600`을 강제합니다.
- 공개 시세·분석과 실잔고·포지션·주문의 API 키 요구 조건이 다름을 설정 화면에 명시했습니다.
- v3.9.0.3 후보의 LEARNING/PAPER/LIVE, 페이퍼 상태 격리, 멀티 AI API, 안정성 수정은 v3.9.0.5 후보에 포함됩니다. v3.9.0.3 Windows EXE는 별도 고객 배포본으로 간주하지 않습니다.
- 상세 사용자 기준은 `docs/SETTINGS_REFERENCE_v3.9.0.5.md`와 앱 `사용자 매뉴얼 → 업데이트`에서 확인합니다.
- 사용자 매뉴얼 상단에 핵심 기능 바로가기·전체 본문 검색·검색어를 AI 어시스턴트에 질문하는 동선을 추가했습니다. 어시스턴트는 실행모드·AI 커스텀 일반/고급·국면 범위·다중 실행을 현재 설정과 함께 설명합니다.
- 증권 연결 성공 로그는 계좌번호 대신 설정 여부만 기록해 지원 로그·릴리스 진단에서 계좌 식별값이 노출되지 않게 했습니다.
- Fix Patch 1까지의 전체 자동 회귀는 `1,233 passed, 6 skipped`입니다. Update Patch 1의 최종 전체 회귀 결과는 `docs/TEST_STATUS.md`를 정본으로 확인합니다.
- Teayu_02 실계정 읽기 전용 점검에서는 암호화폐 6개 거래소가 인증·IP·계정 응답 게이트를 통과하지 못했고 DeepSeek·OpenAI 키 재입력이 필요합니다. 이는 소스 회귀 통과와 별도의 운영 차단 상태입니다.
- 기능별 근거와 남은 게이트는 `docs/INTEGRATED_AUDIT_v3.9.0.5_20260731.md`를 확인합니다.
- 현재 공개 자산은 v3.9.0.5 Fix Patch 1이며, Update Patch 1 manifest는 `pending_windows_rebuild`입니다. Update Patch 1 Windows 빌드·서명·설치·자동업데이트·복원·실연결·장시간 검증 전에는 이번 수정의 배포 완료로 간주하지 않습니다.

## 개발 이력: v3.9.0.3 Windows 후보 변경 (2026-07-28)

이 절은 과거 개발 이력이며 설치된 v3.9.0.5 사용자가 수행할 “배포 전” 절차가 아닙니다. 현재 사용법과 변경사항은 위의 v3.9.0.5 절을 기준으로 합니다.

- Teayu 테스터 데이터에서 숨은 AI 학습 탭의 무한 callback·137만 줄 성능 로그 누적, 캐시 스테이징 EXE가 설치 대상으로 남은 문제, Binance UTC 시간 혼용 예외를 확인하고 수정했습니다.
- 금융 인텔리전스의 작업 없는 50ms 큐 확인을 중단하고, 실제 작업 중이거나 탭이 다시 표시될 때만 확인합니다. 주요 반복 위젯은 완료된 예약 ID를 즉시 제거하고 종료 시 재귀적으로 정리합니다.
- 숨겨진 거래소·증권사 상세 탭은 잔고·포지션·통계를 계속 API 조회하지 않으며, 사용자가 탭을 다시 열면 즉시 갱신을 재개합니다.
- 화면·학습 대상과 실제 주문 대상을 분리하고, 명시적으로 빈 주문 목록은 주문 0개로 처리합니다. 활성 거래소를 주문 대상으로 복사하는 버튼은 설정 저장 전에는 적용되지 않습니다.
- 이 현상은 테스터의 거래소 API 발급 실수로 확인된 것이 아니라 NoahAI의 화면/학습 활성화와 실제 주문 허용 범위가 불명확했던 제품 설정·구버전 fallback 문제입니다.
- AI 커스텀 고급모드 1차로 사용자 지표 기간 2~500, 조건별 시간봉, AND/OR, 진입·전체 청산, 요청 정의·실제 계산값 비교를 추가했습니다.
- OpenAI·DeepSeek에 Anthropic Claude·Google Gemini를 추가하고, Kimi K3 시험 지원을 유지합니다. Claude Code 구독이 아니라 별도의 Anthropic API 키가 필요하며, 설정에서 제공사별 기준일 가격과 공식 가격 링크를 확인할 수 있습니다.
- 작업별 모델 배치를 모델명 문자열에서 `{provider, model}`로 확장했습니다. 빈번 호출은 DeepSeek, 표준 분석은 OpenAI, 정밀 진단은 Claude처럼 서로 다른 엔진을 동시에 배치할 수 있으며 기존 OpenAI 설정은 자동 변환됩니다.
- AI 커스텀 음성 전사는 분석 Provider와 분리된 OpenAI 전사 프로필을 사용합니다. `gpt-4o-mini-transcribe`, `gpt-4o-transcribe`, 다중 화자용 `gpt-4o-transcribe-diarize`를 구분합니다.
- 최신 공식 모델 기준으로 Claude Sonnet 5·Opus 5·Fable 5, Gemini 3.5 Flash-Lite·3.6 Flash, Kimi K3·K2.6을 반영하고 권장·계정 확인·미리보기·비권장·종료 상태를 표시합니다.
- 저장 시 capability·종료 상태를 검사하고 API 키가 있으면 계정 모델 목록을 실제 확인합니다. 설정의 `실제 API 기능 검증`은 텍스트·JSON·사용량·오류 정규화와 선택적 전사를 점검합니다.
- AI 엔진 사용 순서는 `설정 → AI 엔진/API → 제공사 → 키 발급·입력 → 모델 새로고침 → 프리셋·문답 정책 → 저장`으로 인앱 매뉴얼과 사용자 가이드에 통일했습니다.
- 업데이트는 열린 포지션·주문·제출중 상태를 기본 연기하며, 유지 선택은 모든 TP·SL, 청산 선택은 실제 0건 상태를 확인합니다. 종료/DB·로그 flush 실패, manifest SHA 누락, Windows 코드서명 실패 시 적용하지 않습니다.
- 단계·이전/새 버전·해시·대상 경로를 영속 기록하고 재시작 후 버전·DB·API·포지션 복구를 검사합니다. 실패 시 이전 EXE 복원을 예약하고, 성공해도 사용자가 거래 재개를 확인하기 전까지 신규 주문을 잠급니다.
- 부분익절·추적손절·재진입, TradingView webhook, 임의 사용자 지표 샌드박스는 후속입니다.
- 탭 callback·업데이트 P0·멀티 Provider 역할 라우팅·독립 전사·모델 수명주기 집중 회귀와 통합 게이트 기준 `1,128 passed`, 전체 자동 회귀 `1,135 passed, 6 skipped, 0 failed`를 통과했습니다.
- README·문서 색인·AI API 사용자/아키텍처·확장 계획·기술 백서·대시보드 인앱 매뉴얼의 구형 DeepSeek 3.1 및 배포 상태 표기를 정합화했습니다.
- 이번 v3.9.0.3 EXE는 unsigned 재배포본입니다.
- 위 변경은 모두 이미 배포된 v3.9.0.2 이후의 v3.9.0.3 Windows 업데이트 대상입니다. 서명 빌드·실연결·장시간 검증 후 배포합니다.

## v3.9.0.2 (2026-07-26, 2026-07-27 후속 포함) - AI 커스텀 실행정합·레퍼럴·거래 통계·안전 어시스턴트

- daltrading 관리자 KPI에 `1일·7일·30일·90일` 사용자별 동시 실행 거래소 분포를 한 표로 추가했습니다. 각 기간의 사용자별 최신 상태 한 건만 사용하며 0개·1개·2개·3개·4개·5개·6개·7개 이상과 2개 이상 합계를 함께 표시합니다.
- 클라이언트는 거래소 시작·중지뿐 아니라 1분 상태 확인 성공 때도 현재 실행 거래소 스냅샷을 보내 장기 실행·비정상 종료 뒤 기간별 분포가 오래된 상태로 남는 문제를 줄였습니다.
- KRW 거래량 카드에 실제 체결·금액 확인·실패 건수와 Upbit·Bithumb 학습/거래 이벤트 수를 함께 표시합니다. 학습·AI 판단은 거래량이 아니며 실제 체결 전에는 0원이 정상입니다.
- Upbit·Bithumb 시장가 체결 응답의 `price`가 비어 있어도 `average·filled·cost`로 실제 결제금액을 복원하고, 체결 응답에도 없을 때만 주문 시점 시세를 보조값으로 사용합니다.
- 2026-07-27 화면 정합 최종 패치로 블록체인·주식/증권의 `AI 리포트`, 주식/증권 `거래 통계`, 생활금융 관리의 `월간 재무 요약`을 공통 `content_bg → card → input → border` 색상 계층으로 통일했습니다. AI 리포트 안전 대체 화면도 같은 팔레트를 사용합니다.
- 모든 서비스의 공용 AI 어시스턴트 대화 화면에서 내부 `현재 어시스턴트 모델` 상시 표시를 제거했습니다. 모델 설정·실행·안전 검증은 유지되고, 대화 화면은 질문·답변과 복사·저장 도구에 집중합니다.
- 2026-07-27 KPI 수집·대시보드 정합 패치로 Binance와 다중 거래소의 안전 종료가 기존 포지션 종료 경로를 재사용하고, 포지션 생명주기 이벤트는 서버 오류 때 제한적으로 재시도한 뒤 앱 종료 전에 큐를 비웁니다.
- 거래 주문 이벤트에 결제통화(`KRW`/`USDT`)를 함께 보내며, 서버는 Upbit·Bithumb·KRW 심볼의 기존 이벤트도 KRW로 분류합니다. 모의·데모·페이퍼·테스트 체결은 계속 실제 거래량에서 제외합니다.
- 평균 보유시간 수집률은 새 포지션 수집을 시작한 뒤의 청산만 비교합니다. 진입은 있지만 아직 종료가 없으면 과거 청산 전체를 분모로 삼아 `0%`로 오해시키지 않고 `수집 관찰 중`으로 표시합니다.
- 클라이언트 `거래 통계`의 기존 4개 KPI와 표 영역은 유지하면서 실제 체결금액을 `USDT`·`KRW`로 분리하고, 검증 가능한 진입·청산 시각에서 계산한 평균 보유시간을 한 줄 운용 지표와 거래소별 요약에 추가했습니다.
- `AI 자동 리포트`의 오늘·주간·월간·실시간 요약도 같은 로컬 `trade_log` 기준으로 통화별 체결금액과 평균 보유시간을 표시합니다. 동일 시각 구형·모의 기록이나 진입·청산 시각 누락은 보유시간으로 추정하지 않고 `수집 대기`로 표시합니다.
- daltrading 운영 DB를 WAL 모드와 30초 잠금 대기로 전환해 고빈도 KPI 적재 중에도 관리자 거래량·보유시간 조회가 기본 5초 잠금 오류로 실패하지 않게 했습니다.
- 수백만 KPI 환경에서 대시보드가 전체 테이블을 반복 스캔·정렬하지 않도록 이벤트유형·사용자·시각 복합 인덱스, 단일 차원 집계와 인덱스 기반 최근 100건 조회를 적용했습니다.
- 이 절 작성 당시 NoahAI Client 3.9.0.2 Windows EXE가 이전 배포본이었고 v3.9.0.5는 `pending_windows_rebuild`였습니다. 현재 배포 경계는 문서 최상단의 Fix Patch 1 공개/Update Patch 1 재빌드 대기를 따릅니다.
- 2026-07-27 기능 탭 시각 정합 패치로 블록체인·주식/증권의 `시장 트렌드`, `AI 학습`, `AI 어시스턴트`를 `AI 커스텀`·`거래 통계`와 같은 배경·카드·입력창·테두리·버튼 체계로 통일했습니다. 거래소·증권사 운영 소스 탭의 보조 강조색은 의도적인 구분으로 유지합니다.
- 2026-07-27 IP 라이선스·레퍼럴 운영 기반으로 `레퍼럴` 무료회원 등급을 추가했습니다. 대시보드에는 `레퍼럴`로 표시되며 서버 관리자가 제휴 승인·코드·URL을 확인해 활성화한 Binance·Bybit·OKX·Bitget만 사용할 수 있습니다. 신규 설치에서는 네 곳 모두 비활성으로 시작합니다.
- 레퍼럴 회원은 Upbit·Bithumb과 국내 증권사 설정·실행이 강제로 제거되고, 거래소 시작 직전에도 서버 정책 기반 실행 게이트를 다시 확인합니다.
- `daltrading` 관리자 `다운로드 설정` 메뉴를 `설정`으로 바꾸고 기존 다운로드 항목을 유지한 채 거래소별 레퍼럴 코드·가입 URL·활성 여부를 서버에서 관리하도록 확장했습니다.
- 로그인과 1분 주기 상태 확인에 회원정책 버전을 포함하고, 정책 축소 시 신규 주문 루프만 정지합니다. 열린 포지션을 등급 변경만으로 임의 청산하지 않습니다.
- 관리자 KPI에 사용자별 최신 실행 스냅샷을 추가해 1개·2개·3개·4개·5개·6개 거래소를 동시에 실행하는 사용자 수를 중복 없이 집계합니다.
- 레퍼럴 코드는 클라이언트에 하드코딩하지 않습니다. 다만 주문이 사용자 PC에서 거래소로 직접 전달되는 현재 구조상 변조 클라이언트까지 절대 차단하려면 서버 주문 프록시 또는 원격 증명 계층이 추가로 필요합니다.
- 연결한 거래소 계정의 실제 레퍼럴 귀속은 아직 자동 증명하지 않습니다. Affiliate/Broker API 또는 관리자 UID 대사 전에는 가입 링크 사용과 실제 귀속을 같은 것으로 간주하지 않습니다.
- daltrading의 공개 기본 JWT 비밀문자열을 제거했습니다. 환경변수를 우선 사용하고 미설정 시 권한 600의 서버 로컬 랜덤 비밀파일을 생성하며, 회원 활성 전환·레퍼럴 설정은 토큰과 DB의 현재 관리자 권한을 함께 확인합니다.
- 2026-07-27 UI 후속 패치에서 거래 통계의 이중 스크롤을 제거해 코인별 행이 남은 본문 높이를 모두 사용하도록 수정했습니다.
- 공통 탭 색상·폰트·테두리 규칙을 금융 인텔리전스와 생활금융 내부 탭까지 확대하고, 거래소·증권사 탭은 일반 기능 탭과 구분되는 보조 강조색을 적용했습니다.
- 생활금융 목표 진행률 막대의 CustomTkinter 호환 오류를 수정해 초기 데이터 새로고침이 중간에 멈추지 않도록 했습니다.
- 금융 인텔리전스 각 화면에 초보자용 한 줄 사용 순서와 `AI에게 사용법 묻기`를 추가했습니다.
- `코인 정보`는 내 계좌·선택 코인의 운용 상태, `코인 탐색`은 여러 코인의 시장 조건 비교라는 차이를 화면·매뉴얼·AI 어시스턴트에 통일했습니다.
- AI API가 비활성화된 상태에서도 금융 인텔리전스 사용법과 데이터 연결/주문 경계를 로컬 제품 지식으로 안내합니다.
- 2026-07-27 기간별 실행 분포·KRW 체결금액 보강을 포함한 전체 회귀는 `1,079 passed, 6 skipped, 3 warnings`이며, daltrading 정책·KPI 회귀는 `20 passed`입니다. 문서 정합성과 macOS 실위젯 렌더링 검증도 통과했습니다.
- AI 커스텀 선언형 필드를 SMA/EMA 20·50·200, ADX, ATR, MACD 세부값, 볼린저 폭, 거래량과 UTC 시간/요일까지 확대했습니다.
- 지원하지 않는 AI/Pine 필드와 연산자는 조용히 무시하지 않고 승인 전에 차단합니다.
- Pine `crossover/crossunder`는 직전·현재 캔들을 함께 확인하는 실제 교차 조건으로 변환됩니다.
- 코인은 진입 당시 선택된 전략 버전의 명시적 선언형 청산 조건을 Binance/Unified 포지션 모니터까지 보존합니다.
- 자동검증은 겹치지 않는 단일 포지션, 진입·청산 양쪽 수수료, 양방향 슬리피지, 왕복 스프레드, 비용 전/후 PnL, Profit Factor, 기대값, MDD, 거래목록과 국면별 결과를 기록합니다.
- 주식/ETF도 진입 전략 버전의 선언형 청산·TP/SL을 XAI 기록에서 복원해 클라이언트 자동 청산에서 평가합니다. 증권사 서버 측 보험/브래킷 주문은 어댑터별 지원 범위입니다.
- AI 어시스턴트 설정 작업을 타입형 허용 레지스트리와 사용자 최종확인 권한으로 통일했습니다. 구형 AI 권장안도 같은 안전 검증을 우회할 수 없습니다.
- 설정 저장 후 파일 재조회가 다르면 자동 롤백하며, API 키·전체 설정을 기록하지 않는 감사로그로 재시작 후에도 마지막 설정 변경을 되돌릴 수 있습니다.
- 감사로그는 로그인 사용자별 앱 데이터에 분리되며, 되돌리기도 저장 후 재조회가 다르면 취소되어 다른 설정을 덮어쓰지 않습니다.
- AI 어시스턴트가 NoahAI 공식 정체성, 최근 대화 맥락, 제품 설정 경로를 유지하고 모호한 설정 변경은 추정 적용 대신 재질문합니다.
- `high vol 차단` 질문에는 현재 전략 엔진 값·정확한 메뉴 경로·남아 있는 가드레일을 설명하며, 명확한 변경 요청은 변경 전/후와 2단계 확인 후 `advanced_trading_layers.strategy_engine`에 저장합니다.
- 설정의 `고급 자동매매 → 전략 엔진 세부 설정`에서 고변동장 평가/차단, 합의 임계값, 심볼 쿨다운을 직접 조절할 수 있습니다.
- 전략 엔진은 국면 판단 시각·신뢰도·근거와 전환 후보를 기록하고, 정상 국면 전환은 연속 확인 후 적용하며 오래된 시세 입력은 거래 판단에서 차단합니다.
- AI 커스텀 XAI 결과에 출처 근거 확보 여부, 전략 역할, 적용 범위·시장상황·우선순위와 “승인·검증 전 미적용” 상태를 사람 말로 먼저 표시합니다.
- 소스가 상승·하락·횡보·고변동·저변동 시장을 명시하면 해당 시장상황을 추천 선택합니다. 제외 문맥은 추천에서 빼고, 근거가 없으면 임의 추정하지 않습니다.
- AI 커스텀 화면에서 `처음 사용법 AI에게 묻기`, `이 결과 AI에게 묻기`로 NoahAI 어시스턴트에 현재 전략 맥락을 전달할 수 있습니다.
- 최신 OpenAI·호환 API 모델명은 안전한 이름 형식을 검증해 사용할 수 있으며, 모델 변경은 분석 품질·비용 선택일 뿐 수익성 통과나 거래 기회를 보장하지 않습니다.
- 설정 변경은 `현재 상태 수집 → 의도/위험 판단 → 변경안 → 사용자 확인 → 저장 → 런타임 갱신` 범위에서만 수행합니다. 주문 시작·중지·임의 주문·API 키 입력은 대화로 자동 실행하지 않습니다.
- “매수해줘”, “자동매매 시작해줘”, “API 키 바꿔줘”, 출금·송금 요청은 일반 상담으로 흘려보내지 않고, 실행하지 않았음을 명시하며 실제 개방 전에 필요한 안전 검증을 안내합니다.
- 과거 설정의 `ai_auto` 값은 더 이상 거래 관련 변경의 확인 단계를 생략하지 않습니다. 모든 대화형 설정 저장은 적용 버튼과 최종 확인을 거칩니다.
- 시장·성과·포지션 데이터가 부족하거나 최근 손실·열린 포지션이 확인되면 레버리지 확대 등 위험을 높이는 변경을 적용 단계에서 차단합니다.
- 전체 자동 회귀 `1,053 passed`, AI 커스텀·어시스턴트 집중 회귀 `81 passed`, 문서 정합성·배포 `prekey` 게이트를 통과했습니다.
- 이 절 작성 당시 선행 Windows EXE는 고객 배포로 간주하지 않았습니다. 현재 Fix Patch 1은 공개 자산이고 Update Patch 1 manifest가 `pending_windows_rebuild`이며, 새 EXE의 설치·실행·로그인·AI 커스텀 전수 클릭과 Fix Patch 1 자동업데이트 E2E가 배포 게이트입니다.
- 실제 YouTube URL 입력과 실계좌 장시간 검증은 외부 배포 게이트로 남아 있습니다.

### 가장 쉬운 사용 순서

1. `AI 커스텀`의 `처음 사용법 AI에게 묻기`로 화면 순서를 확인합니다.
2. 전략 자료를 넣고 분석하면 소스에 명시된 시장상황 추천과 그 근거를 확인합니다.
3. XAI에서 원문 근거·규칙·엔진 적용값·누락 조건을 읽고, 모르면 `이 결과 AI에게 묻기`를 누릅니다.
4. 검토 버전 저장 → 사용자 승인 → 자동검증 → 최종 적용 → 대상 거래소/증권사 시작 순서를 지킵니다.
5. 어시스턴트의 설정 변경안은 현재 시장·계좌·로그 근거와 변경 전/후를 확인한 뒤 적용합니다.

## v3.9.0.1 (2026-07-24) - 금융 인텔리전스 통합 분석 허브

- 주문 체결과 실제 포지션 생명주기 KPI를 분리했습니다. 코인·주식·ETF의 진입, 부분 청산, 전량 청산을 고유 포지션으로 연결하고 검증된 UTC 시각 차이만 보유시간으로 전송합니다.
- 시간대가 다른 시각을 빼다가 예외가 나면 `0초`로 저장하던 경로를 제거했습니다. 실제 진입시각을 확인할 수 없는 복구 포지션은 임의의 보유시간을 만들지 않고 수집 보류로 남깁니다.
- 증권 자동매매의 매수·매도를 각각 즉시 종료된 거래로 저장하던 구조를 수정했습니다. 매수는 열린 포지션으로 유지하고 매도는 FIFO 진입 로트에 연결하며 부분 매도 후 남은 수량을 보존합니다.
- 서버는 중복 `event_id`를 한 번만 저장하고 종료 시각과 `hold_seconds`가 맞지 않는 이벤트를 거부합니다. 과거의 잘못된 구형 0초 이벤트는 평균에서 제외되며 새 종료 이벤트 전까지 `수집 대기`로 표시됩니다.
- 관리자 비즈니스 KPI는 `mock/demo/paper/test` 실행을 제외한 실제 실행만 집계하며, pytest 실행 중에는 클라이언트 KPI 네트워크 전송을 차단해 운영 지표 오염을 방지합니다.
- 하단 상태·업데이트·AI 실행 기록을 한 줄로 압축해 거래소·분석 탭의 본문이 잘리지 않도록 화면 공간을 복구했습니다.
- 운영체제 이모지 대신 앱이 직접 그리는 공용 아이콘을 적용해 macOS와 Windows에서 메뉴 모양·크기가 동일하게 보입니다.
- 서비스별 선택 색상, 비선택 탭 배경·테두리, 매뉴얼·설정 탭의 시각 계층을 보강했습니다.
- AI 커스텀에 `거래당 허용손실`, `최대 증거금 사용률`, `레버리지 상한`, `국면 이탈 시 동작`을 추가했습니다. 실제 레버리지는 고정 권장값이 아니라 위험예산과 전략 손절거리에서 계산됩니다.
- 진입·청산·손절·익절·포지션·시장상황이 빠지면 AI가 질문과 입력 예시를 보여주며, 임의로 채우거나 사용자 승인 없이 전략을 변경하지 않습니다.
- 커스텀 대상 국면이 아니면 사용자가 고른 대로 기본 NoahAI에 판단을 맡기거나 커스텀 신규 진입만 일시정지합니다.
- 실행검증 후 비용 반영 PnL·손익비·낙폭·수수료 근거를 사용한 다음 버전 개선안을 표시하지만 현재 버전에는 자동 적용하지 않습니다.
- AI 분석의 실제 변동성·거래량 전달, 신뢰도 키, TP/SL fraction 단위를 교정하고 비정상 TP/SL 설정 자동 복구를 추가했습니다.
- 자동 최적화가 낮은 승률을 반복 감지할 때 TP를 공유 설정에 1.1배씩 누적하던 결함을 제거했습니다. 조정안은 사용자 승인 전까지 설정이나 주문에 반영되지 않습니다.
- 코인 정보·종목 정보 탭 전체를 스크롤 가능하게 바꾸고, 종목 정보에 실제 `내 보유·최근 체결 종목`을 추가했습니다. 일반 종목 목록은 검색용 시장 미리보기로 분리했습니다.
- 앱 재시작 후에도 실제 종료 거래 이력을 유지해 장기 사용자를 신규 사용자로 오인하지 않으며, 고변동장은 일괄 차단 대신 합의 점수와 가드레일로 평가합니다.
- 블록체인·주식/증권에 `금융 인텔리전스`, 자산 통합에 `성과·위험 분석`, AI애널리스트에 `금융 인텔리전스 허브`를 추가했습니다.
- Yahoo Finance·Binance 공개 시세를 사용한 시장·섹터 분석과 이벤트·RSS 뉴스·근거 연결 내러티브 분석을 제공합니다.
- 기업 재무, SEC/DART 공시 입력, 상대가치·DCF·Reverse DCF·DDM·RIM, 종목·코인 스크리너와 멀티타임프레임 기술지표를 제공합니다.
- 수수료·슬리피지·Profit Factor·Expectancy·Sharpe·Sortino·MDD·회복기간과 자산·전략·브로커·레짐별 성과를 계산합니다.
- 신호와 체결 시점을 분리하고 비용·펀딩·최소주문·부분체결·API 실패·강제청산·OOS·Walk-forward를 반영한 범용 백테스트를 제공합니다.
- 대시보드 하단에서 이번 업데이트 요약을 확인하고 `업데이트·사용법`으로 인앱 `금융 인텔리전스` 안내를 바로 열 수 있습니다.
- 데이터 출처·기준시각·지연·품질과 계산 가정을 표시하며, 외부 데이터가 없으면 AI가 수치나 일정을 임의 생성하지 않습니다.
- 금융 인텔리전스 결과는 직접 주문 신호가 아니며 기존 주문 가드레일을 자동 변경하거나 우회하지 않습니다.
- 전체 회귀 `1030 passed, 6 skipped, 0 failed, 3 warnings`, 포지션 KPI·테스트 격리 집중 회귀 `8 passed`, TP/SL·정보 탭 집중 회귀 `7 passed`, AI 커스텀 위험기반 전략 집중 회귀 `56 passed`, 화면·공용 아이콘 집중 회귀 `29 passed`를 통과했습니다.
- Windows 신규 빌드·ProductVersion·manifest SHA 검증 결과는 배포 직전 최종 반영합니다.

### 가장 쉬운 사용 순서

1. 대시보드 하단 한 줄 안내의 `업데이트·사용법`을 누르고 `업데이트`에서 전체 변경을 확인합니다.
2. 사용자 전략은 `AI 커스텀`에서 설명/Pine/자료를 넣고 AI 질문에 답한 뒤 위험예산·증거금·레버리지 상한과 국면 이탈 동작을 선택합니다.
3. `검토 및 전략 버전 저장` → 사용자 승인 → 자동검증 → 최종 적용 순서로 진행합니다.
4. 금융 분석은 `금융 인텔리전스` 매뉴얼에서 위치를 확인하고 시장 프리셋이나 종목을 선택해 조회합니다. JSON이나 로컬 경로를 입력하지 않습니다.
5. 출처·기준시각·비용 반영 성과와 거래 로그를 함께 확인합니다. 관리자 평균 보유시간은 이 빌드 이후 실제 종료 거래부터 누적되며 과거 0초 값을 소급 추정하지 않습니다.
6. AI 개선안은 사용자가 새 버전으로 승인하기 전까지 적용되지 않습니다.

## v3.9.0.0 (2026-07-22) - AI 커스텀 실제 자동매매 운용 정식 연결

- AI 커스텀을 단순 프롬프트가 아닌 `전략 소스 → 실행 규칙 → XAI → 승인 → 자동검증 → 운용 → 복기` 전략 생성 계층으로 정식화했습니다.
- 설정에 `AI 커스텀 전략을 실제 자동매매 엔진에서 사용` 스위치를 추가했습니다. 신규·업데이트 사용자 모두 안전 기본값은 OFF이며, OFF이면 전략은 보존되지만 주문 판단에서 제외됩니다.
- 자동검증 통과 전략은 `최종 적용`으로 일반 운용하고, 자동검증 미통과 전략은 사용자가 명시적으로 선택할 때만 `1배·최대 1% 제한운용`할 수 있습니다.
- 제한운용도 독립 신호/기존 AI 확인·필터, 거래소·증권사 범위, 시장국면, 우선순위, 모든 기존 가드레일을 동일하게 통과합니다.
- 전략이 선택되거나 조건·수익성·리스크·주문 가드레일에서 보류될 때 실시간 로그에 전략명·운용 방식·차단 사유를 남깁니다.
- 거래소 카드에 전략명, 확인·필터/독립 신호, 일반/제한운용, 우선순위와 현재 국면을 표시합니다.
- 런타임·대시보드·인앱 매뉴얼·Windows 리소스·자동업데이트·빌드 문서의 버전을 `3.9.0.0`으로 통일했습니다.
- 이전 EXE를 새 버전으로 잘못 업로드하지 않도록 릴리스 자산 생성기에 stale EXE 차단 게이트를 추가했습니다.
- 복잡한 실행 용어를 초보자용 `기본 AI와 함께 사용 (권장)`과 고급 `내 전략이 진입 신호 생성`으로 정리하고 고급 진입 방향을 조건부로 표시합니다.
- `검토 및 전략 버전 저장` 실패 원인을 화면에 표시하며, 저장·승인·자동검증 통과/미통과를 서로 다른 상태로 안내합니다.
- 설정의 `AI 트레이딩 모델` 명칭을 `AI 애널리스트 모델`로 변경하고 어시스턴트 기본 모델·작업별 모델 배치와의 역할을 구분했습니다.
- 전체 회귀: `977 passed, 6 skipped, 0 failed`; v3.9.0.0 집중 게이트: `42 passed`.
- 2026-07-23 기준 Windows EXE, 실제 크기·SHA-256 manifest와 GitHub v3.9.0.0 릴리스 자산 게시를 확인했습니다. 코드서명·Windows 실행 및 이전 설치본 자동업데이트 E2E는 별도 운영 검증 항목입니다.

### 실제 사용 순서

1. 설정 → OpenAI API → 안전 기본값 OFF 확인 → 사용자가 이해한 경우 `AI 커스텀 전략을 실제 자동매매 엔진에서 사용` ON → 저장
2. AI 커스텀에서 텍스트/Pine/PDF/차트/영상/YouTube/TradingView 입력
3. 적용 범위·시장국면·우선순위·전략 역할 선택 → AI 분석
4. `검토 및 전략 버전 저장` → 내용 승인 → 자동 검증 실행
5. 통과 시 `최종 적용`, 미통과 시 위험을 이해한 경우에만 `1% 제한운용`
6. 해당 거래소/증권사 시작. 이후 일치하는 시장국면과 조건에서 전략이 자동 선택됨
7. 거래소 카드와 실시간 로그에서 선택·HOLD·가드레일 차단 이유 확인, 필요 시 `적용 해제`

## v3.8.9.29 (2026-07-19) - 다중 거래소·대시보드·AI 커스텀 안정화

### 2026-07-22 AI 커스텀 실행 연결 보강

- 확인·필터/독립 신호 모드, TP/SL 단위 정규화, 거래소 캔들 자동 검증, 전사 설정, 거래소별 활성 전략 상태를 추가했습니다.
- 대시보드 수기 실행검증은 제거했습니다. 과거 재생은 보조 검증이며 실전 수익을 보장하지 않습니다.
- 전체 회귀: `975 passed, 6 skipped, 0 failed`.

### 사용자 피드백 수정

- 모든 거래소 카드가 실제 엔진 상태와 동기화되어 `시작 → 중지`, `시작 중 → 진행 중`으로 전환됩니다.
- Binance 거래 사이클의 `time` 지역변수 충돌을 제거했습니다.
- OKX/Bybit/Bitget 분석에 요청 거래소 컨텍스트를 끝까지 전달하고, OKX 문자열 후보가 전처리에서 유실되던 경로를 수정했습니다.
- Bybit/Bitget의 토큰화 주식 선물이 암호화폐 후보에 섞이지 않도록 기본 제외 목록과 거래소별 설정 목록을 추가했습니다.
- 거래소 실시간 통계가 접히지 않도록 최소 높이를 보장하고, 실시간·상세 통계 모두 4개 KPI를 한 줄로 배치했습니다.
- 거래소·증권사 탭 통계를 88px 슬림 바로 줄이고 포지션 영역을 최소 230px로 확대했습니다. 잔고는 3열 카드, 포지션은 3개 동시 가시 카드와 초과 항목 내부 스크롤로 표시합니다.
- 상단 `종료` 버튼과 공통 안전 종료 경로를 추가해 거래 루프·DB·비동기 로그를 flush한 뒤 종료합니다.

### AI 커스텀 전략 모드

- 사용자 전략을 즉시 실행하지 않고 `소스 추출 → 조건 확인 → XAI 설명 → 사용자 승인 → 실행검증 → 최종 실거래 확인` 순서로 강제합니다.
- 계정별 프라이빗 저장소에 전략을 최대 10개 버전까지 저장하며, 실행검증 통과 버전만 롤백 대상으로 허용합니다.
- 최대손실·포지션·집중도·시장위험 가드레일을 전략보다 우선하고 출금/외부송금 규칙을 차단합니다.
- 대시보드에 `AI 커스텀` 탭을 추가해 안전 적용 흐름과 버전 상태를 표시합니다.
- `AI 커스텀` 탭에서 텍스트/Pine Script/PDF/차트 OCR/로컬 영상/YouTube 자막+화면/TradingView 공개 링크를 직접 분석합니다.
- AI가 출처 근거, 진입·청산·손절·익절·포지션·시장조건, 엔진 setting 값을 설명하고 검토용 전략 버전으로 저장합니다.
- 대시보드 실행 검증은 거래소 15분봉 자동 과거재생으로 제공하며, 수기 숫자 입력은 허용하지 않습니다.
- 개별 거래소/모든 블록체인/개별 증권사/모든 주식·ETF/모든 자산 범위와 시장국면·우선순위를 설정할 수 있습니다.
- 승인된 활성 전략을 최대 10개까지 두고 현재 자산·채널·시장국면·진입조건에 맞는 한 전략을 거래 직전에 선택합니다.
- 인앱 매뉴얼에 자료 수집부터 저장·승인·검증·최종 적용까지 실제 버튼 순서를 설명하는 전용 탭을 추가했습니다.
- AI 커스텀 상단에 실제 사용 모델과 설정 경로를 표시하고, 텍스트 입력 위치·PDF 100쪽/60,000자·영상 대표 9장 분석 범위를 명시했습니다.
- 차트 이미지와 영상 대표 장면은 OCR뿐 아니라 비전 AI로 지표·선·영역·매매 마커를 구조화하며 실패 시 OCR로 안전하게 폴백합니다.

### 설정 UX

- OpenAI API, 거래소 API, 거래소 선택, 일반, 고급 매매 계층, AlphaArena 탭 상단에 실제 저장 버튼을 추가했습니다.
- X 닫기 확인창을 Python 기본 메시지박스 대신 NoahAI 로고의 저장/폐기/계속 편집 창으로 교체했습니다.
- 대시보드는 화면 범위에서 최대 1500x980, 사용자 매뉴얼은 최대 1240x900으로 확장해 하단 통계 잘림과 9개 매뉴얼 탭 겹침을 해소했습니다.

### 신규 사용자·PnL·AI 모델

- 거래 데이터가 없을 때 영구 정지하지 않고 1포지션·1배 레버리지·위험배수 0.10의 제한 운용 학습으로 시작합니다.
- PnL 기반 신호 임계값을 거래소별로 분리해 OKX/Bybit/Bitget/Binance가 서로 덮어쓰지 않습니다.
- GPT-5.6 Sol/Terra/Luna 선택과 API 계정별 모델 새로고침, 역할별 비용 프리셋을 제공합니다.
- 설정 X 닫기는 자동 저장/앱 종료 대신 저장·저장 안 함·취소를 묻습니다.

### 배포/검증

- 고객 제공 6개 거래소 로그를 원인 분석에 사용했습니다.
- Windows 배포 실행 파일과 업데이트 manifest는 v3.9.0.0으로 게시됐습니다. 코드서명·Windows 실행 및 이전 설치본 자동업데이트 E2E는 별도 검증해야 합니다.
- 2026-07-19 당시 전체 회귀 테스트: `967 passed, 6 skipped, 0 failed` (최신 결과는 위 2026-07-22 항목 참조).

## v3.8.9.27 (2026-07-04) - 시작/정지 응답성 근본 패치

### 🔧 v3.8.9.28 운영 업데이트 (2026-07-10) - 공통 학습/로그 안정성 구조 개선

### 🔧 v3.8.9.28 운영 업데이트 (2026-07-12) - S등급 기능 완성 + fee 안전 패치

- S-3 AI 일일 리포트 추가(아침 브리프/저녁 복기 + 비용 영향)
- S-4 AI 대화형 투자비서 고도화(일반 질문에도 계좌 지표 기반 브리핑)
- 거래 통계/거래소 통계/AI 리포트에 수수료 지표(누적Fee/평균Fee/Fee-PnL) 노출 강화
- 사용자 DB 경로 자동 패치(exchange_trade_stats fee 컬럼 보강 + 1회 백필 + 합계 검증)
- 실시간 로그 하단 `❓로그도움말` 추가 및 매뉴얼 `📅 업데이트` 탭 바로 열기 동선 연결
- 시장 트렌드 상태에 `수집 중(경과초)`/`수집 지연` 표기를 추가해 대기 원인 가시화
- AI 어시스턴트 컨텍스트에 최근 로그 요약(최신 20줄)을 포함해 해석 정확도 보강
- AI 리포트 위젯 파괴 타이밍(Tkinter/CustomTkinter) 경합 방어 강화
   - `safe_after` 생존성 가드, destroy 시 예약 콜백 정리, 안전 텍스트 갱신 경로 적용
   - 탭 전환/창 종료 중 `TclError: invalid command name` 재발 경로 차단

#### 1️⃣ 공통 학습 API 딜레이 판단 구조 개편

- `trading/exchange_learning_manager.py`
   - 최근 1분 요청 판단을 전체 이력 스캔 방식에서 deque 롤링 윈도우 방식으로 변경
   - 거래소/코인 수 증가 시에도 딜레이 판단 경로의 시간복잡도를 상수 수준으로 유지

#### 2️⃣ timezone 혼용 오류 근본 제거

- `trading/exchange_learning_manager.py`
   - timestamp 로드/저장/조회 경로를 timezone-aware UTC 기준으로 통일
   - `offset-naive and offset-aware` 비교 오류 재발 경로 제거

#### 3️⃣ 로그 폭주 상황 비차단 처리

- `log_system/log_adapter.py`
   - 동일 메시지 단기 반복 로그 자동 억제 + 생략 건수 요약 라인 기록
- `main.py`, `log_system/log_adapter.py`
   - loguru 파일 sink를 `enqueue=True` 비동기 기록으로 변경
   - 고빈도 로그에서도 파일 I/O가 트레이딩/UI 흐름을 직접 블로킹하지 않도록 개선

#### 4️⃣ 검증

- `tests/test_exchange_learning_manager.py` 신규 추가
- `PYTHONPATH=. pytest -q tests/test_exchange_learning_manager.py tests/test_auto_update_manager.py` -> `7 passed`

#### 5️⃣ 코인 선택 안정성 보강 (목표 달성 후 fallback 차단)

- `trading/evaluator.py`
   - 초기 선정이 목표 수를 만족한 경우 즉시 반환하도록 수정
   - 목표 달성 후에도 기준 완화 재분석으로 진입하던 경로를 차단해 선택 결과 일관성 강화
- `tests/test_evaluator_selection_flow.py`
   - 목표 달성 시 fallback 분석이 추가 호출되지 않는 회귀 테스트 추가
   - 검증: `PYTHONPATH=. pytest -q tests/test_evaluator_selection_flow.py tests/test_exchange_learning_manager.py tests/test_auto_update_manager.py` -> `8 passed`

### 🔧 v3.8.9.27 운영 업데이트 (2026-07-06) - 자동업데이트 경로/진행률 가시성 강화

#### 1️⃣ 업데이트 진행률 실시간 표시

- `utils/auto_update_manager.py`, `ui/settings_modern.py`
   - 다운로드 시작/진행률(%) 또는 수신량(MB) 이벤트를 설정 업데이트 탭에 실시간 표시
   - 완료/실패 상태를 상태 라벨로 즉시 확인 가능

#### 2️⃣ 완료 시 경로 안내 강화

- `ui/settings_modern.py`
   - 다운로드 완료 팝업/상태 영역에 다음 경로를 명확히 노출
     - 다운로드 파일 경로
     - 적용 대상 EXE 경로
     - 업데이트 캐시 경로

#### 3️⃣ 캐시 경로 정책 및 누적 정리 개선

- `utils/auto_update_manager.py`
   - Windows 배포본에서 업데이트 캐시를 설치 위치 우선 경로로 선택
   - 쓰기 불가 시 `LOCALAPPDATA` -> `TEMP` 순으로 폴백
   - 오래된 auto_updater 버전 폴더/적용 스크립트 자동 정리(최신 2개 유지)

#### 🧪 검증

- `python -m pytest tests/test_auto_update_manager.py -q` -> `5 passed`
- 수정 파일 정적 오류 점검: `No errors found`

### 🎯 핵심 업데이트

#### 1️⃣ 거래소 시작/정지 UI 비동기화

- `ui/dashboard_modern.py`
   - 거래소별 토글 버튼 동작을 백그라운드 스레드로 전환
   - `Starting...`/`Stopping...` 중간 상태를 즉시 표시하고 처리 중 중복 클릭 차단

#### 2️⃣ Unified 계층 지연 초기화(lazy connect)

- `trading/unified_trading_manager.py`
   - 로그인 직후 전체 거래소 eager connect를 제거
   - `get_exchange()` 시점에 필요한 거래소만 생성/연결
- `trading/unified_trader.py`
   - 생성 시 전체 거래소 초기화/복구를 제거
   - `start_trading(exchange)` 시점에 대상 거래소만 초기화

#### 3️⃣ API 신호 수집 초기 동기 호출 제거

- `trading/api_signal_manager.py`
   - `start_signal_collection()`의 즉시 `_collect_signals()` 호출 제거
   - 초기 수집을 타이머 스레드로 이관해 초기 진입 블로킹 최소화

#### 🧪 검증 포인트

- 로그인 직후 대시보드 진입 시 거래소 연결 대기 때문에 UI가 장시간 멈추지 않아야 함
- 다중 거래소 개별 시작 시 버튼 상태/배지가 즉시 반영되고, 요청이 순차적으로 처리되어야 함

## v3.8.9.26 (2026-07-01) - 업데이트 경로 정합/적용 안정성 강화

### 🎯 핵심 업데이트

#### 1️⃣ 자동/수동 업데이트 경로 정합화 (Windows)

- `utils/auto_update_manager.py`
   - 업데이트 적용 대상을 "현재 실행 파일 단순 사용"에서 "정상 설치 경로 우선 + cache 경로 배제" 정책으로 변경
   - 정상 실행 경로를 `auto_update_target.json`에 저장하고, cache 실행 상태에서도 저장된 정상 경로를 우선 적용
   - 적용 대상이 `cache/auto_updater` 하위로 해석되면 업데이트 적용을 중단해 경로 오염 차단

#### 2️⃣ 업데이트 캐시 혼동/실패 완화

- `utils/auto_update_manager.py`
   - 캐시 스테이징 파일명을 `AITrading.exe` -> `AITrading.new.exe`로 분리
   - 종료 직후 파일 잠금 경합 대응을 위해 적용 스크립트에 복사 재시도(최대 60초) 추가
   - 설정 업데이트 탭에서 적용 대상 EXE/현재 실행 EXE/캐시 경로를 즉시 확인 가능하도록 진단 정보 노출

#### 3️⃣ 업데이트 회귀 방지 테스트 보강

- `tests/test_auto_update_manager.py`
   - cache 실행 시 저장된 정상 설치 경로를 우선 사용하는 테스트 추가
   - 스테이징 파일명이 `AITrading.new.exe`로 저장되는 테스트 추가
   - `PYTHONPATH=. pytest -q tests/test_auto_update_manager.py` 기준 `5 passed`

#### 4️⃣ 이전 거래소 설정 관련 버그 수정 내용 포함

- `ui/dashboard_modern.py`, `trading/unified_trader.py`, `trading/risk_manager.py`, `trading/trader.py`
   - Binance+BitGet 설정창 멈춤 완화(UI 비차단 진단 경로)
   - Unified 자산분류/거래소별 학습 이력 태깅(exchange) 정합화
   - 기존 설정/거래소 관련 재현 이슈를 포함해 v3.8.9.26 테스트 기준선으로 묶음 반영

## v3.8.9.25 (2026-07-01) - 운영 정합 패치

### 🎯 핵심 업데이트

#### 1️⃣ Unified 자산분류 정합화

- `trading/unified_trader.py`
   - Unified 포트폴리오 할당 경로에서 upbit/bithumb 자산군 분류를 `crypto`로 고정
   - 거래소별 KPI/리포트 해석 일관성 강화

#### 2️⃣ 거래소별 학습 필터 정합화

- `trading/risk_manager.py`
   - `update_coin_trade_history()`에 `exchange` 태그 저장 경로 추가(옵션)
- `trading/trader.py`
   - Binance 청산 이력 저장 시 `exchange='binance'` 명시
- `trading/unified_trader.py`
   - Unified 청산 경로에서도 RiskManager 이력 저장
   - 메모리 이력 부족 시 Recorder DB 이력으로 자동 보강

#### 3️⃣ 로그 이중 프리픽스 재발 방지

- `log_system/log_adapter.py`
   - 이미 포맷된 로그 메시지가 재유입될 때 선행 시간/레벨 프리픽스 제거 가드 추가
   - 신규 생성 로그에서 중복 포맷 재발 방지

#### 4️⃣ 계획/문서 동기화

- `docs/UPDATE_PLAN.md`: 2026-07-01 실행 정본(리스크/대응/검증 게이트/KPI·회원관리 정합 체크리스트) 반영
- `docs/CHANGELOG.md`, 인앱 `📅 업데이트` 동기화

## v3.8.9.22 (2026-06-11) - AI 학습 데이터 경량화 최적화 + 버전 동기화

### 🔧 v3.8.9.22 운영 업데이트 (2026-06-17) - 증권 연결 안내 문구 정합화 + 대량 사용자 전달 자동화

#### 0️⃣ 32bit Python 안내 문구 정합화

- 키움 연결 안내의 32bit 문구를 "절대조건"이 아닌 "현재 앱의 OpenAPI+(ActiveX) 경로 기준 권고"로 정정
- 사용자 혼선(왜 32bit를 써야 하는지 불명확) 완화

#### 0-1️⃣ 사용자 전달 파일 자동화 강화

- `지원요약/3분 점검본/사용자 안내문`에 자동 포함 항목 추가
   - 현재 Python 버전/비트수
   - Python 다운로드 링크
   - 키움 OpenAPI+ 설치 안내 링크
   - 진단 결과 기반 즉시 조치 순서
- 목적: 운영자가 수동으로 개별 안내하지 않아도 파일 그대로 전달 가능

#### 0-2️⃣ SaaS 증권사 검토 기준 문서 반영

- SaaS 확장 판단 시 4개 기준을 필수 체크로 명시
   1) 개인 사용 가능
   2) 서버 동작 가능
   3) 다수 고객 계좌 연결 가능
   4) 약관/제휴 정책 허용 여부

#### 🧪 확인

- 변경 파일 정적 오류 점검: 신규 오류 없음

### 🔧 v3.8.9.22 운영 업데이트 (2026-06-15) - 증권 연결 전달력/점검 실행력 보강

#### 0️⃣ 사용자 전달 흐름 보강 (기존 지원요약 강화)

- `증권 연결 점검` 팝업의 지원요약에 `개발자 전달 핵심요약` 섹션 추가
   - 환경 제약/인증/OCX 의심 경로를 상단에서 즉시 식별 가능
   - 교차 태깅 이력(예: 주식 컨텍스트 + `ex=binance`) 감지 문구 반영
- 기존 `지원요약 복사`/`파일로 저장` 흐름은 유지하고, 개발 전달용 문장 품질을 강화

#### 0-1️⃣ 사용자 3분 점검본 저장 추가

- 점검 팝업에 `3분 점검본 저장` 버튼 추가
- 생성 파일: `stock_broker_3min_checklist_YYYYMMDD_HHMMSS.txt`
- 목적: 사용자가 개발팀 전달 전에 3분 내 실행 가능한 체크리스트를 바로 저장/공유

#### 0-2️⃣ 설정 파일 선택 혼선 완화

- `scripts/stock_d1_preflight.py`가 계정별 설정(`data/*/config/settings.json`)을 우선 선택하도록 개선
- 기본 `data/nwsoft`를 먼저 읽어 오진하던 케이스를 줄이고, 실제 사용자 설정 기준 판정을 우선화

#### 🧪 확인

- `python3 -m py_compile ui/settings_modern.py` 통과
- `python3 scripts/stock_d1_preflight.py` 재실행 시 실제 계정 설정 파일 선택 확인

### 🔧 v3.8.9.22 핫픽스 (2026-06-13) - 다중 거래소 분리/설정 저장 멈춤 개선

#### 0️⃣ 사용자 체감 이슈 직접 수정

- 거래소 코인 분리 버그 수정
   - 업비트/바이비트 시작 시 바이낸스 코인이 함께 섞일 수 있던 경로 차단
   - 거래소별 코인 목록이 현재 선택 거래소 기준으로만 갱신되도록 보강
- 설정 저장 후 멈춤 완화
   - 저장 직후 자동으로 뜨는 증권 연결 진단을 비동기 처리로 전환
   - 설정 저장/탭 전환 시 UI 멈춤 체감이 줄어들도록 개선

#### 0-1️⃣ 사용자 확인 방법

- 거래소를 업비트/바이비트로 선택 후 코인 목록 새로고침
- 바이낸스 USDT 심볼이 섞이지 않고 거래소별 목록이 독립적으로 유지되는지 확인
- 설정 저장 직후 화면 조작(탭 전환/스크롤)이 즉시 반응하는지 확인

### 🔧 v3.8.9.22 패치 업데이트 (2026-06-12) - 증권사 연결 UX 완성도 보강

#### 0️⃣ 사용자 동선 보강 (설정/매뉴얼/점검)

- 설정 화면(`거래소 API`)에 `연결 실패 5분 점검 가이드 열기` 버튼 추가
- 버튼 클릭 시 즉시 문서를 열지 않고, 현재 입력값 기준 `증권사 1차 진단`을 먼저 안내
- 진단 확인 후 상세 점검 문서(`docs/STOCK_BROKER_WINDOWS_CONNECTION_CHECKLIST_20260611.md`) 열기 선택 가능

#### 0-1️⃣ 브로커별 진단 정밀도 개선

- 선택된 증권사만 대상으로 점검 수행
- 진단 출력에 `API 타입/버전`을 함께 표시해 운영/지원 커뮤니케이션 정확도 강화
- 경고 분기:
   - 키움: 비Windows + mock 아님
   - 신한: mock 아님 + app_key/app_secret(대응 입력값) 누락
   - 미래에셋: mock 아님 + app_key/app_secret(대응 입력값) 누락

#### 0-2️⃣ 저장 직후 자동 점검 옵션 추가

- 옵션명: `설정 저장 후 연결 위험이 보이면 자동으로 1차 진단 안내`
- 기본값 ON
- 저장/로드 연동 키: `ui_settings.auto_show_stock_broker_diagnosis_after_save`
- 설정 저장 성공 후, 위험 신호가 있는 경우에만 자동 팝업 노출

#### 0-3️⃣ 사용자 가이드 진입점 동기화

- docs 인덱스/사용자 가이드/실행 설명서에 동일 경로 반영
- 대시보드 사용자 매뉴얼에서 업데이트 확인 후 설정 화면 진입 시 문서와 동작이 일치하도록 문구 정렬

### 🎯 핵심 업데이트

#### 1️⃣ AI 학습 데이터 보관 상한 최적화 (10,000 이하)

- 거래소별 보관 상한을 운영 특성에 맞게 조정
   - 선물(고빈도): `binance/bybit/okx/bitget` = `10,000`
   - 현물(중저빈도): `upbit/bithumb` = `6,000`
   - 기타 거래소 기본값 = `8,000`
- 단일 JSON 파일 유지 + 최신 N개 슬라이딩 보관 방식으로 저장/로딩 부담 완화
- 저장 로그에 거래소별 보관 상한 출력 추가

#### 2️⃣ 버전 동기화

- `config/app_version.py` 기준 버전 상향: `3.8.9.21` → `3.8.9.22`
- 대시보드/인앱 사용자 매뉴얼 버전 표기는 단일 소스(`app_version`)로 자동 반영

#### 2-1️⃣ 증권사 설정 저장 보강 (신한/미래에셋)

- 설정 저장 시 `id/password` 입력값이 `app_key/app_secret`에도 동기화 저장되도록 보강
- 전용 app_key 입력 UI가 없는 현재 구조에서 인증 누락으로 인한 연결 실패 가능성 완화

#### 3️⃣ 기존 안정화 패치와의 결합 효과

- 2026-06-10에 적용한 JSON 원자 저장/안전 로딩/재진입 방지 패치와 결합 시,
  학습 데이터 파일 경합 및 대형 파일 파싱 지연 리스크를 추가 완화

### 🧪 검증 결과

- 문법 검증: `python3 -m py_compile trading/exchange_learning_manager.py config/app_version.py` 통과
- 정적 오류: 변경 코드 기준 신규 오류 없음

## v3.8.9.21 (2026-06-05) - 전역 전체시작 제거 + 거래소별 제어 단순화

### 🎯 핵심 업데이트

#### 1️⃣ 전역 시작/정지 버튼 제거

- 상단 전역 `전체 시작/정지` 버튼 제거
- 거래소 카드의 개별 시작/정지 버튼만 기본 제어 경로로 유지
- 설정 도움말/사용자 가이드/인앱 매뉴얼 문구 동기화

#### 2️⃣ 안정화 패치 적용 상태 유지

- 실시간 로그 버퍼 상한 + 구독 해제 처리로 장시간 메모리 누적 완화
- 거래소별 코인/시장상태 분리 유지로 다중 거래소 간 상태 오염 완화

#### 3️⃣ 후속 핫픽스 (운영 로그 기반)

- 시장 분석 로그 태깅을 거래소 루프 기준으로 강제(`exchange=exchange_name`)해 교차 표기 오탐 경로 축소
- 현물 어댑터 호환 경로 보강:
   - `get_markets()` 미구현 어댑터도 `get_exchange_info()` 기반으로 KRW 마켓 구성
   - 티커 조회 시 어댑터 메서드 부재 시 ccxt 객체 폴백 사용
- 다중거래소 안정성 점검 스크립트를 전역 제어 제거 정책 기준으로 갱신

## v3.8.9.20 (2026-06-05) - AI 애널리스트 중복 UI 제거 + 상단 화면 고도화

### 🎯 핵심 업데이트

#### 1️⃣ 중복 AI 분석 패널(드로어) 기능 철회

- Quick Actions의 `AI 분석 패널` 버튼 제거
- 우측 드로어(열기/닫기/리사이즈 재배치) 경로 전체 제거
- 사용자 체감이 낮고 기존 상단 `AI 애널리스트`와 중복되던 동선을 단일화

#### 2️⃣ 상단 AI 애널리스트 화면 고도화

- 핵심 스냅샷 카드 추가:
   - 종료 거래 수
   - 승률
   - 누적 손익
   - 리스크 가드 활성 상태
- 빠른 이동 액션 추가:
   - `AI 요약 리포트 탭 열기`
   - `시나리오 점검 탭 열기`
   - `AI 어시스턴트 열기`
- 화면 내 미리보기 추가:
   - AI 요약 리포트 미리보기
   - 시나리오 점검 미리보기
   - 미리보기 새로고침 버튼

#### 3️⃣ 기존 기능 유지 범위 명시

- 상단 `AI 애널리스트` 메뉴 진입 구조 유지
- `show_ai_analyst_content`, `_request_ai_analyst`, `시나리오 점검 탭` 흐름 유지
- 드로어 제거 외 기존 서비스 탭 정책/Quick Actions 동작에는 영향 없음

### 🧪 검증 결과

- 회귀 테스트: `pytest -q tests/test_dashboard_full_button_e2e.py tests/test_service_tab_policy_snapshot.py tests/test_menu_regression.py`
   - 결과: **99 passed**
- 문법 검증: `python3 -m py_compile ui/dashboard_modern.py` 통과
- 정적 오류: `ui/dashboard_modern.py` 신규 오류 없음

## v3.8.9.20 (2026-06-04) - AI 최적화 진입 정비 + 서비스 탭 정책 단순화

### 🎯 핵심 업데이트

#### 1️⃣ Quick Actions에 AI 최적화 진입 추가

- 대시보드 Quick Actions에 `AI 최적화` 버튼 추가
- 클릭 시 동작:
   - AI 어시스턴트 탭 자동 전환
   - 최적화 진단 질문 자동 전송
   - 설정관리 모달 자동 오픈

#### 2️⃣ 차트 분석 진입 경로 통합

- Quick Actions의 차트 분석 버튼 제거
- AI 어시스턴트 입력영역의 `📊 차트분석` 버튼으로 단일 진입 경로 통합

#### 3️⃣ 생활금융 오류 수정

- 생활금융 목표 관리 버튼의 `_show_goals` 누락 오류 수정
- 목표 탭 전환/갱신 경로 복구

#### 4️⃣ 서비스 탭 정책 정리

- 자산통합/생활금융/AI애널리스트 서비스의 중복 `사용가이드` 탭 제거
- 비거래 서비스에서 AI 어시스턴트 탭 자동 강제 유지 정책 제거
- 사용가이드는 인앱 사용자 매뉴얼로 일원화

### 🧪 검증 결과

- 회귀 테스트: `PYTHONPATH=. pytest -q tests/test_service_tab_policy_snapshot.py tests/test_dashboard_full_button_e2e.py tests/test_menu_regression.py`
   - 결과: **99 passed**
- 정적 오류: 주요 수정 파일 기준 신규 오류 없음

## v3.8.9.20 (2026-05-31) - 다중 거래소 코인/시작/학습 동기화 안정화

### 🎯 핵심 업데이트

#### 1️⃣ 거래소 간 코인 오염 방지 (공통 경로 수정)

- 거래소별 코인 저장 경로를 `selected_coins_by_exchange` 중심으로 고정
- 다중 거래소 동시 실행 시 한 거래소의 코인 선택이 다른 거래소로 섞이지 않도록 보강
- 코인 재선택 루프에서도 거래소별 재선택만 수행하도록 수정

#### 2️⃣ 시작/정지 상태 정합화 (실패 시 Running 표기 방지)

- 거래소 시작 콜백 반환값(`True/False`) 기반으로 UI 상태를 반영
- 시작 실패 거래소는 `Running`으로 올리지 않고 `Stopped` 유지
- 전체 실행/개별 토글 모두 동일한 판정 규칙 적용

#### 3️⃣ AI 학습 저장 안정성 보강

- 거래소별 `ExchangeLearningManager`를 캐시 재사용하도록 수정
- 신호마다 매니저 재생성으로 인한 로드/저장 경합 가능성을 완화

#### 4️⃣ KRW/USDT 혼재 환경 분류 정합화

- 메이저 코인 분류를 거래소 포맷 독립형(`base_symbol`/심볼 파싱)으로 개선
- KRW-USDT, BASE/QUOTE, BASE-QUOTE 형식 혼재 시 분류 일관성 향상

#### 5️⃣ 원샷 재발 점검 스크립트 추가

- 신규 스크립트: `scripts/multi_exchange_stability_check.py`
- 실계정 없이 한 번에 점검: 컴파일 검증, AI 실행/대시보드 회귀 테스트, 재발 방지 소스 불변조건(코인 분리, 시작 상태 bool 판정, 학습 매니저 캐시) 검사
- 결과 리포트: `data/reports/multi_exchange_stability_latest.json`

#### 6️⃣ AI 모델 역할별 비용 티어 배치 + 어시스턴트 조절 지원

- 신규 설정 키: `ai_model_roles`
  - `frequent_cheap`: 빈번 호출(신호분석/패턴/포지션 크기)
  - `standard`: 표준 분석(손익 분석/리포트)
  - `premium`: 정밀 진단(문제 진단/파라미터 최적화)
- 설정 UI(OpenAI API)에 역할별 모델 배치 콤보 추가
- AI 어시스턴트 설정 변경 JSON 파서에 `openai_model`, `assistant_ai_model`, `ai_model_roles` 허용
- 적용 정책은 기존과 동일하게 `user_confirm`/`ai_auto`를 따름

#### 7️⃣ 초기 AI 온보딩(5문항) 구현

- 시작 경로 추가:
  - 설정 → OpenAI API → `AI로 초기 설정하기 (5문항 가이드)`
  - AI 어시스턴트 → 설정관리 → `초기 온보딩 시작`
  - AI 어시스턴트 채팅 명령(`온보딩`, `초기 설정`)
- 진행 방식:
  - 목표/리스크/예산/적용방식/재검증 기준을 순차 대화형으로 수집
  - 마지막에 설정 요약 + 근거 + 재검증 시점을 표시하고 최종확인 후 저장
- 안전 정책:
  - 고위험 요청은 즉시 공격적 반영 대신 1차 완화 적용 후 재검증 기반 상향

### 🧪 검증 결과

- 문법 검증: `python3 -m py_compile trading/unified_trader.py ui/dashboard_modern.py trading/evaluator.py main.py` ✅
- 회귀 테스트: `tests/test_dashboard_full_button_e2e.py`, `tests/test_e2e_ai_execute_flow.py` ✅ **72 passed**
- 온보딩 테스트: `tests/test_ai_onboarding_flow_e2e.py` ✅ (완료/취소 2경로)
- 정적 오류: 수정 파일 기준 신규 오류 없음
- 원샷 점검 실행: `python3 scripts/multi_exchange_stability_check.py` ✅

## v3.8.9.20 (2026-05-29) - 실시간 청산 net PnL 기준 정합화 + 사용자 가시성 강화

### 🎯 핵심 업데이트

#### 1️⃣ 실시간 청산 판단 PnL 산식 정합화

- `trading/unified_trader.py`의 실시간 청산 판단용 `net_pnl_percent` 계산을
   recorder(`trading/recorder.py`)와 동일한 화폐단위 기준으로 정렬
- 적용 산식:
  - `gross_pnl_ccy = (현재가-진입가) * 수량 * side_sign`
  - `net_pnl_ccy = gross_pnl_ccy - fees_ccy - slippage_ccy`
  - `net_pnl_percent = net_pnl_ccy / notional * 100`
- 효과: 청산 판단 계층과 거래 기록 계층 간 PnL 기준 불일치 축소

#### 2️⃣ 운영 관찰성(Observability) 보강

- 실시간 계산 반환값에 `gross_pnl_ccy`, `net_pnl_ccy`, `estimated_fees`, `estimated_slippage`를 포함
- 대시보드/AI 어시스턴트/거래 통계 해석 시 기준 추적이 쉬워짐

#### 3️⃣ 문서 및 인앱 업데이트 동기화

- 인앱 사용자 매뉴얼 `📅 업데이트`에 2026-05-29 항목 추가
- `docs/TRADING_FLOW.md`, `USER_GUIDE_AI_EXECUTION.md`, `docs/USER_GUIDE.md` 반영
- 내부 감사/분석 문서(Noahailabs)에도 동일 판정 기준 반영

### 🔐 리스크 및 한계

- 본 패치는 "실시간 청산 판단 PnL 기준 정합화"에 집중된 패치입니다.
- RR 구조(TP<SL 기본값), 전역 포지션 사이징 상향, 심볼 집중 손실 가드레일은 별도 개선 과제로 유지됩니다.
- 권장: 7일/14일 롤링으로 승률·PnL·손실집중도를 함께 모니터링하고 단계적으로 조정

## v3.8.9.20 (2026-05-22) - AI 설정 적용 방식 선택권 추가 (사용자 최종확인/AI 자동적용)

> 이 절은 당시 동작 기록이다. v3.9.0.2부터 `ai_auto`는 폐기됐으며 과거 값도 `user_confirm`으로 강제 변환된다.

### 🎯 핵심 업데이트

#### 1️⃣ 사용자 선택형 적용 정책 추가

- **신규 설정 키**: `assistant_apply_mode`
  - `user_confirm` (기본값): AI 제안 후 사용자 최종확인 필수
  - `ai_auto`: AI 제안을 자동 저장/반영
- 설정 UI에 **"AI 설정 적용 방식"** 항목 추가

#### 2️⃣ AI 어시스턴트 적용 경로 연동

- AI 어시스턴트가 `assistant_apply_mode`를 인식하여 적용 흐름을 변경
- `user_confirm` 모드에서는 기존 2단계 확인 게이트 유지
- `ai_auto` 모드에서는 자동적용 가능(운영 정책 검토 권장)

#### 3️⃣ 안전 정규화/위험 표시 강화

- AI 제안 JSON 파서에서 `assistant_apply_mode` 허용 및 값 정규화
- 변경 전/후 diff 및 요약 문구에 적용 방식 표시
- `ai_auto` 변경 시 위험 분류에 "주의" 근거를 함께 표기

### 🔐 운영 권고

- 기본 운영은 **`user_confirm`** 권장 (법적/운영 리스크 최소화)
- `ai_auto` 사용 시에도 사용자는 정기적으로 변경 이력/성과를 점검해야 합니다

## v3.8.9.20 (2026-05-21) - FL/RL 사전 준비 구조 + 사용자 안내 동기화

### 🎯 핵심 업데이트

#### 1️⃣ 집단강화학습(연합학습) 사전 준비 구조 추가 (기본 OFF)

**기존 사용자 운영 안정성을 해치지 않도록, 학습 준비 구조를 "비활성 기본값"으로 선반영했습니다.**

- **RL 데이터 어댑터**: `trade_log`를 RL 전이(state/action/reward/next_state)로 변환
- **익명화 파이프라인**: 사용자 식별 정보 제거(user hash, symbol bucket, date-only)
- **FL 배치 매니저**: pending/sent 배치 파일 생성·보관·상태 전환
- **FL API 클라이언트**: 향후 서버 연동용 업로드/모델 조회 경로 사전 구현
- **로컬 모델 매니저**: 모델 버전/체크섬 무결성 관리

#### 2️⃣ 메인 초기화 경로 안전 연동

**대시보드/거래 루프를 변경하지 않고, 준비 구조만 초기화하도록 연동했습니다.**

- `main.py`에 FL 준비 서비스 초기화 추가
- 예외 발생 시 기존 로직 그대로 진행(운영 영향 차단)
- `federated_learning.enabled=false`일 때 기존 동작 100% 유지

#### 3️⃣ 사용자 안내 체계 동기화

**사용자가 업데이트 내용을 바로 이해할 수 있도록 문서/인앱 메뉴얼을 동일 기준으로 갱신했습니다.**

- 인앱 `📅 업데이트` 탭에 v3.8.9.20 항목 추가
- `USER_GUIDE_AI_EXECUTION.md` 버전 기준 상향 + FL 사전 준비 안내 추가
- `docs/USER_GUIDE.md` 업데이트 확인 예시 버전 상향 + 3.8.9.20 변경점 추가

### 🔐 안정성 원칙 (유지)

- ✅ **기본 비활성화**: FL/SaaS 준비 기능은 기본값 OFF
- ✅ **기존 경로 보호**: 현재 자동매매/설정/업데이트 흐름 무변경
- ✅ **점진 활성화**: 향후 ON 전환 시에도 단계별 검증 방식 적용

### 📊 검증 및 테스트

| 항목 | 결과 |
| --- | --- |
| **신규 단위 테스트** | ✅ 3 passed |
| **정적 오류 점검** | ✅ 신규/수정 파일 오류 없음 |
| **운영 영향도** | ✅ 기본값 OFF 상태에서 영향 없음 |

### 📝 문서 업데이트

| 문서 | 변경 사항 |
| --- | --- |
| **RELEASE_NOTES.md** | v3.8.9.20 릴리즈 항목 추가 |
| **USER_GUIDE_AI_EXECUTION.md** | 버전/날짜 갱신 + FL 사전 준비 안내 |
| **docs/USER_GUIDE.md** | 업데이트 기준 버전 상향 + 변경점 추가 |
| **docs/CHANGELOG.md** | 내부 기술 변경 이력 상세 기록 |
| **docs/TEST_STATUS.md** | v3.8.9.20 검증 결과 반영 |

---

## v3.8.9.19 (2026-05-07) - AI 실행 & 설정 연동 업데이트

### 🎯 핵심 업데이트

#### 1️⃣ 대시보드 "AI 실행" 버튼 (접근성 모드)

**기존 "모두시작" 버튼이 "AI 실행"으로 재정의되어, AI 기반 의사결정 중심으로 전환되었습니다.**

- **AI 준비도 진단**: 거래소 연결, API 키, 증권 브로커 설정 자동 스캔
- **실행 전 확인**: 위험도 레벨(🟢 일반/🟡 주의/🔴 고위험) 표시 후 명시적 확인 필요
- **실행 이력 기록**: 최근 실행 요약 + 과거 10개 실행 로그 조회 가능
- **안전성**: 자동 실행 금지, 항상 사용자 승인 필수

**사용 시나리오:**

```text
1. 대시보드 상단 "AI 실행" 버튼 클릭
   ↓
2. AI 준비도 진단 (거래소, API, 증권사 확인)
   ↓
3. 준비도 요약 + 위험경고 모달
   ↓
4. "확인" 또는 "취소" 선택
   ↓
5. 실행 결과 하단 요약 카드에 표시
```

#### 2️⃣ 설정 화면과 AI 진단 자동 연동

**설정을 열 때 AI 진단 결과가 자동으로 표시되어, 부족한 항목을 즉시 확인하고 수정할 수 있습니다.**

- **진단 결과 패널**: 설정 화면 상단에 진단 요약 + 부족 항목 목록 표시
- **"수정하기" 바로가기**: 문제 유형에 따라 관련 탭으로 자동 이동
- **설정 저장 후 진단 갱신**: 변경 사항 반영하여 새로운 진단 결과 자동 생성
- **양방향 동기화**: 설정 ↔ 대시보드 진단 결과 항상 최신 상태 유지

**수정 바로가기 자동 이동 규칙:**

| 부족 항목 | 이동 탭 |
| --- | --- |
| 거래소 API 키 미입력 | 거래소 API 설정 |
| 증권사 계정 미설정 | 증권사 설정 |
| 기타 설정 오류 | 설정 화면 |

#### 3️⃣ AI 설정 변경 2단계 확인 (기존 + 강화)

**AI 어시스턴트가 제안한 설정 변경은 반드시 2단계 확인을 거칩니다.**

1. **1차 확인**: 채팅창의 `적용` 버튼 (변경 내용 미리보기)
2. **2차 확인**: 최종 확인 대화상자 (변경 전/후 diff + 고위험 경고)

**고위험 경고 표시:**

- 🔴 레버리지 15배 이상
- 🔴 잔고 활용 한도 40% 이상 + 공격적 모드
- 🔴 실주문 플래그(enable_stock_live_order) ON
- 🔴 증권 자동매매 활성화

#### 4️⃣ 음성 입력(STT) 설정 변경 확인 게이트

**음성으로 인식된 설정 변경은 문자 확인 후 적용됩니다.**

```text
음성 입력 → 인식된 텍스트 확인 대화상자 → 
확인 → 설정 변경 의도 판정 → 2단계 확인 게이트
```

### 🔐 안전 원칙 (변함 없음)

- ✅ **사용자 주도**: AI는 제안만 하고, 최종 결정은 사용자
- ✅ **2단계 확인**: 모든 설정 변경에 명시적 2단계 확인 필수
- ✅ **변경 전/후 비교**: Diff를 함께 표시하여 의도하지 않은 변경 방지
- ✅ **고위험 경고**: 위험도 높은 설정 변경은 별도 경고 문구 추가
- ✅ **자동 실행 금지**: 어떤 기능도 사용자 확인 없이 자동 실행되지 않음

### 📊 검증 및 테스트

| 항목 | 결과 |
| --- | --- |
| **회귀 테스트** | ✅ 9개 모두 통과 |
| **문법 검증** | ✅ 오류 없음 (2개 파일) |
| **통합 테스트** | ✅ AI 어시스턴트 기능 무손상 |

### 📝 문서 업데이트

| 문서 | 변경 사항 |
| --- | --- |
| **AI_ASSISTANT_GUIDE.md** | 4.3절 추가: "대시보드 AI 실행 버튼과 설정 화면 연동" |
| **DEVELOPMENT_STATUS.md** | [2026-05-07] 3개 신규 항목 추가 |
| **USER_GUIDE_AI_EXECUTION.md** | 📌 신규 작성: AI 실행 기능 단계별 가이드 |

### 🚀 시작하기

#### 대시보드에서 "AI 실행" 버튼 사용하기

1. 대시보드 상단의 **"AI 실행"** 버튼을 클릭합니다
2. AI가 거래소·API·증권 설정을 자동 진단합니다
3. 부족한 항목이 있으면 **위험 레벨과 함께** 표시됩니다
4. **"설정 수정"** 또는 **"그대로 진행"**을 선택합니다
5. 실행 결과는 하단 요약 카드에 저장됩니다

#### 설정 화면에서 부족 항목 수정하기

1. 대시보드 설정 버튼 클릭
2. 설정 화면 상단의 **AI 진단 패널** 확인
3. **"지금 수정하기"** 버튼으로 해당 항목 탭으로 이동
4. 필요한 정보(API 키, 계정번호 등) 입력
5. **저장** 클릭 → 자동으로 AI 진단 갱신

#### 음성으로 설정 변경 제안 받기

1. 대시보드 AI 어시스턴트 탭 열기
2. **🎤 음성 입력** 버튼 클릭
3. 음성으로 말하기 (예: "레버리지 5배로 해줘")
4. 인식된 텍스트 확인 후 **확인** 클릭
5. 2단계 확인을 거쳐 설정 변경 완료

### ⚠️ 주의사항

1. **실주문 플래그 ON 상태에서의 작업**
   - "enable_stock_live_order"가 활성화된 상태에서는 모든 설정 변경이 **고위험**으로 표시됩니다
   - 주의 깊게 검토 후 진행하세요

2. **음성 입력 오인식 시**
   - 설정 변경으로 판정되면 반드시 **2단계 확인**을 통해 의도 확인 가능합니다
   - 취소 가능합니다

3. **설정 범위 자동 보정**
   - AI가 제안한 값이 안전 범위를 벗어나면 자동으로 보정됩니다
   - 보정 이유는 화면에 표시됩니다

### 🔄 이전 버전과의 호환성

- ✅ 기존 AI 어시스턴트 기능 100% 호환
- ✅ 기존 설정 파일 자동 인식
- ✅ 기존 거래 기록 유지
- ⚠️ 새로운 "AI 실행" 기능은 옵션 (기존 방식도 계속 사용 가능)

### 📞 피드백 및 지원

버그 보고, 기능 요청, 사용 질문은 다음을 참고하세요:

- 📖 [AI 어시스턴트 가이드](docs/AI_ASSISTANT_GUIDE.md)
- 📖 [AI 실행 기능 사용 설명서](USER_GUIDE_AI_EXECUTION.md)
- 📖 [트러블슈팅](docs/TROUBLESHOOTING.md)

---

## v3.8.9.18 (2026-05-03) - 증권사 어댑터 로버스트니스 강화

- 신한/미래에셋 REST 어댑터: 토큰 자동 갱신 + 재시도 로직 추가
- API type/version 조합 검증: 실행 전 조합 방어 체크
- 전체 테스트 **404 passed, 6 skipped**

---

## v3.8.9.17 (2026-05-01) - 증권 AI 자동매매 통합

- 시장 레짐 동적 감지 및 임계값 조정
- 피드백 학습 루프: 거래 성과 분석 반영
- 전략 엔진·수익성 검증기 활성화

---

## v3.8.9.16 (2026-04-27) - 생활금융 Phase F

- LifeFinanceQualityTracker 회귀 테스트
- FinanceProductAdvisor 외부 JSON 카탈로그 연동
- 전체 테스트 **336 passed, 6 skipped**

---

## 🎓 이전 버전 히스토리

더 자세한 내용은 [archive/history/DEVELOPMENT_STATUS_COMPREHENSIVE_20260427.md](docs/archive/history/DEVELOPMENT_STATUS_COMPREHENSIVE_20260427.md)를 참고하세요.

---

**최종 업데이트**: 2026년 5월 29일 (한국 시간)
