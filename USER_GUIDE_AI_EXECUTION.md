## 2026-09-24 - v3.9.1.47 Strategy Repair and Reconciliation Diagnostics Patch (미배포 소스 후보)

현재 소스 후보 v3.9.1.47 / updater 3.9.147 · 공개 v3.9.1.46. 전략 등록·기존 버전 보완 및 손익 대조 차단 안내 개선. 원문 실행값의 저장 단계 변형을 제거하고 미완성 초안 보관과 기존 버전의 원문 재분석/명시적 기본 AI 진입 전환을 제공합니다. 기존 성과는 새 버전에 승계하지 않습니다. 손익 확인 실패의 일반 409/워커 장애 안내를 구체화하고 복구 대상과 위험 검사 대상의 불일치를 보강하며 거래별 미확정 사유를 표시합니다. 미확정 손익 무시·자동 LIVE 재개는 제공하지 않습니다.

소스 검증: 전체 회귀 3,736 통과·9 제외·3 subtests 통과. 코인/증권 브라우저 신규 등록·명시적 기존 버전 전환·미완성 초안 보관 6경로 통과(실제 로컬 컴파일러/검증기, 저장 API fixture). 숫자 30.0/30 직렬화 차이의 오차단과 기존 고정 TP/SL 불러오기 충돌도 수정했습니다. Windows 설치본·실계좌·9월 24일 미대조 1건은 미검증입니다. 현재 data/Teayu-001이 없어 고객 사본 회귀 1개도 제외됐습니다. 설치기 빌드·게시를 하지 않았고, 미확정 손익의 조건부 LIVE 재개는 구현하지 않았습니다. 상세 범위는 TEST_STATUS와 최신 V39147 검증 원장을 따릅니다. 아래 이전 수치는 이력입니다.

## v3.9.1.46 관찰 시작·알림 안내 (미배포 후보, 공개 v3.9.1.45)

거래소 카드의 분석·학습 시작과 알림 수신 선택은 별개입니다. 기관별 LEARNING/PAPER/LIVE에 맞게 시작을 검사하며 관찰만 하려고 LIVE로 바꾸지 마세요. 시장국면 수신은 설정 → 알림·리포트에서 해당 기관과 PAPER/LIVE/학습·관찰을 선택해 저장합니다. 국면 유지·최초 관찰·확인 대기에는 변경 알림이 없을 수 있습니다. 모의시험 통과와 실제 Windows·Telegram 수신 검증은 별도입니다. [46 검증 상태](docs/V39146_RUNTIME_BOUNDARY_TEST_PLAN.md).

# AI 실행 기능 사용 설명서

v45 Web UI 전략 상담: AI 어시스턴트 → `전략 상담 · 외부 AI` 또는 전략 스튜디오 → `AI와 전략 상담`. 일반 전략 학습은 예산 없이, 맞춤 초안은 목적·상품·예산·손실 한도·기간·레버리지 조건을 확인하며 대화합니다. 전송 시 기존 AI Provider·비용 한도를 사용합니다. 검토 초안을 스튜디오에 전달하고 사용자 검토 후 재분석하며 기존 버전 저장·승인·과거재생/PAPER를 건너뛰지 않습니다. 공개 원칙과 추정·변형을 구분하고 실시간 웹 검색·최신 수익률 순위 확인·자동 거래는 하지 않습니다. [상세 사용법](docs/USER_GUIDE.md).

2026-09-22 v3.9.1.45 후보: RSI·MACD·추세는 실행 분석 시점의 실제 지표를 학습에 보존합니다. 과거 미기록 값은 현재 시세·추론 문장으로 생성하지 않습니다. 코인 선정의 기술점수 미산출과 실행 분석 지표를 구분하며, 종합점수·주문 정책은 별도입니다. 사용자 초기화나 반복 복구 없이 업데이트 후 새 분석 기록에서 확인합니다. 공개 v44 자산은 유지하고 v45 설치기는 별도 빌드합니다.

2026-09-22 최종 감사: 증권 국면의 중복 날짜·누락 종가·비정상 등락률을 정상 근거로 사용하지 않고, 알림 서버의 비정상 응답과 채널별 오류를 분리합니다. 전략·손실 한도를 완화하는 변경이 아니며 외부 메신저의 수신이나 수익 개선을 보장하지 않습니다.

2026-09-22 미배포 소스 변경: 설정 → 알림·리포트에서 시장국면의 PAPER/LIVE/학습·관찰 수신을 따로 선택합니다. 거래 실행이나 전략 조건은 바뀌지 않고 LIVE 손실·위험 경고도 이 필터의 영향을 받지 않습니다. 승인된 전략의 국면 적용은 시장 전체/종목/둘 다/사용 안 함 선언에 따릅니다. 기관 시장 알림을 개별 전략 활성화 알림이나 성과 보장으로 해석하지 않습니다. [수신 조건과 제한](docs/NOTIFICATION_INTEGRATIONS_GUIDE.md).

v44 거래 복구 보강: 설정 → 업데이트 → 유지관리에서 한 번 실행하면 앱 실행 중 설정 창을 닫아도 이어갑니다. Binance 분할 청산·수량 불일치는 전체 체결 근거가 증명될 때 수정합니다. 통계의 완료 거래 수와 진입·청산·분할 체결 수는 별개이며 상세에서 주문·체결 번호를 확인합니다. [사용자 절차](docs/USER_GUIDE.md), [검증 범위](reports/v39144-recovery-completion-verification.md)를 확인하세요.

## 이 설명서에 대해

v44 유지관리 보강: 평소 최적화 버튼을 반복 실행할 필요는 없습니다. 용량·미완료 경고가 지속될 때 실행하세요. 닫힌 진단 로그는 검증된 압축 보관본으로 옮기며, 현재 쓰는 로그·거래 원장·기존 학습 원본은 보존합니다. 새 기관/모드 충돌은 원문과 사유를 별도 보존하고 학습에서 제외해 표시합니다. 최적화가 모호한 과거 거래를 추측하거나 PnL 보호를 해제하지 않습니다. 타임아웃의 실제 주문 여부도 주문 근거 없이 ‘없음’으로 단정하지 않습니다.

**v3.9.1.44 현행 추가 안내 (2026-09-21):** 학습·XAI 저장소와 공통 이벤트 기록 방식을 개선했습니다. 코인 7개 기관과 증권 4개 기관·주식·ETF는 내부 연결 방식과 자산 규칙은 유지하면서 `LEARNING/PAPER/LIVE → 판단 → 차단/주문 → 체결/청산 → 원장 → XAI` 외곽 계약을 공유합니다. 증권 화면에서도 종목 유형·판단 사유·실제 주문 여부를 확인합니다. `설정 → 업데이트 → 유지관리 · 저장소`에서 용량·압축·DEBUG 만료와 최적화 진행을 확인합니다. 거래 원장은 삭제하지 않고, 모드·전략·손익 판단 기준 및 실행 권한은 변경하지 않습니다. 근거 없는 과거 값은 추정하지 않습니다. 상세 사용법은 [사용자 가이드](docs/USER_GUIDE.md), 시험·미완료 Windows 게이트는 [검증 보고](reports/v39144-storage-verification.md)를 확인하세요. 아래 v39 공개 상태·배포 보류 문장은 당시 기록이며 현재 배포 상태를 뜻하지 않습니다.

**v39 배포 보류:** [PnL 재감사](docs/V39139_PNL_TRUST_AUDIT.md)로 추가 결함을 보강 중입니다. 미대조 PnL은 수익성 검증의 정상 성과로 인정하지 않으며, 시간/수량 후보만의 자동 복구는 철회했습니다. 모든 학습 경로와 실계정 대조가 완료된 상태는 아닙니다.

이 문서는 **NoahAI v3.9.1.39 소스 후보(2026-09-18)** 기준 `"AI 실행"` 기능의 단계별 사용 방법을 설명합니다. 현재 공개 stable/latest는 **v3.9.1.38**이며, 공개 v38 이후 변경은 v39 새 Windows 산출물로만 배포합니다. 업데이트/재시작·실제 Provider·기관별 동작은 환경별 검증으로 판단합니다.

v3.9.1.38에서는 PAPER 카드와 거래 통계가 같은 원장·기관·통화 범위를 사용합니다. 카드는 전체, KPI는 오늘 등의 표시 범위를 확인하고 비교하세요. 과거 재생은 저장한 전략 시간봉을 사용하며, 없는 시간봉을 15분/일봉으로 대체하지 않습니다. 관리형 예제 외 전략은 원문의 시간봉을 확인해 저장합니다. 실제 사용 기관·시간봉·UTC 기간·표본을 검증 근거에서 볼 수 있습니다.

`검증 대상: NoahAI 기본 진입 + 사용자 위험·청산값`은 원문 전체의 진입 전략을 실행했다는 의미가 아닙니다. 외부 원문은 보존되고 지원 가능한 규칙만 구조화되며, 패키지 가져오기 후에도 검토·승인·PAPER가 필요합니다. [사용자 가이드](docs/USER_GUIDE.md)의 v3.9.1.39 안내와 [환경별 검증 계획](docs/V39139_EXTERNAL_CLOSE_RECONCILIATION_TEST_PLAN.md)을 참고하세요.

Web UI 제품은 로그인, 전체 설정·write-only 자격증명, 실시간 계좌 snapshot, 다중 시장 차트, 프라이빗 전략 원본·버전·승인·검증·PAPER 귀속, 생활금융·자산·로그, 로컬 AI 가이드와 사용자 명시 외부 AI 심층분석을 통합했습니다. 거래 엔진은 인증 후 명시적 시작 확인에서만 지연 연결되며 준비 실패는 실패 폐쇄됩니다. 공개 설치본이 존재하더라도 업데이트·롤백·실브로커·PAPER soak의 모든 환경이 검증됐다는 뜻은 아닙니다.

AI 커스텀은 전략 구조화·버전·승인·검증을 담당하지만 실행 권한이나 기존 주문 가드레일을 자동 우회하지 않습니다. AI 어시스턴트는 현재값·영향·설정 위치·운영 근거를 설명하는 읽기 전용 보조이며, 설정 저장·복구·거래 시작·정지를 대신 실행하지 않습니다.

**대상 사용자:**
- 초보 트레이더: 자동매매 전에 AI로 준비도를 확인하고 싶은 분
- 바쁜 사용자: 한 번의 버튼 클릭으로 거래 준비 상황을 파악하고 싶은 분
- 안전 중심 사용자: 자동 실행 없이 항상 확인 후 진행하고 싶은 분
- 장애인/디지털취약층: 간단한 조작으로 AI 상담을 받고 싶은 분

업데이트 신뢰 원칙:
- 사용자에게 보이는 기능 변경은 인앱 매뉴얼과 가이드가 함께 갱신된 상태로만 배포됩니다.

### v3.9.1.39 외부 TP/SL 청산 체결·손익 복구

- LIVE 통계의 `대조 전`은 손실된 표시가 아니라 거래소 확정 체결과 아직 연결되지 않은 상태입니다.
- 시간·수량 후보만으로는 거래 소유권을 증명할 수 없습니다. 실제 청산 주문 ID가 있는 행만 대조하며, ID가 없는 기존 v38 행의 자동 복구는 아직 미완료입니다.
- 거래소 수집 체결 총손익과 NoahAI 연결 청산 순손익을 구분하고 미확정이 있으면 부분 합계임을 알립니다. 같은 기간·거래·비용·원가 기준으로 비교해야 합니다.

### v3.9.1.38 전략 과거재생·기관별 거래내역

v39 소스 후보에는 전략 초보자 설명을 보강했습니다. `이 전략 쉽게 이해하기` → `이 결과 AI에게 묻기`에서 규칙·읽은 범위·일부 발췌를 확인합니다. 일반 안내는 정형 로컬 설명이며, AI와 되묻는 대화는 기본 보호 경로의 심층분석을 직접 요청합니다. AI 답변은 검토 후 보완 근거로 확정·재분석해야 하고 저장·승인·실행을 대신하지 않습니다. [초보자·자료 분석·차트 상세 안내](docs/STRATEGY_STUDIO_BEGINNER_GUIDE_V39139.md).

- Strategy Studio 과거재생 결과에서 검증 당시 캔들, 진입·청산 타점, 청산 기준 누적 수익률과 거래표를 함께 확인합니다. 이 결과는 로컬 과거 검증 근거이며 PAPER·LIVE 체결이나 미래 수익 보장이 아닙니다.
- 코인과 주식·ETF는 같은 표시 구조를 사용하지만 기관·통화·수수료·세금·시간봉 계약은 서로 구분합니다. 국내 현물 신규 SHORT는 계속 차단됩니다.
- 기관 실행 모드가 PAPER이면 `가상 포지션`·`가상 거래 통계`와 현재 PAPER 거래내역을, LIVE이면 상품에 맞는 `포지션/보유자산`·`실거래 통계`와 해당 계정·기관의 LIVE 종료 기록을 표시합니다.
- PAPER/LIVE 거래내역은 모두 기본 접힘입니다. `LIVE 거래내역` 또는 `PAPER 검증 이력` 탭을 조회해도 실제 실행 모드·전략 승인·거래 권한은 바뀌지 않습니다.
- LIVE 손익은 확정·미확정·외부·가져오기·통화 불명·조회 실패를 구분합니다. 조회 실패나 근거 없는 과거 기록을 0원·0건 또는 확정 성과로 바꾸지 않습니다.
- 소스 회귀와 격리 브라우저 검증은 통과했지만 Windows v38 설치기, v37→v38 업데이트, 키움 OCX·기관 실계정 및 장시간 PAPER 검증 전에는 배포 완료로 보지 않습니다.

### v3.9.1.29 전략과 실제 계정 운용 용량

- Strategy Studio의 전략 제작·로컬 분석·PAPER 검증·내보내기·공유 준비는 무료·유료가 같은 조건입니다. 회원등급은 Level, 전략 품질, 검증 점수나 증거등급을 높이지 않습니다.
- 집중운용은 모두 1개입니다. 실제 계정의 관리형 다중포지션은 국내 무료와 레퍼럴 확인 해외 무료가 거래소별 최대 3개, `pro_coin`·`premium`은 최대 5개입니다.
- 3개·5개는 채워야 할 목표가 아니라 계정 상한입니다. 전략 요청·계좌 위험·시장·성과회복·하드 가드레일이 더 작은 값으로 줄이거나 진입하지 않을 수 있습니다.
- Upbit·Bithumb·Coinone 국내 현물은 레퍼럴 UID 없이 무료로 사용할 수 있습니다. 해외 무료 실행은 서버가 레퍼럴 귀속을 확인한 Binance·Bybit·OKX·Bitget 범위입니다.
- Coinone은 KRW 현물 시세·PAPER·통계·Strategy Studio 준비 상태입니다. SHORT와 레버리지는 없고, 실계좌 시장가·부분체결·수수료·재시작 대조 E2E 전에는 LIVE 시작과 주문이 차단됩니다.
- Upbit·Bithumb·Coinone의 공개 코인 선택·분석과 PAPER는 공개 시세와 기기 안의 가상자금·가상포지션·가상 청산 원장만 사용하므로 거래소 API 키 없이 시험할 수 있습니다. API 키는 실제 잔고·주문·체결 동기화에만 필요하며 PAPER 성과를 LIVE 체결로 표시하지 않습니다.

### v3.9.1.27 Strategy Assistant 연속성·안내 UX

- 승인·자동검증·PAPER 재개·삭제는 같은 버튼을 두 번 누르지 않고 작업명과 영향을 보여 주는 확인/취소 창에서 승인합니다.
- 전략 버전에는 저장된 생성 시각을 표시하고, 시각이 없는 구버전은 추정하지 않습니다.
- `interactive_ai_budget_exceeded` 상태 429는 거래나 회원 제한이 아니라 NoahAI가 Provider 호출 전에 적용한 외부 AI 비용·반복 호출 상한입니다. 기본 일 30회·월 500회이며 `설정 → AI 엔진/API → AI 비용 관리`에서 현재 사용량을 보고 일 1~1000회·월 1~30000회로 변경할 수 있습니다.
- Provider가 직접 반환한 429는 제공사 측 요청 속도·동시 요청·쿼터·결제 제한입니다. NoahAI 로컬 한도와 원인·해결 방법이 다르며, 일일 로컬 집계는 UTC 00:00(한국시간 09:00)에 갱신됩니다.
- 같은 카드의 오늘·이번 달 비용은 사용자가 직접 요청한 AI 호출 중 실제 토큰과 공개 단가를 모두 확인한 건만 계산한 참고값입니다. 자동매매 백그라운드 AI, 같은 키를 쓰는 다른 앱, Provider 전체 청구서는 포함하지 않으며 미확정 비용은 0원으로 표시하지 않습니다.
- `나만의 AI 구성 · 작업별 모델`에서 현재 빈번·저비용/표준/정밀·전략/애널리스트/어시스턴트/전사 경로를 확인한 뒤 아래 역할별 설정에서 바꿉니다. 변경은 저장해야 적용되며 실제 계정 모델 목록은 연결 점검으로 확인합니다.
- DeepSeek의 정식 최신 Flash 선택값은 `deepseek-v4-flash`입니다. 이미지 분석은 별도 `deepseek-v4-flash-vision-exp`를 사용하며 일반 Flash를 차트 분석용으로 선택하면 호출 전에 차단합니다.
- 외부 요청은 Provider 전송 시도 전에 1회를 예약하므로 전송 뒤 실패한 요청도 반복 폭주 방지를 위해 집계합니다. 유효한 동일 질문 캐시 응답은 새 외부 호출로 계산하지 않습니다.
- 한도에 도달해도 텍스트·Pine의 결정형 로컬 규칙 분석과 일반 안내는 계속됩니다. 이미지·영상처럼 외부 AI가 필수인 입력은 한도 갱신 또는 설정 변경이 필요합니다.
- AI 답변은 `전략 스튜디오 검토 영역으로 보내기`로 전달할 수 있지만 아직 적용되지 않은 편집 초안입니다. 사용자가 내용을 확인하고 확정한 뒤에만 원본과 별도 근거로 재분석합니다.
- 답변 전달·재분석은 저장·승인·PAPER·LIVE를 자동 실행하지 않습니다. 암호화폐와 주식·ETF의 공통 제작 경로에 적용하며 레버리지 가드레일은 그대로입니다.
- 초보자·일반·고급은 실제 답변 지시문과 캐시를 분리하고, Strategy Studio에서 열면 초보자를 기본으로 사용합니다.
- 설정 화면의 9개 탭은 각 `AI에게 묻기`에 현재 탭 문맥을 함께 전달합니다. 전체 AI 어시스턴트로 이동하면 `설정 문맥 고정` 표시를 확인할 수 있고, 다른 탭으로 바꿀 때 이전 질문·응답이 새 탭에 섞이지 않습니다.
- 모델 비교는 OpenAI·DeepSeek·Kimi·Anthropic·Gemini별 상태·기능·용도·강점·제약·기준일 공개 단가를 표시합니다. 정적 호환 목록은 실제 계정 권한이 아니므로 `저장된 키로 실제 연결 점검` 결과를 우선합니다.
- OpenAI 데이터 공유 무료 토큰은 계정별 외부 프로그램입니다. NoahAI가 켜거나 무료량을 보장하지 않습니다. v3.9.1.27은 기본 보호 Project와 공개 일반 질문용 Project 키를 분리하지만 공개 경로는 기본 OFF입니다. AI 어시스턴트 심층분석에서 사용자가 공개 질문 1건을 별도 확인한 때만 최근 대화와 앱 상태를 제외하고 공유용 키를 사용하며, 전략·Pine·문서·차트·포지션·계좌·설정 문맥은 기본 보호 경로를 사용합니다.

### v3.9.1.26 Strategy Studio 위험 입력 안내

- `거래당 계좌 손실 0.5%, 증거금 사용은 최대 10%`를 입력하면 명시적 위험률과 최대 증거금 비중으로 구조화합니다. 주식·ETF는 `종목당 투자 비중은 최대 10%`를 사용합니다.
- 누락 카드의 `현재 선택값을 원문에 추가`는 사용자가 화면에서 고른 값만 복사합니다. 추가 후 다시 분석해야 합니다.
- `%` 없는 숫자는 단위를 추측하지 않습니다.
- 진입·청산·방향·지표·임계값은 자동으로 채우지 않습니다. 이 판단까지 NoahAI에 맡기는 경우 기본 NoahAI 또는 confirm 경로를 선택하며, 사용자 독립 전략으로 표시하지 않습니다.

#### 전략 만들기 방식과 질문 답변

1. `원문 그대로 구조화`는 기본값입니다. 원문에서 확인할 수 없는 조건은 누락으로 남고 승인·PAPER·LIVE가 차단됩니다.
2. `질문으로 함께 완성`을 선택하면 분석 뒤 빠진 진입·청산·TP/SL·위험·시장 조건을 한 번에 하나씩 묻습니다. 예시는 입력 형식을 보여줄 뿐 자동 적용되지 않습니다.
3. 사용자는 질문 카드의 `내 전략의 답변`에 자신의 기준을 직접 쓰고 `답변 확정 후 다시 분석`을 누릅니다. 답변은 원본과 별도 해시로 보존되므로 파일·Pine·PDF 원본은 바뀌지 않습니다.
4. 지원하지 않는 Pine 함수·미래 데이터·해석 불가능한 상태 조건은 답변만으로 우회하지 않습니다. 원본을 지원 규칙으로 명확히 다시 작성해야 합니다.
5. `기본 NoahAI에 맡기기`는 커스텀 전략 생성이 아닙니다. NoahAI 기본 후보·시장판단·위험관리를 사용하며 별도의 사용자 독립 전략 성과로 표시하지 않습니다.

세 방식은 현재 Level을 바꾸지 않습니다. Level 1~4는 설명·편집·운용 깊이이고, 실행 준비 여부는 필수 조건 완성 여부, 성과 근거는 E0~E5로 각각 구분합니다. AI에게 질문을 상의하러 이동해도 Strategy Studio 입력과 답변은 유지되며, AI 답변이 전략 원문이나 실행 규칙을 직접 수정하지 않습니다.

### v3.9.1.23 LIVE/PAPER 통계·운영 KPI 안내

- LIVE 청산 수·승률·실현손익·비용은 `trade_log`, PAPER 결과는 독립 가상 청산 원장을 기준으로 하며 서로 섞지 않습니다. 거래소가 확인한 실체결 건수는 또 다른 실행 원장이므로 NoahAI 청산 건수와 같다고 설명하지 않습니다.
- 실행 기관이 모두 PAPER이면 메인 운영 KPI는 자동으로 `PAPER`가 되어 현재 가상 포지션과 오늘 가상 청산을 보여 줍니다. LIVE 또는 혼합 운용에서는 LIVE 계좌/청산을 유지합니다. 기간 비교가 필요할 때만 거래 통계 탭에서 `LIVE / PAPER`와 `오늘 / 7일 / 30일 / 전체 / 사용자 지정`을 선택합니다.
- `통계 표시 기준 새로 시작`은 삭제가 아닙니다. 사용자가 경고를 확인하고 누른 정확한 시각 이후 청산부터 표시하며, 학습·Strategy Studio PAPER·가드레일·성과회복·열린 포지션·기존 원장은 유지합니다.
- KRW 현물·주식과 USDT 선물 손익은 환율 정책 없이 한 숫자로 합산하지 않습니다. 출처가 불명확한 구형 기록은 확정 거래소로 추정하지 않고 별도 표시합니다.
- AI 어시스턴트는 현재 기간과 기준시각을 설명할 수 있지만 통계 기준을 대신 변경하거나 원장을 삭제하지 않습니다.

### v3.9.1.23 현재 AI 실행·PAPER·전략 검증 계약

- 집중 운용은 거래소 실행 어댑터별 1개, 다중 운용은 기본 3개이며 LIVE와 PAPER가 같은 한도 함수를 사용합니다. 여러 거래소의 전체 합계 상한이 아닙니다.
- PAPER 화면은 실계정 잔고·포지션이 아닌 런타임 가상 포지션을 표시하고, 청산된 가상 거래는 `활성 수와 무관`한 이력으로 구분합니다. PAPER 통계는 실거래 DB가 아닌 가상 청산 원장을 사용합니다.
- AI 커스텀 버전이 `적용 중`이고 전역 모드가 PAPER이면 가상 실행 풀에 자동 포함됩니다. 같은 버전에 `PAPER 전진검증 시작`을 다시 누를 필요가 없으며, 그 버튼은 아직 최종 적용하지 않은 후보용입니다.
- `PAPER 검증 중` 후보는 최종 적용 전략이 아닙니다. 후보 조건이 현재 종목·국면과 맞지 않아도 기본 NoahAI 분석·진입 후보를 HOLD로 바꾸지 않습니다. 최종 적용 전략만 사용자가 선택한 조건에 따라 신규 진입을 차단할 수 있습니다.
- 독립 전략은 `LONG` 또는 `SHORT` 방향, 선언형 진입조건, 단위가 명확한 TP/SL이 모두 있어야 PAPER 전진검증을 시작할 수 있습니다. `PAPER 실행 규칙 미구조화`가 보이면 원문 보존은 완료됐지만 실행 전략은 아직 아니므로 새 버전에서 규칙을 구조화해야 합니다.
- AI 자동 리포트 요약과 상세는 같은 NoahAI 청산 완료 원장을 사용합니다. 거래소 확인 체결 수와 NoahAI 청산 수는 부분체결·진입·청산 때문에 1:1이 아닙니다.
- `일반 안내`는 외부 Provider를 호출하지 않고 설정 정본·현재 관리 포지션·최근 구조화 신호·실행 메트릭을 주제별로 설명합니다. `심층분석`만 사용자가 선택한 Provider·모델·예산을 사용합니다.
- 근거가 없으면 보유 이유·추세·TP/SL을 임의로 만들지 않으며, 수동·외부 보유자산을 NoahAI 관리 포지션으로 단정하지 않습니다.
- 기본 코인 선정은 거래소 지원 시장·유동성·캔들·변동성·거래량·추세를 이용해 분석 유니버스를 만들며 주문을 강제하지 않습니다. 일반(confirm) AI 커스텀은 이 자동 후보를 확인하고, 고급(independent)은 승인된 UniversePolicy로 별도 후보를 만들 수 있습니다.
- Strategy Studio의 `검증 근거 보기`는 과거재생과 PAPER를 구분하고 PAPER를 거래소·KRW/USDT별로 표시합니다. 과거재생 통과는 자동 LIVE 승격이나 수익 보장이 아닙니다.
- `PAPER 일시정지`는 미통과나 초기화가 아닙니다. 현재 버전의 가상 체결·거래소별 근거와 활성 검증일수를 보존하며 `PAPER 검증 재개`로 이어서 계산합니다. 일시정지 중 시간과 신규 가상 진입은 7일 조건에 포함하지 않습니다.
- `새 검증 시작`은 재개와 다릅니다. 두 번 확인하면 현재 시도를 이전 시도 이력으로 보관하고 새 attempt ID로 0일부터 시작합니다. 최소 거래 수 또는 7일을 채우지 못한 상태는 `진행 근거`이며 실패가 아닙니다.
- 같은 전략의 v1과 v2를 동시에 PAPER 검증할 수는 없습니다. 한 버전이 검증 중이면 다른 버전의 시작은 차단되며, 먼저 기존 버전을 `PAPER 일시정지`하면 됩니다. 이 절차는 검증값 초기화가 아니라 버전별 실행 권한과 근거를 보호하기 위한 것입니다.
- 각 저장 버전의 `이 버전에 저장된 실행 파라미터 보기`에서 시장상황·전략 역할·진입 방향·청산 책임·TP/SL·위험·레버리지·동시 포지션·적용 범위를 다시 확인할 수 있습니다.
- `검증 거래 내보내기`는 선택한 key/version의 계정 로컬 PAPER 청산만 JSON으로 저장합니다. 종목·방향·진입/청산 시각·가격·보유시간·수량·Notional·레버리지·손익·수수료/슬리피지/세금·진입/청산 사유·진입 당시 국면과 신호 출처·수량 목표/최종값과 제한 사유·실제 TP/SL·Smart Exit 근거가 있는 경우 포함합니다. 구형 기록에 없는 값은 0으로 추정하지 않습니다.
- 세부 검증 거래는 개인 운용 정보이므로 `.noahstrategy` 공유 패키지에 포함되지 않고 daltrading으로 자동 업로드되지 않습니다. 공유할 때는 파일 내용을 먼저 확인하세요.
- 제작 화면의 거래소 호환성은 Upbit·Bithumb 현물의 신규 SHORT 금지와 해외 USDT 선물의 LONG/SHORT 계약을 미리 표시합니다. 지원하지 않는 방향을 반대 의미로 바꾸어 실행하지 않습니다.
- Upbit·Bithumb은 KRW 현물이라 신규 SHORT와 레버리지가 없습니다. 내부 SHORT 분석은 NoahAI 관리 LONG의 청산일 뿐이며, 관리 원장이 없는 수동·에어드롭·외부 보유분을 매도하지 않습니다. PAPER도 같은 LONG/청산 계약을 사용합니다.
- Binance·Bybit·OKX·Bitget은 USDT 선물이라 LONG/SHORT와 reduce-only 청산을 사용합니다. 계정의 단방향/헤지 모드가 주문 필드와 맞지 않으면 진입을 추정하지 않고 실패 폐쇄해야 합니다.
- 업비트 시장가 BUY는 KRW 총액, 시장가 SELL은 base 수량입니다. LEARNING 행은 신호·학습 기록이며 실제 체결이나 PAPER 성과가 아닙니다.
- 15분봉 기반 빠른 시장국면은 기본 5분 TTL로 거래소별 공유하므로, 10초 거래 루프마다 같은 데이터와 로그를 반복하지 않습니다.
- v3.9.1.15에서 Binance 후보 캐시는 현재 사용자 계정 폴더로 격리됩니다. `선택된 코인 수`가 10이어도 모두 `fallback_unscored`이면 실행 가능한 후보는 0개이며, 성공 선정으로 캐시하지 않고 기본 60초 쿨다운의 single-flight 재선정으로 복구합니다. 복구 전에는 기존 포지션 보호만 계속하고 신규 상세 분석·진입은 하지 않습니다.
- v3.9.1.14 이하의 `최종 검증 실패: 5.10 USDT < 20 USDT`는 정상 HOLD가 아니라 목표 주문금액과 거래소 최소 주문규격을 혼용한 오류입니다. v3.9.1.15 후보는 목표금액·다중 거래소 승인 수량 상한·거래소 최소 주문/수량/정밀도를 분리합니다. 승인 상한 안에서 최소 규격을 만족하면 진행하고, 만족하지 못하면 위험을 늘리지 않고 주문 가드레일로 차단합니다.
- Strategy Studio Level 4의 안정형·표준형·적극형은 위험예산·최대 비중·레버리지 상한·국면 이탈 대응만 조절합니다. 사용자 승인·LIVE 권한·일일 손실 중단·주문 규격·TP/SL·중복 주문 방지·포지션 대조·긴급 정지는 모든 단계에서 유지됩니다.
- daltrading 무료 전략 허브의 첫 다운로드는 가격 0인 취득 기록과 해당 버전의 계정별 영구·비독점 NoahAI 실행 라이선스를 발급합니다. NoahAI 전략 스튜디오의 `내 전략 라이선스` 링크에서 취득 버전과 재다운로드 상태를 확인하며, 가져온 파일은 항상 비활성 검토 버전으로 시작해 승인·자동검증·PAPER를 다시 거칩니다.
- 시장 후보 평가를 완료하지 못한 안전 폴백은 `0.00점`이 아니라 `미산출`로 표시합니다. 고득점이나 추천 의미가 아니며, 연결·데이터 회복 뒤 재선정해야 합니다.
- 현재 v3.9.1.24는 소스 후보입니다. Windows v3.9.1.24 설치본·v3.9.1.23→v3.9.1.24 자동업데이트·6개 거래소·4개 증권사 비회귀·Strategy Studio/Hub 화면 E2E가 통과하기 전에는 `문제없음`이나 `배포 완료`로 판정하지 않습니다.

### v3.9.0.8 Fix 1 공통 안정화

- 동적 탭의 화면·캐시·콜백·native 메뉴는 한 소유권 경계로 함께 정리됩니다. AI 어시스턴트뿐 아니라 시장 트렌드·금융 인텔리전스·AlphaArena·코인/종목 정보·거래 통계·AI 애널리스트도 같은 기준입니다.
- 통합자산은 계좌에서 수신한 현재 잔고 한 저장소를 사용하고 KRW/USDT를 환율 없이 더하지 않습니다. 종료 거래는 구형·신형 DB 공통 어댑터를 거쳐 실현손익·시나리오·보안 점검에 제공됩니다.
- AI 질문과 심층분석은 요청 ID 하나가 하단 기록에서 `응답 생성 중 → 응답 완료/오류`로 갱신됩니다.
- 소스 자동 회귀는 통과했지만 Windows EXE를 새로 빌드해 설정·서비스 왕복 100회, USER/GDI 상한, 실제 잔고·AI 응답 화면을 확인하기 전에는 배포 완료가 아닙니다.

### v3.9.0.7 Fix Patch 3 통합 안정화

- Fix Patch 3는 Windows VC 런타임 단일화·ONNX 지연 로드에 UI 탭 생명주기, 거래소별 데이터 출처, KRW 현물 잔고-포지션 조정을 함께 포함합니다.
- 서비스·거래소 탭은 callback을 정리하고 실제 프레임까지 파괴하므로 반복 전환 뒤 설정창의 Windows 메뉴 리소스가 누적되지 않아야 합니다.
- Bitget·Bybit·OKX·Upbit·Bithumb 분석은 선택 거래소의 데이터만 사용합니다. Binance가 활성 범위에 없는 회원은 Binance REST/WebSocket을 만들거나 Binance K라인·펀딩비·OI로 폴백하지 않습니다.
- 비바이낸스 실행은 거래소별 코인 저장소만 사용합니다. 과거 전역 코인 목록 호환 폴백과 현재 화면 거래소로의 암묵 주입은 제거했습니다.
- 토큰화 주식·지수·원자재·외환 파생상품은 알려진 심볼과 거래소 상품 metadata 양쪽에서 암호화폐 후보에서 제외합니다.
- Upbit·Bithumb의 최소주문금액 미만 잔여분은 dust로 분리합니다. 중요 보유자산은 최대 포지션 수에 합산하고 동일 종목 중복 진입을 차단하며, 청산은 앱이 진입한 관리수량만 대상으로 합니다.
- PAPER·LEARNING은 개인 잔고/체결 API를 호출하지 않으며, AI 분석 완료가 회원등급·거래소 승인·실주문 권한을 자동으로 켜지 않습니다.
- 현재 Windows EXE는 `pending_windows_rebuild`입니다. Fix Patch 3 표시와 게시 SHA-256을 확인하기 전에는 이 수정이 설치됐다고 판단하지 않습니다.

### Fix Patch 5 인앱 실거래 필수 안내

- 업데이트 후 최초 실행에 `실거래 시작 전 필수 안내`가 한 번 표시됩니다.
- 이후에는 대시보드 하단 `실거래 필수 · 업데이트·사용법`, 설정 상단 `실거래 필수 안내`, `메뉴얼 → 실거래 준비`에서 같은 내용을 다시 확인할 수 있습니다.
- 창 제목·매뉴얼·대시보드 하단에 `v3.9.0.5 Fix Patch 5`가 표시되어야 이번 누적 수정본입니다.
- 인앱 안내는 6개 거래소·4개 증권사, 회원등급, LEARNING/PAPER/LIVE, 현물 보유자산·선물 포지션, 다중 대상 실행, API·증권 계약·가드레일·실계정 검증 순서를 설명합니다.
- AI 실행과 전략 승인은 실제 주문 권한을 자동으로 켜지 않습니다. PAPER OFF, 거래소 주문 대상 또는 증권 전역·증권사별 LIVE 권한, API 준비상태와 가드레일이 별도로 필요합니다.

### v3.9.0.5 안정성·AI 커스텀·멀티 AI 변경사항

- Windows에서는 설치된 `맑은 고딕(Malgun Gothic)`을 Tk·CustomTkinter 기본 글꼴로 우선 사용하고, 없으면 Segoe UI·Arial 순으로 대체합니다. 화면 배율에서 글자가 잘리면 배율과 화면 캡처를 고객지원에 전달합니다.
- 시작 시 실행 세션 표식을 만들고 정상 종료 때 제거합니다. 다음 시작에서 표식이 남아 있거나 처리되지 않은 메인·스레드·화면 callback 예외가 발생하면 `logs/runtime_stability.jsonl`에 진단 근거를 남깁니다. 지원 환경의 네이티브 치명 오류는 `logs/runtime_faulthandler.log`에 남깁니다. 기존 제보만으로 종료 원인을 하나로 단정하지 않습니다.
- 숨은 AI 학습·금융 인텔리전스·거래소·증권사 상세 탭은 반복 callback과 잔고·포지션·통계 API 조회를 중지하고 다시 열 때 즉시 재개합니다. v3.9.0.5는 모든 거래소 탭에서 실제 실행부의 연결을 재사용하며 12초 안에 값·API 키 없음·API 키 오류·연결 실패·빈 응답·조회 실패 중 하나를 표시합니다. 실제 자동매매 워커는 화면 표시와 분리되어 계속 작동합니다.
- 거래소 실행 성공은 카드의 빨간 `정지` 버튼과 LIVE/PAPER/LEARNING별 상태로 즉시 동기화하며, 화면 콜백이 누락돼도 실제 워커를 주기적으로 대조해 복구합니다. CCXT 거래소 잔고는 한 번 갱신에 인증 API를 한 번만 호출합니다.
- Fix Patch에서는 국내 현물 `KRW`, 해외 선물 `USDT` 뒤에 실제 양수 보유자산을 최대 2개 표시합니다. BTC·ETH를 고정하지 않으며 3칸을 넘는 자산은 화면에서만 생략되고 전체 잔고 데이터에는 유지됩니다.
- AI 어시스턴트는 설정 8개 탭의 설치된 v3.9.0.5 정본을 질문별로 검색하고 API 키를 제외한 현재 설정을 함께 설명합니다. 외부 AI가 꺼져도 설정 질문은 로컬 정본으로 답하며 값을 바꾸려면 별도 최종 확인이 필요합니다.
- `설정 → 거래소 선택`의 첫 체크는 화면·시세·분석·학습 범위입니다. 실제 주문 대상은 같은 화면의 `실제 주문 실행 거래소`에서 별도로 선택합니다.
- 학습 전용 거래소에서 시작을 누르면 시세 수집·종목 선정·기본 AI·AI 커스텀·시장국면·수익성·후행 전략·계좌 위험·수량·청산계획·최종 주문 규격과 전략명·버전·허용/차단 사유 기록까지 실행됩니다. 거래소·증권사 상태를 바꾸는 레버리지/마진 설정과 신규 주문은 제출하지 않습니다. 주문 대상으로 선택해도 API 키·권한·회원등급·손실한도·주문 가드레일을 통과해야 실제 주문할 수 있습니다.
- AI 커스텀 초보자 화면은 `AI가 시장에 맞춰 자동 대응` 1개와 검토용 초안 `추세 따라가기`, `눌림목 진입`, `횡보장 저점·고점 대응`, `강한 거래량 돌파` 4개로 구성합니다.
- AI 커스텀의 거래소·증권사·전체 자산 선택은 전략 적용 범위입니다. 일반은 사용자 고정+NoahAI 자동 후보를 기본 AI 뒤에서 확인하고, 고급은 StrategyUniversePolicy의 로컬 필터와 전략 지표로 독립 후보를 만듭니다.
- 전략 국면 기준은 전체 시장(권장)·종목·둘 다·사용 안 함 중 명시합니다. 선택 범위 데이터가 없으면 다른 기준으로 몰래 대체하지 않습니다.
- `모든 블록체인`이나 `모든 증권사`를 선택해도 한 대상의 종목 목록을 다른 대상에 복사하지 않습니다.
- 여러 실제 주문 대상은 기본적으로 각각 실행합니다. 같은 기회를 ID로 묶어 통합 위험과 결과를 관리하며, 사용자가 설정한 경우에만 총위험 분할 또는 우선순위 한 곳을 적용합니다.
- 고급모드에서는 EMA·SMA·RSI·ATR·거래량 평균 기간 2~500, 조건별 시간봉, AND/OR, 진입·전체 청산 규칙을 안전한 JSON으로 편집합니다.
- 고급 규칙의 요청 지표와 거래소 캔들에서 계산한 실제 값은 런타임 비교 메타데이터로 남습니다.
- 부분익절·추적손절·재진입, TradingView webhook, 임의 사용자 지표 샌드박스는 아직 후속 단계입니다.
- AI 자동 대응과 모든 초안은 조건 불일치·다중 시간대 충돌·유동성·스프레드·손익비 미달에서 HOLD를 우선합니다. 이름에 표시되는 위험도는 상대적 설명이며 승률이나 수익을 보장하지 않습니다.
- AI 어시스턴트에 `이평선 매매를 알고 있어?`라고 물으면 적합 국면과 위험을 설명합니다. `그 전략 초안을 AI 커스텀에 넣어줘`라고 명시하면 입력창까지만 전달하며 분석·저장·승인·실행하지 않습니다.
- OpenAI·DeepSeek·Anthropic Claude·Google Gemini·Kimi는 분석·어시스턴트·작업별 Provider 선택 대상입니다. Kimi는 정식 OpenAI 호환 API로 텍스트·JSON·비전을 지원하지만, 일반 Kimi 서비스와 개발자 API 키·잔액·사용량 과금은 별개입니다.
- Claude Code 구독/로그인이 아니라 Anthropic API 키를 사용합니다. 설정의 기준일 가격표와 공식 가격 링크로 단가를 확인합니다.
- 설정 순서는 `AI 엔진/API → 제공사 → 키 발급 → 키 입력 → 모델 새로고침 → 프리셋·문답 정책 → 저장`입니다. 제공사를 바꿔도 주문 권한과 거래 가드레일은 자동 확대되지 않습니다.
- v3.9.0.5는 v3.9.0.3 후보의 필수 keyring·Windows 보안 저장소 의존성을 철회했습니다. AI 키는 거래소·증권사 키와 같은 사용자별 로컬 설정에 저장하며 사용자는 Python 패키지를 설치하지 않습니다.
- 입력란에 `v3.9.0.3 키 재입력 필요`가 표시된 Provider만 키를 한 번 다시 입력합니다. 설정·백업에는 민감정보가 있으므로 지원 로그 압축에 포함하지 않습니다.
- 설정·Provider 진단은 점검만으로 사용자 파일을 변경하지 않으며 API 키나 변경 전후 비밀값을 로그에 출력하지 않습니다.
- AI 커스텀 전략 파일은 사용자 전용 권한으로 저장하고 초안·승인·검증·활성 버전 참조와 롤백을 확인합니다.
- `default_tp 0.18 → 0.0018`, `default_sl 0.2 → 0.002`는 구버전 표시 단위를 내부 fraction으로 옮기는 변환입니다. 설정 마이그레이션에서는 최초 1회만 발생합니다.
- 2026-07-30 수정에서는 균형 전략 프리셋이 매 시작마다 구단위를 다시 주입하던 경로도 제거했습니다. 프리셋의 `0.18%/0.20%`는 시작 기본값이지 모든 주문의 고정값이 아니며, 실제 TP/SL은 분석·승인 전략·변동성·RR 가드레일에 따라 달라질 수 있습니다.
- 실시간 로그는 `저장된 기본 폴백`, `현재 종목에 적용된 TP/SL과 근거`, `거래소에 실제 제출·확인된 보험 가격과 상태`를 분리합니다. 내부 단위는 fraction 하나이며, 서버 보험 주문이 없는 현물·증권 경로는 미제출 상태와 클라이언트 모니터 청산을 표시합니다.
- 전체 KPI 거래 수는 해당 거래소 전체 성과이고 TP/SL 최적화 표본은 거래소·종목별 청산 완료 이력입니다. 종목 표본이 부족하면 같은 거래소의 다른 종목 청산 표본을 축소 가중치로 보조 사용하며, 로그에 두 표본 수와 적용 출처를 분리합니다.
- 통합 거래소 PAPER 청산은 순손익·비용·가격·수량·방향과 KRW/USDT를 원장에 기록합니다. 계산 근거가 없는 v3.9.1.10 통합 거래소 0원 행은 패배나 0% 수익으로 간주하지 않고 `과거 손익 미확정`으로 제외합니다.
- 업데이트는 열린 포지션/주문 기본 연기, TP·SL 유지 검증 또는 전량 청산 체결 확인, SHA·Windows 코드서명, 종료 flush, 재시작 health check를 모두 통과해야 완료됩니다. 성공 뒤에도 사용자가 거래를 다시 켜기 전까지 신규 주문은 잠깁니다.
- `문답 절약형`은 최근 4회·입력 6,000자·출력 500토큰, `질문답변 표준형`은 8회·12,000자·1,200토큰, `분석 정밀형`은 12회·20,000자·2,200토큰을 기본 예산으로 사용합니다. 문답 정책은 거래 전략·수익성·주문 가드레일을 바꾸지 않습니다.
- 위 변경은 v3.9.0.5에 포함되어 있습니다. 실제 AI 호출 성공 여부는 사용자 Provider 키·모델 권한·잔액·네트워크 상태에 따라 달라집니다.

### v3.9.0.2 AI 커스텀·어시스턴트

- `레퍼럴` 무료회원은 daltrading 관리자가 활성화한 Binance·Bybit·OKX·Bitget만 사용할 수 있고 Upbit·Bithumb 및 증권 기능은 사용할 수 없습니다.
- AI 어시스턴트에 레퍼럴 등급·허용 거래소·코드 관리 위치를 물으면 API 없이도 운영 기준을 설명합니다. 코드와 가입 URL은 클라이언트가 아니라 daltrading `설정`에서 관리합니다.
- 등급·허용 목록은 로그인과 1분 상태 확인으로 다시 적용됩니다. 허용이 취소된 거래소의 새 주문 루프는 중지하지만 열린 포지션은 자동 강제 청산하지 않습니다.
- 2026-07-27 화면 후속 패치에서 거래 통계 표의 화면 활용을 확대하고, 거래소·증권사 소스 탭을 기능 탭과 은은하게 구분했습니다.
- 블록체인·주식/증권의 `시장 트렌드`, `AI 학습`, `AI 어시스턴트`는 `AI 커스텀`·`거래 통계`와 같은 배경·카드·입력창·테두리·기본 버튼 체계를 사용합니다. 거래소·증권사 이름 탭의 보조 강조색만 운영 소스 구분을 위해 유지합니다.
- 블록체인·주식/증권 `AI 리포트`, 주식/증권 `거래 통계`, 생활금융 `월간 재무 요약`도 같은 공통 색상 계층을 사용하며, AI 어시스턴트 대화 화면의 내부 모델명 상시 표시는 제거했습니다.
- 관리자 KPI는 1일·7일·30일·90일의 동시 실행 거래소 사용자 분포를 함께 표시하고, KRW는 실제 체결·금액 확인·실패 건수로 수집 상태를 구분합니다.
- 금융 인텔리전스 각 화면의 `처음 사용`과 `AI에게 사용법 묻기`로 시장·코인/종목 탐색·지표·전략 검증·이벤트·기업·기관 메뉴를 바로 질문할 수 있습니다.
- `코인 정보`는 내 계좌·선택 코인 운용 상태이고 `코인 탐색`은 여러 코인 후보 비교입니다. 금융 인텔리전스의 간단 전략 검증은 AI 커스텀 외부 전략 자동검증과 구분됩니다.
- AI API가 없어도 위 제품 사용법과 조회/주문 경계를 로컬 도움말로 답합니다.
- 기간별 실행 분포·KRW 체결금액 보강을 포함한 전체 회귀는 `1,079 passed, 6 skipped, 3 warnings`, daltrading은 `20 passed`, 문서 정합성과 macOS 실위젯 렌더링 PASS입니다.
- `처음 사용법 AI에게 묻기`: 소스 입력부터 저장·승인·자동검증·최종 적용까지 현재 화면 순서로 질문
- `이 결과 AI에게 묻기`: 전략명·입력 형식·추천 시장상황·누락 조건을 전달해 XAI 결과를 쉬운 말로 설명
- 소스에 명시된 상승·하락·횡보·고변동·저변동만 추천하며 제외 문맥과 근거 없음은 자동 확정하지 않음
- 위치·방법 질문과 실제 변경 요청을 구분하고, 모호한 변경은 재질문
- 설정 변경은 현재 상태 분석, 위험 판단, 허용 키·범위, 변경 전/후, 사용자 확인, 저장·갱신·이력 순서
- 저장 후 파일을 다시 읽어 불일치하면 자동 롤백하며 사용자별 감사로그로 재시작 후에도 마지막 설정을 되돌릴 수 있음
- 주문 시작·중지·임의 주문·API 키·출금은 보호 작업으로 분류하고 `실행하지 않았습니다`를 명시
- AI 커스텀 실행 필드는 SMA/EMA 20·50·200, ADX, ATR, MACD 세부값, 볼린저, 거래량·UTC 시간/요일과 직전·현재 캔들 기반 `crossover/crossunder`를 지원하며 미지원 조건은 승인 전에 차단
- 자동검증은 비중첩 단일 포지션과 양쪽 수수료·슬리피지·스프레드를 적용하고 비용 전/후 PnL·PF·기대값·MDD·거래목록·국면 결과를 표시
- 코인 명시 청산은 진입 버전과 함께 포지션 모니터에 보존합니다. 주식/ETF도 전략 버전·명시 청산·TP/SL을 XAI 기록에서 복원해 클라이언트 자동 청산에서 평가하며, 증권사 서버 측 보험/브래킷 주문은 어댑터별 지원 범위입니다.
- 시장국면 판단은 판단 시각·신뢰도·현재/후보 국면을 기록하고 오래된 입력 차단과 전환 히스테리시스를 적용
- 거래 통계·AI 리포트는 실제 체결금액을 USDT·KRW로 분리하고 검증 가능한 청산만 평균 보유시간에 포함
- 전체 자동 회귀 `1,079 passed, 6 skipped`, 문서 정합성 PASS. Windows 설치 전수 클릭·실제 URL·실계좌 검증은 별도

### v3.9.0.1 화면 구분과 하단 안내

- 하단 상태·업데이트·AI 실행 기록은 한 줄로 표시되어 AI 실행·거래소 화면의 마지막 항목을 가리지 않습니다.
- 코인 정보·종목 정보는 탭 전체가 스크롤되며, 종목 정보의 `내 보유·최근 체결 종목`과 검색용 시장 미리보기를 분리해 표시합니다.
- `업데이트·사용법`은 가운데 강조 영역에 유지하고, 상세 AI 실행 이력은 오른쪽 `기록` 버튼에서 확인합니다.
- 상단 서비스·매뉴얼·설정 아이콘은 운영체제 이모지가 아니라 앱이 직접 렌더링하므로 macOS와 Windows에서 동일하게 보입니다.
- 현재 서비스는 고유 선택 색상과 밝은 테두리로 구분되며 아이콘이 보이지 않아도 버튼 텍스트는 유지됩니다.

### v3.9.0.8 Fix 4 AI 호출 비용·주문 안전

- 사용자 제공 데이터에서는 2026-07-24~08-09 매일 시장 AI 호출 한도 1,200회를 모두 소진했습니다. 같은 LONG/SHORT·RSI 극단 상태와 새 캔들을 매 루프 신규 이벤트로 보던 원인을 수정했습니다.
- 같은 상태는 30분 재사용하고, 단순 새 캔들은 새 AI 호출 사유가 아닙니다. 후보 방향 전환·RSI 임계 진입·MACD 교차·시장국면 전환은 즉시 분석하되 미세 변화는 최소 5분 간격을 둡니다.
- 기존 사용자 설정에 1,200회가 남아도 명시적 고비용 허용이 없으면 시장분석은 전역 240회/일, 거래소별 40회/일, 6,000회/월 상한을 적용합니다.
- 시장분석 밖의 패턴·진입·포지션 크기·손익·리포트·진단도 공통 자동호출 원장에 기록합니다. 기본 전체 자동호출 상한은 360회/일·9,000회/월이며 역할별 상한을 함께 적용합니다. 한도 도달 시 로컬 신호와 기존 안전검사는 계속됩니다.
- AI 어시스턴트에서 사용자가 직접 보낸 질문은 자동매매 호출예산과 분리합니다.
- LIVE 암호화폐 주문은 주문 제출 전 영속 명령을 선점하고 거래소 client-order ID를 함께 보냅니다. 청산 결과가 모호하면 무작정 반복하지 않고 해당 거래소 신규 진입을 중단한 뒤 거래소 주문·포지션 조정을 요구합니다.

### v3.9.0.1 거래기회 보존형 AI 비용·성과 운영

- `실제 주문 실행 거래소`에서 명시적으로 선택한 거래소만 실제 주문하고 다른 활성 거래소는 학습 전용으로 동작합니다.
- 같은 시장상태는 AI 응답을 15분 재사용합니다.
- 새 캔들·가격·RSI·MACD·시장 국면 변화가 있으면 기다리지 않고 즉시 새 분석을 요청합니다.
- 일·월·거래소별 호출 한도에 도달해도 AI 실행이 멈추지 않습니다. 로컬 기술 신호로 계속 분석하고 LONG/SHORT 후보를 유지합니다.
- 일반 성과 미달은 1포지션·1배·위험배수 0.15 회복 학습으로 표본을 계속 쌓고, Hard MDD 초과만 신규 진입을 차단합니다.
- 실제 체결 수수료·주문번호·모델·전략을 함께 기록하고 7일 리포트는 수수료 차감 순PnL과 거래당 순기대값을 우선 비교합니다.
- 이 정책은 SaaS 전용이 아니라 클라이언트·IP 라이선스·무료 라이트 버전에 공통 적용됩니다.

### v3.9.0.1 AI 커스텀 위험기반 전략

- AI가 빠진 진입·청산·손절·익절·위험예산·시장상황을 질문과 예시로 안내하며 임의로 채우지 않습니다.
- 사용자는 `거래당 허용손실`, `최대 증거금 사용률`, `레버리지 상한`을 정합니다.
- 실제 레버리지는 거래당 허용손실과 전략 손절거리에서 필요한 노출을 구한 뒤 증거금·레버리지 상한 안에서 계산합니다. 상한은 목표 배수가 아닙니다.
- 대상 시장상황이 끝나면 `기본 NoahAI에 맡김` 또는 `커스텀 신규 진입 일시정지`를 선택합니다.
- 최근 승률이나 시장상황만으로 시스템이 사용자 전략을 자동 변경하지 않습니다. AI 개선안은 전략 행의 `개선안 · …`에서 확인하고 새 버전으로 승인해야 반영됩니다.
- 1배·최대 1% 강제는 자동검증 미통과 전략의 사용자 선택형 안전 시험에만 적용됩니다. 일반 운용 전체에 1배를 강제하지 않습니다.
- 성과 기반 자동 최적화는 TP/SL·레버리지 제안만 만들며 사용자 승인 전 공유 설정이나 주문값을 직접 변경하지 않습니다.

### AI 실행/커스텀 전략 실제 운용 흐름

- AI 커스텀은 사용자가 이해하는 이동평균·추세·RSI·돌파·TradingView·영상·책 전략을 초기부터 활용하고, NoahAI 학습은 이후 국면 선택·감독·비용·체결 품질을 개선하기 위한 기능입니다.
- `기본 AI 후보 재확인`은 NoahAI 후보를 사용자 조건으로 확인합니다.
- `사용자 전략 원형 독립 실행`은 사용자 전략이 방향·지표·시간봉·임계값·TP/SL·청산조건을 결정합니다. NoahAI 기본 신호 동의·합의 임계값·전체 전략 수익성으로 다시 바꾸거나 차단하지 않고, 시장국면·계좌·주문 안전만 감독합니다.
- 전략의 SL이 계좌 위험예산과 충돌하면 SL을 좁히지 않고 수량을 줄이며, 최소 수량도 안전하지 않으면 요청값과 차단 사유를 표시합니다.

- 거래소별 `시작`은 준비 중 `시작 중…`, 실제 워커 확인 후 `진행 중`으로 표시되고 버튼은 `중지`로 전환됩니다.
- AI 커스텀 전략은 일반 설정 제안과 별개로 다음 단계를 강제합니다.
  1. 텍스트/Pine/PDF/OCR/영상/YouTube/TradingView 근거 추출
  2. 진입·청산·손절·익절·포지션·시장조건 구조화
  3. 누락 조건 질문·입력 예시와 XAI 근거/변경/위험 설명
  4. 사용자 승인
  5. 과거 재생(보조)·실시간 관찰학습·최소단위 제한 운용 중 적합한 실행 검증
  6. 최종 확인 후 실거래 적용
- 최대손실·포지션·집중도·데이터/API·주문 안전 경계는 전략보다 항상 우선하지만, 전략 지표·방향·TP/SL을 몰래 다시 쓰지는 않습니다.
- 출금 API/외부송금은 지원하지 않으며, 전략별 프라이빗 버전은 최대 10개까지 보관합니다.
- PDF/문서, 차트 이미지 OCR, 로컬 영상 대표 프레임, YouTube 자막+화면 OCR, TradingView 공개 링크/Pine Script 입력을 현재 제공합니다.
- 보호된 TradingView 원문은 우회하지 않으며 사용자가 내보낸 Pine 또는 공개 소스를 이용합니다.
- 상태와 버전은 대시보드 `AI 커스텀` 탭에서 확인합니다.
- 설정에서 AI 커스텀 런타임을 켠 뒤, 자동검증 통과 전략은 검증 통과 운용, 미통과 전략은 사용자 선택 시에만 1배·최대 1% 제한시험을 할 수 있습니다.
- 상세 사용법과 한계는 `docs/AI_CUSTOM_STRATEGY_ARCHITECTURE.md`를 확인합니다.

---

## 목차

1. [개요: "AI 실행"이란?](#-개요-ai-실행이란)
2. [준비 단계](#-준비-단계)
3. [사용 방법: 3가지 시나리오](#-사용-방법-3가지-시나리오)
4. [결과 해석](#-결과-해석)
5. [자주 묻는 질문](#-자주-묻는-질문)
6. [문제 해결](#-문제-해결)

---

## 개요: "AI 실행"이란?

### 핵심 개념

**"AI 실행" 버튼은 다음을 합니다:**

```
┌─────────────────────┐
│ 1⃣  "AI 실행" 클릭     │
└──────────┬──────────┘
           │
┌──────────▼──────────┐
│ 2⃣  거래소·API·증권 진단   │  ← AI가 자동으로
└──────────┬──────────┘     모든 설정을 확인합니다
           │
┌──────────▼──────────┐
│ 3⃣  준비도 + 위험경고      │  ← 색깔로 표시됩니다
│  일반 / 주의 / 고위험 │
└──────────┬──────────┘
           │
┌──────────▼──────────┐
│ 4⃣  "확인" 또는 "취소"    │  ← 사용자가 선택합니다
└──────────┬──────────┘
           │
┌──────────▼──────────┐
│ 5⃣  결과 저장 + 기록      │
└──────────────────────┘
```

### 이것이 필요한 이유

| 문제 | 해결 |
|------|------|
| "거래할 준비가 됐나?" | AI가 자동 진단 |
| "어떤 설정이 부족한가?" | 부족 항목 목록 표시 |
| "위험하지 않나?" | 위험 레벨 경고 |
| "무엇을 수정해야 하나?" | "수정하기" 버튼으로 바로 이동 |

---

## 준비 단계

### 필수 조건

1. **NoahAI v3.8.9.22 이상** 설치
2. **거래소 API 키** 최소 1개 설정됨
   - 바이낸스, 업비트, OKX, 바이빗, 비트겟 중 1개 이상
3. **AI API 키** 설정됨
   - 설정 → OpenAI API 섹션에서 확인

4. **AI 설정 적용 방식 확인**
   - 설정 → OpenAI API에서 `사용자 최종확인`으로 고정됩니다.
   - 과거 `ai_auto` 설정이 있어도 적용 버튼과 최종 확인을 생략하지 않습니다.

5. **AI 모델 역할별 배치 확인 (비용 최적화)**
   - 설정 → OpenAI API 섹션의 `AI 모델 역할별 배치`에서 확인
   - `빈번 호출`: 신호분석/패턴/포지션 크기(저비용 권장: gpt-4o-mini)
   - `표준 분석`: 손익 분석/리포트(표준 권장: gpt-4o)
   - `정밀 진단`: 문제 진단/파라미터 최적화(고성능 권장: gpt-4o 또는 gpt-5)
   - 동일 설정은 AI 어시스턴트 대화로도 제안할 수 있지만 변경 전/후와 2단계 확인 뒤 저장됩니다.

6. **원클릭 프리셋으로 쉽게 시작하기**
   - 위치: 설정 → OpenAI API, 또는 AI 어시스턴트 → 설정관리
   - `절약형`: 비용이 가장 중요할 때
   - `균형형`: 잘 모를 때 기본 권장
   - `정밀형`: 고난도 진단 품질이 중요할 때
   - 프리셋 클릭은 "미리보기"이며, 저장/최종확인 전에는 실제 반영되지 않습니다.

7. **사용자 맞춤 최적화 방법 (3단계)**
   1. 먼저 `균형형`으로 3~7일 사용
   2. 비용이 부담되면 `절약형`으로 전환, 진단 정확도가 부족하면 `정밀형`으로 전환
   3. `빈번 호출/표준/정밀` 3개 티어를 본인 사용 패턴에 맞게 세부 조정

8. **초기 AI 온보딩(5문항)으로 자동 시작값 만들기**
   - 시작 위치:
      1. 설정 → OpenAI API → `AI로 초기 설정하기 (5문항 가이드)`
      2. AI 어시스턴트 → 설정관리 → `초기 온보딩 시작`
      3. 채팅 입력으로 `온보딩` 또는 `초기 설정`
   - 질문 항목: 목표 / 리스크 / 예산 / 적용방식 / 재검증 기준
   - 결과 화면에서 반드시 확인할 것:
      - 무엇이 바뀌는지(설정 요약)
      - 왜 바꾸는지(근거 설명)
      - 언제 다시 점검할지(7일 또는 최소 표본)
   - 고위험 성향을 선택해도 1차는 완화 설정으로 시작할 수 있으며, 관찰 후 재조정됩니다.

9. **증권사 연결 1차 진단 옵션 확인 (v3.8.9.22 패치)**
   - 위치: 설정 → 거래소 API
   - 기본 ON 옵션: `설정 저장 후 연결 위험이 보이면 자동으로 1차 진단 안내`
   - 저장 후 위험 경고가 뜨면 `AI 연결 점검`으로 현재 상태와 다음 조치를 확인하세요.

### v3.8.9.22 패치 핵심 안내 (2026-06-12)

- 설정 화면에 `AI 연결 점검` 버튼이 추가되었습니다.
- 버튼 클릭 시 현재 선택 증권사(키움/신한/미래에셋/한국투자증권) 기준으로 API 타입/버전과 누락 항목을 먼저 점검합니다.
- 저장 직후에도 위험 신호가 있으면 자동으로 같은 1차 진단 팝업이 표시됩니다.
- 팝업 안에 왜 코인과 다른지, 브로커별 핵심 점검, 성공 기준, 실패 시 전달 항목이 쉬운 문장으로 포함됩니다.
- `지원요약 복사`와 `파일로 저장`으로 개발팀/서비스사 전달용 자료를 바로 만들 수 있습니다.
- 상세 절차 문서: `docs/STOCK_BROKER_WINDOWS_CONNECTION_CHECKLIST_20260611.md`
- AI 학습 데이터 보관은 거래소별 최신 N개 슬라이딩 방식입니다.
   - 선물(고빈도): `binance/bybit/okx/bitget` 최대 `10,000`
   - 현물(중저빈도): `upbit/bithumb` 최대 `6,000` (기타 기본 `8,000`)
- 따라서 바이낸스 학습 분석수가 `10,000` 근처에서 유지되는 것은 정상 동작입니다.
- 이 경량화는 학습 JSON 저장/로딩 안정화 목적이며, KPI 이벤트 전송 경로와 거래 통계 집계 경로를 대체하지 않습니다.

### v3.8.9.22 패치 핵심 안내 (2026-06-16)

- 설정의 `AI 실행 준비도 진단` 주변과 업데이트 탭에 다음 정보가 표시됩니다.
   - 현재 OS / OS 비트수
   - 현재 Python 버전 / Python 비트수
- 키움 OpenAPI+ 사용 시 현재 앱 경로 기준으로 `32bit Python`이 가장 안정적인 조합이라는 안내가 추가되었습니다.
- 설정 화면에서 `32bit Python 다운로드`와 `설치 가이드 보기` 버튼으로 바로 이동할 수 있습니다.
- 한국투자증권(KIS)도 같은 1차 진단 흐름에서 점검 대상에 포함됩니다.

### v3.8.9.22 핫픽스 안내 (2026-06-13)

- 다중 거래소 코인 분리 버그를 수정했습니다.
   - 업비트/바이비트를 시작해도 바이낸스 코인이 함께 섞이지 않도록 보강되었습니다.
- 설정 저장 후 자동 증권 연결 진단이 UI를 붙잡는 경로를 비동기로 전환했습니다.
   - 저장 직후 탭 전환/스크롤이 멈추는 체감이 줄어들도록 개선되었습니다.
- 사용자 확인 방법:
   1. 업비트 또는 바이비트를 먼저 선택하고 코인 목록을 새로고침합니다.
   2. 바이낸스 USDT 심볼이 함께 섞이지 않는지 확인합니다.
   3. 설정 저장 직후 화면을 바로 조작해 응답이 즉시 오는지 확인합니다.

### v3.8.9.22 추가 안내 (설정 설명 보강)

- 설정 → 거래소 API에 `증권 설정 사용법` 안내가 추가되었습니다.
- 설정 → 거래소 선택에 `증권 주문 가드레일` 숫자 설명과 `증권 자동매매 제어` 설명이 추가되었습니다.
- 핵심 해석:
   - `실주문 허용`은 AI 정확도 옵션이 아니라 실제 주문 권한 스위치입니다.
   - 가드레일 숫자는 전략 최적화 값이 아니라 과대주문 방지용 안전 한도입니다.
   - 모르면 기본값 유지, 실거래 초기는 더 보수적인 한도로 시작하는 것이 권장됩니다.

### 선택 사항

- 증권사 브로커 설정 (주식/ETF 거래 할 경우)
- 음성 입력 설정 (TTS/STT 사용 할 경우)

### v3.8.9.20 추가 안내 (학습 구조 사전 준비)

- 연합학습/집단강화학습 사전 준비 구조가 앱에 포함되었습니다.
- 기본값은 모두 OFF이며, 현재 사용자 거래 흐름은 기존과 동일합니다.
- 즉, "미래 확장 준비 완료 + 현재 운영 안정성 유지" 상태입니다.

### v3.8.9.20 추가 안내 (2026-05-29 실시간 청산 net PnL 정합화)

- 실시간 청산 판단에서 사용하는 `net_pnl_percent` 계산이 거래 기록(recorder)과 같은 기준으로 정렬되었습니다.
- 의미:
   - 이전보다 청산 시점 판단 값과 사후 리포트 값의 해석 차이가 줄어듭니다.
   - 거래 통계/AI 어시스턴트에서 보는 손익 설명의 일관성이 높아집니다.
- 사용자 확인 방법:
   1. `거래 통계` 탭에서 최근 7일/30일 승률·손익 확인
   2. `AI 어시스턴트`에서 최근 거래 요약(승률, 총수익, 최근 7일 내역) 확인
   3. 두 화면의 손익 방향성과 청산 해석이 일치하는지 점검
- 한계:
   - 이 패치는 계산 기준 정합화입니다.
   - TP/SL 기본값의 RR 구조나 전역 포지션 사이징 정책은 별도 최적화가 필요합니다.

### v3.8.9.20 추가 안내 (2026-05-31 다중 거래소 안정화)

- 적용 내용:
   - 거래소별 코인 선택을 분리 저장해 다중 거래소 실행 시 코인 오염 가능성을 낮췄습니다.
   - 시작 실패 거래소는 실행 중으로 표시되지 않도록 시작 결과 기반 상태 반영으로 정합화했습니다.
   - 코인 재선택이 거래소 단위로만 동작하도록 보강했습니다.
   - AI 학습 저장 시 거래소별 매니저 캐시를 재사용해 저장 경합 가능성을 완화했습니다.
- 사용자 확인 방법:
   1. AI 실행 후 거래소별 상태가 실제 시작 성공/실패와 일치하는지 확인
   2. 다중 거래소 동시 실행 시 거래소별 코인 목록이 서로 다른지 확인
   3. AI 리포트/학습 기록에서 학습 데이터 누적 흐름이 자연스러운지 확인
- 검증:
   - 대시보드 실행 흐름 회귀 테스트 72건 통과
   - 핵심 수정 파일 문법/정적 오류 점검 완료
   - 원샷 재발 점검: `python3 scripts/multi_exchange_stability_check.py`
   - 점검 리포트: `data/reports/multi_exchange_stability_latest.json`

---

## 사용 방법: 3가지 시나리오

### 권장 진행 순서 (운영 안정화)

1. 거래소/API/계정 모드 점검
2. AI 실행으로 준비도 진단
3. 필요한 설정만 AI 제안으로 조정
4. 변경 전/후를 보고 적용 버튼과 최종 확인을 모두 통과
5. 실행 이력/성과를 주기적으로 검토

### 7일 운영 루프 (잊지 않기 체크리스트)

아래 루프를 기준으로 운영하면 "개선 신호는 있는데 PnL은 정체" 상황을 놓치지 않고 점검할 수 있습니다.

적용 범위:

- 본 루프는 사용자 개인 계정의 최근 7일 데이터 기준으로 동작합니다.
- 커뮤니티/타 사용자 원문 데이터 기반 최적화는 기본 범위에 포함하지 않습니다.
- 공개 화면에는 익명화된 집계 KPI만 사용합니다.

#### Day 1 (기준선 고정)

- 이번 주 기준값을 고정: 승률, RR, 평균 이익, 평균 손실, 최대 낙폭
- 적용 정책 기록: `user_confirm`
- 실행 후 운영 메모 작성 위치: `docs/AI_OPTIMIZATION_BUTTON_SPEC_20260522.md`의 7일 템플릿

#### Day 2~6 (소폭 조정 + 관찰)

- 한 번에 하나만 조정 (TP/SL/신호 임계값/포지션 크기 중 1개)
- 조정 폭은 작게 유지 (권장: 5~10% 이내)
- 매일 확인: 손실 꼬리(큰 손실 1~2건)가 커지는지 여부

#### Day 7 (판정 + 롤백 여부 결정)

- 통과 조건: RR 개선 + 최대 낙폭 악화 없음
- 보류 조건: 승률 개선만 있고 RR 정체
- 롤백 조건: RR 하락 + 낙폭 악화 동시 발생

#### 핵심 원칙

- "많이 바꾸기"보다 "작게 바꾸고 검증"이 우선입니다.
- 사용자에게는 항상 "무엇이 왜 바뀌었는지"(XAI 요약)를 같이 보여줘야 합니다.
- NoahAI는 판단 품질 최적화를 보조하며, 최종 판단과 책임은 사용자에게 있습니다.

### 시나리오 1⃣: 거래 전 준비도 확인

**상황:** 오늘 거래를 할 준비가 되었는지 확인하고 싶습니다.

**단계:**

1. **대시보드를 열고 상단의 "AI 실행" 버튼을 클릭합니다**
   ```
   ┌─────────────────────────────────┐
   │  NoahAI 대시보드                  │
   │  ┌───────────────────────────┐  │
   │  │ [AI 실행] [설정] [기록]    │  ← 상단 버튼 영역
   │  └───────────────────────────┘  │
   │                                 │
   │  (대시보드 차트 및 정보)          │
   └─────────────────────────────────┘
   ```

2. **AI가 자동으로 거래 준비도를 진단합니다**
   - 진행 중 메시지가 표시됩니다
   - 약 1-2초 걸립니다

3. **진단 결과 모달이 표시됩니다**
   ```
   ┌────────────────────────────────┐
   │ AI 실행 준비도 진단          │ ← 상태
   ├────────────────────────────────┤
   │ 상태: 준비 완료                │
   │                              │
   │ 바이낸스 연결됨             │
   │ 업비트 API 키 입력됨        │
   │  신한증권 계정 미설정        │
   │                              │
   │ 위험도: 일반                │
   ├────────────────────────────────┤
   │ [그대로 진행] [설정 수정]       │
   └────────────────────────────────┘
   ```

4. **결과를 확인하고 선택합니다**
   - **"그대로 진행"**: 계속 거래합니다
   - **"설정 수정"**: 부족 항목을 수정합니다 (다음 시나리오 참고)

5. **결과는 하단 카드에 저장됩니다**
   ```
   ┌─────────────────────────────────┐
   │ 최근 AI 실행                  │
   │ ├─ 실행 시각: 2026-05-07 14:32  │
   │ ├─ 상태: 준비 완료             │
   │ ├─ 거래소: 바이낸스, 업비트       │
   │ └─ 이력: [더 보기]              │
   └─────────────────────────────────┘
   ```

---

### 시나리오 2⃣: 부족 항목 즉시 수정

**상황:** AI 진단 결과 "신한증권 계정 미설정"이 나왔습니다. 지금 바로 수정하고 싶습니다.

**단계:**

1. **AI 진단 결과 모달에서 "설정 수정" 버튼을 클릭합니다**

2. **설정 창이 열리고 상단에 진단 패널이 표시됩니다**
   ```
   ┌────────────────────────────────┐
   │ 설정                         │
   ├────────────────────────────────┤
   │ ┌──────────────────────────┐  │
   │ │  AI 실행 준비도 진단   │  │ ← 자동 표시됨
   │ │ 문제: 신한증권 계정 미설정 │  │
   │ │ [지금 수정하기]        │  │
   │ └──────────────────────────┘  │
   ├────────────────────────────────┤
   │ 탭 목록:                    │
   │ • OpenAI API                   │
   │ • 거래소 API 설정              │
   │ • 거래소 선택  ← 현재 탭      │
   │ • 일반                        │
   │ • AI 설정                     │
   │ • 증권사 설정  ← 여기로 이동  │
   └────────────────────────────────┘
   ```

3. **"지금 수정하기" 버튼을 클릭하면 자동으로 "증권사 설정" 탭으로 이동합니다**

4. **신한증권 계정번호를 입력합니다**
   ```
   신한증권 설정:
   ├─ 활성화: 체크
   ├─ API 유형: OpenAPI / REST / Mock
   ├─ ID: [________________]
   ├─ 비밀번호: [________________]
   ├─ 계정번호: [________________] ← 여기 입력
   └─ API 버전: [________________]
   ```

5. **"저장" 버튼을 클릭합니다**
   - 설정이 저장됩니다
   - **자동으로 AI 진단이 다시 실행됩니다**
   - 부족 항목이 사라집니다

---

### 시나리오 3⃣: 음성으로 설정 변경 제안 받기

**상황:** AI 어시스턴트와 대화하면서 "레버리지를 3배로 해줘"라고 음성으로 말했습니다.

**단계:**

1. **AI 어시스턴트 탭에서 음성 입력 후 말합니다**
   ```
   사용자: "레버리지를 3배로 줄여줄래?"
   (음성 입력 버튼을 누르고 말합니다)
   ```

2. **음성이 인식되어 텍스트로 변환됩니다**
   ```
   인식된 음성:
   "레버리지를 3배로 줄여줄래?"
   
   확인  취소
   ```

3. **"확인"을 클릭합니다**

4. **AI가 설정 변경을 제안합니다**
   ```
   AI: "현재 레버리지는 5배입니다. 
   이를 3배로 변경하는 것이 맞나요?
   
   변경 내용:
   - 레버리지: 5x → 3x
   
   위험도: 안전한 변경
   
   [적용] [취소]"
   ```

5. **"적용" 버튼을 클릭합니다**

6. **최종 확인 대화상자가 나타납니다**
   ```
   정말로 다음과 같이 변경하시겠습니까?
   
   변경 전/후:
   - 레버리지: 5x → 3x
   
   이 변경은 안전합니다.
   
   [예] [아니오]
   ```

7. **"예"를 클릭하면 설정이 저장됩니다**
   - 설정이 변경됩니다
   - 채팅창에 "설정이 저장되었습니다" 메시지 표시
   - AI 진단이 자동으로 갱신됨

---

## 결과 해석

### 위험도 레벨 해석

| 레벨 | 아이콘 | 의미 | 조치 |
|------|-------|------|------|
| **일반** | | 모든 설정이 안전함 | 그대로 진행 가능 |
| **주의** | | API 키 일부 미입력 | 필요 항목만 수정 후 진행 |
| **고위험** | | 실주문 ON + 높은 레버리지 등 | 주의 깊게 검토 후 진행 |

### 진단 결과 항목 해석

#### 준비됨
```
바이낸스 연결됨
→ 거래소 API 키가 설정되고 연결됨
→ 의미: 즉시 거래 가능
```

#### 부분 준비
```
업비트 API 키 미입력
→ 거래소를 선택했으나 API 키가 없음
→ 의미: 설정에서 API 키 입력 필요
```

#### 미준비
```
신한증권 계정 미설정
→ 증권사를 활성화했으나 계정정보 없음
→ 의미: 설정에서 계정번호/비밀번호 입력 필요
```

### 실행 이력 이용

**하단 카드에서 최근 실행 내역을 확인할 수 있습니다:**

```
최근 AI 실행
├─ 실행 시각: 2026-05-07 14:32
├─ 상태: 준비 완료
├─ 활성 거래소: 바이낸스, 업비트
├─ 활성 증권사: (없음)
└─ [더 보기] ← 클릭하면 최근 10개 이력 조회 가능
```

**"더 보기"를 클릭하면 이력 모달이 표시됩니다:**

```
┌──────────────────────────────┐
│ AI 실행 이력 (최근 10개)     │
├──────────────────────────────┤
│ 1. 14:32 준비 완료         │
│    거래소: 바이낸스, 업비트    │
│                            │
│ 2. 14:15  부분 준비        │
│    거래소: 바이낸스           │
│    문제: 신한증권 미설정      │
│                            │
│ 3. 13:45 고위험           │
│    거래소: 바이낸스           │
│    경고: 실주문 플래그 ON    │
│                            │
│ ... (더 많은 이력)           │
└──────────────────────────────┘
```

---

## 자주 묻는 질문

### Q1: "AI 실행"을 누르면 자동으로 거래가 시작됩니까?

**A:** 아니요. **절대 자동 실행되지 않습니다.**
- AI는 준비도를 진단하고 표시할 뿐입니다
- 사용자가 "확인" 버튼을 명시적으로 클릭해야만 다음 단계가 진행됩니다
- 언제든 "취소"로 중단할 수 있습니다

### Q2: "고위험" 경고가 나왔습니다. 거래를 시작해도 됩니까?

**A:** 신중하게 검토 후 진행해야 합니다.
- **고위험 상황 예시:**
  - 실주문 플래그(enable_stock_live_order)가 ON
  - 레버리지가 15배 이상
  - 잔고 활용 한도가 40% 이상
  
- **권장 조치:**
  1. 설정 화면에서 위험 항목 검토
  2. 필요하면 레버리지·활용한도 낮추기
  3. 다시 AI 진단 실행하여 "일반" 또는 "주의"로 변경 확인 후 진행

### Q3: 설정을 수정한 후 "AI 실행"을 다시 해야 합니까?

**A:** 자동으로 갱신됩니다.
- 설정 화면에서 저장하면 자동으로 새로운 AI 진단 결과가 생성됩니다
- 대시보드로 돌아가면 즉시 반영됩니다
- 다시 클릭할 필요 없습니다

### Q4: 음성 입력이 잘못 인식되었습니다. 다시 할 수 있습니까?

**A:** 네, 언제든 취소할 수 있습니다.
1. 인식된 텍스트 확인 화면에서 **"취소"** 클릭
2. 다시 음성 입력 버튼 클릭하여 재시도
3. 또는 직접 텍스트로 입력 가능

### Q5: API 키가 없는데 AI 실행을 눌러도 됩니까?

**A:** 네, 진단하면 됩니다.
- "미설정" 상태로 표시됩니다
- "설정 수정" 버튼으로 바로 API 키 입력 화면으로 이동
- 한 번에 모든 부족 항목을 확인하고 수정 가능

### Q6: 증권 거래는 어떻게 준비합니까?

**A:** AI 진단이 자동으로 안내합니다.
1. "AI 실행" 클릭
2. 진단 결과에서 "신한증권 미설정" 등 부족 항목 표시
3. "설정 수정" → "증권사 설정" 탭으로 자동 이동
4. 계정번호, ID, 비밀번호 입력
5. 저장 시 자동으로 진단 갱신

---

## 문제 해결

### 문제 1: "AI 실행" 버튼이 응답하지 않습니다

**원인:** 네트워크 또는 AI API 문제

**해결:**
1. 인터넷 연결 확인
2. 설정 → OpenAI API에서 API 키 입력 확인
3. 어시스턴트 탭에서 간단한 텍스트 질문으로 AI 작동 확인
4. 5초 정도 대기 후 다시 시도

### 문제 2: 진단 결과가 이전과 다릅니다

**원인:** 정상입니다. 설정이 변경되었기 때문입니다.

**확인:**
- 최근에 설정을 변경했는지 확인
- API 키를 추가했는지 확인
- 거래소 연결이 끊어졌는지 확인

### 문제 3: "지금 수정하기" 버튼이 잘못된 탭으로 이동합니다

**원인:** 부족 항목 유형 인식 오류 (드물게 발생)

**해결:**
1. 수정 대화상자를 닫고 수동으로 관련 탭 선택
2. 필요한 정보 입력
3. 저장 후 AI 진단 다시 실행

### 문제 4: 음성 입력 후 설정이 변경되지 않았습니다

**확인:**
1. 최종 확인 대화상자에서 "예"를 클릭했는지 확인
2. 채팅창에 "설정이 저장되었습니다" 메시지 있는지 확인
3. 설정 화면을 열어서 실제로 변경되었는지 확인

### 문제 5: "고위험" 경고가 자주 표시됩니다

**원인:** 현재 설정이 고위험 범주에 해당합니다

**권장:**
1. 설정 화면에서 다음 확인:
   - `enable_stock_live_order` 플래그 상태
   - `default_leverage` 값 (15 이상이면 높음)
   - `balance_utilization_limit` (40% 이상이면 높음)

2. 필요하면 다음과 같이 조정:
   - 레버리지 10 이하로 낮추기
   - 활용한도 30-40% 범위로 조정
   - 실주문 플래그 끄기 (테스트 시)

---

## 추가 지원

**더 자세한 정보:**
- [AI 어시스턴트 가이드](docs/AI_ASSISTANT_GUIDE.md)
- [트러블슈팅](docs/TROUBLESHOOTING.md)
- [설정·실행 모드 상세 설명](docs/USER_GUIDE.md#처음-설정과-실행-모드)

**버그 보고:**
- GitHub Issues 또는 이메일로 보고해주세요

---

**마지막 업데이트**: 2026년 7월 27일 (한국 시간)
