# v3.9.1.32 사용자 피드백 → v3.9.1.33 수정·검증 범위

작성: 2026-09-15. 상태: **v3.9.1.33 미배포 소스 후보**. 제품 버전은 3.9.1.33, Electron 업데이트 버전은 3.9.133이다. 이미 배포된 v3.9.1.32 GitHub 릴리스·Windows 설치본·업데이트 메타데이터는 보존한다.

## 자료 범위

- 사용자 첨부: 7개 거래소, 전체 대시보드, KIS 학습/실행, 거래 통계, 자산 통합, AI 애널리스트/인텔리전스/어시스턴트 화면.
- `data/Teayu`는 읽기 전용으로 조사했다. PAPER 마지막 청산은 `2026-09-14T21:42:55+00:00`(한국시간 9월 15일 06:42), AI 결정 마지막 저장은 `2026-09-14 21:43:53` UTC다.
- 스크린샷은 같은 날 20시대다. 이 오전 자료로 저녁 수치·Coinone 신규 거래·KIS 분석·Provider 실패 원인을 정확히 재현했다고 주장하지 않는다.
- 기존 파일·원장을 보정하거나 미확정 거래를 확정 거래로 승격하지 않았다. 사용자 API 호출·주문·키 외부 전송은 하지 않았다.

## 판정 및 변경

| 피드백 | 판정 | 소스 변경 및 경계 |
|---|---|---|
| 통합/거래소 PnL 다름 | 화면 기간 차이부터 구분해야 함 | 통합은 오늘, 기관 카드는 전체 기간. 동일 기관·기간·통화·모드 비교 안내 추가. 원장 집계는 공통 `_paper_statistics_snapshot` 사용 |
| PAPER 승률·PnL `대조 전` | 실제 UI 버그 | LIVE의 `reconciled_closed_count`로 PAPER 숫자를 숨기던 조건 제거. 가상 청산 기준으로 표시하고 LIVE 대조 조건은 유지 |
| KIS 로그는 있는데 학습 0 | 실제 조회 경로 버그 | 주식 기록은 DB `ai_decisions`인데 공용 코인 JSON을 읽었다. 증권사 명시 필터를 페이지 제한 전에 적용하고 기관별 총계 산출 |
| 증권사 전환해도 같은 파일 | 같은 DB 자체는 정상, 잘못된 JSON 안내는 버그 | KIS/키움/미래에셋/신한 별칭 정규화. 기관 미확정 기록은 다른 기관으로 추측하지 않음. 새 분석에 신호·지표·시장 정보 보존 |
| 미국주식 학습/운용 여부 | 현재 국내주식 어댑터 범위 | 국내 주식·국내 상장 ETF 자동분석과 해외 시세 조회를 구분해 학습 화면에 안내. 해외주식 주문·학습 엔진을 새로 구현한 것이 아님 |
| 키움 `py64:control0:event0` | 프로세스 분리만으로 비트수 문제 미해결 | x64 엔진에서 x86 호스트를 인증된 로컬 IPC로 실행. 누락·잘못된 PE 비트수 선차단. x86 빌드와 설치기 포함 검사 추가. 실제 OCX 설치/로그인/체결 검증은 Windows 게이트 |
| AI 애널리스트 0/대조 전 | LIVE만 조회하던 화면의 모드 누락 | PAPER/LIVE 명시 선택 및 같은 원장 집계 연결. 가상 수익을 실거래 수익으로 표시하지 않음 |
| 자산 통합 손익과 통계 불일치 | 실제 근거 계약 불일치 | 확정되지 않은 기존 `pnl`을 실현 성과처럼 사용하던 경로 수정. 체결 대조 상태·`net_pnl`이 있는 LIVE만 합산. 최근 최대 5,000건 범위 표시. 0평가액을 낮은 집중위험으로 단정하지 않음 |
| 생활금융 일부 기능 | 거래 PnL과 별도 저장소 | 수입·지출·목표는 LifeFinanceManager 정본. PAPER 대조 상태 때문에 생활금융 전체가 차단되는 공통 조건은 확인되지 않음 |
| 주요시장 입력과 결과 다름 | 실제 입력/프리셋 UX 버그 | 실제 프리셋 종목 표시. 입력 수정 시 직접 입력으로 전환. 프리셋의 종목별 자산 유형 유지 |
| 이벤트/내러티브/거시 조회 불가 | 공급자 미연결 + 잘못된 완료 표시 | `configuration_required`/`no_data`를 완료로 표시하지 않음. 데이터 공급을 새로 개통한 것이 아님 |
| 공개 Project ON인데 선택 불가 | 별도 키·설정 확인 필요 + 상태 캐시 버그 | 선택 시 최신 준비 상태를 재조회. OFF·공개 키 미저장·조회 실패 구분. ON만으로 인증 완료가 되지 않음. 비공개 계좌/전략 문맥을 공개 경로로 보내지 않는 제한 유지 |
| 심층분석 상투적 답변 | 로컬 질문 분류/근거 조회 오류, 실제 Provider 실패 원인은 미확정 | Strategy Studio 질문을 전용 지식으로 전달. 일반/독립 후보와 국면 기준 설명 보강. 런타임 메트릭이 없어도 저장 청산 통계를 사용. 호출 실패는 Provider·모델·HTTP·오류 코드를 보존하고 원시 오류 본문은 노출하지 않음 |

추가 발견: 변동성을 RSI, 추세 강도를 MACD로 대입하던 과거 학습 표시를 제거했다. 없는 지표를 0이나 다른 지표로 꾸미지 않는다. `AI READY`도 모델 연결 증거가 아니므로 분석 기록 있음/대기/조회 실패로 구분한다.

## 원장 대조 결과 (오전 공유본)

| 기관 | 계산 가능한 가상 청산 | 전체 기간 순손익 | 통합/개별 대조 |
|---|---:|---:|---|
| Binance | 1,096 | +180.9823 USDT | 일치 |
| Bybit | 214 | -1.7382 USDT | 일치 |
| Bitget | 224 | +3.4869 USDT | 일치 |
| OKX | 120 | -158.6701 USDT | 일치 |
| Upbit | 160 | -5,423.4857 KRW | 일치 |
| Bithumb | 1,069 | -77,691.4404 KRW | 일치 |

유효 2,883건, 미확정 940건 별도 보존. USDT와 KRW는 환산 근거 없이 더하지 않는다. Coinone·증권 PAPER 청산은 이 공유본에 없다. 키움 DB 분석 기록 223건은 조회 가능하며, KIS `stock_analyze_symbol`은 이 공유본에 없어 0건이다. 저녁 KIS 화면의 주문수도 0이므로 분석 로그만으로 실제 주문/체결 성공이라 말할 수 없다.

## 재발 방지 검증

- `tests/test_v39132_feedback_contracts.py`: 7개 코인·4개 증권 기관별/통합 집계, 기간 차이, 통화 분리, 증권사별 DB 페이지/총계, 미확정 LIVE 배제, 저장 원장 기반 답변, Provider 실패 메타데이터, x86 호스트 누락·비트수 검사.
- `scripts/test_feedback_webui.cjs`: 실제 React 컴포넌트를 독립 fixture로 렌더링하여 PAPER/LIVE 숫자, 애널리스트 모드, 티커 전달값, 공급자 미연결 표시, 설정 갱신 후 Project 선택, 증권사 전환을 브라우저에서 검사한다. 실제 거래소/API E2E가 아니다.
- 기존 테스트의 미확정 손익·지표 오인 대입 기대값은 새 근거 계약으로 수정했다. 유효한 기록 fixture에는 명시적 대조 상태를 넣고 미확정 legacy가 제외되는 검증도 유지한다.

## 배포 전 남은 필수 확인

현재 소스 검증: 전체 Python 테스트 **2,392 통과·8 건너뜀**, Node 22 웹 프로덕션 빌드 성공, 실제 React 컴포넌트 브라우저 fixture 검증 통과. 웹 번들 크기 경고는 남아 있으며, 이 결과는 Windows 설치본·사용자 계좌 E2E 검증을 대신하지 않는다.

1. 9월 15일 20:55 이후 최신 Teayu 진단본으로 저녁 원장·KIS 기록·Provider 오류를 재대조한다. 키 원문 공유는 필요 없다.
2. Windows에서 x64 엔진+x86 키움 호스트 설치·로그인·잔고·시세·PAPER·종료/재시작·주문 결과 미확정 차단을 확인한다. OCX 등록 문제는 x86 호스트만 추가한다고 자동 해결되지 않는다.
3. 뉴스·일정·거시 공급자를 실제 운영에서 연결할지 별도 결정한다. 이번 패치는 미연결 기능을 완료처럼 표시하지 않도록 하는 수정이다.
4. 승인된 v3.9.1.33 번호로 Windows 설치본·blockmap·latest.yml·manifest를 새로 만들고 v3.9.1.32 → v3.9.1.33 업데이트 경로를 검증한다. 이미 배포된 v3.9.1.32를 같은 번호로 덮어쓰지 않는다.

OpenAI Project 인증은 [공식 API 인증 문서](https://developers.openai.com/api/reference/overview)의 키/프로젝트 구분을 확인했다. 앱의 공개 경로 ON은 별도 API 키 저장·실제 모델 호출 성공·청구 확인을 대체하지 않는다.
