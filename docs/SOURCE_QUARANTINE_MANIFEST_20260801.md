# NoahAI 활성 소스·격리 정본

- 기준일: 2026-08-01
- 활성 프로젝트: `noahai_client`
- 격리 위치: 프로젝트 밖 `../noahai_client_source_quarantine_20260801`
- 기계 판독 정본: `config/source_quarantine_manifest.json`

## 원칙

격리 파일은 현재 대시보드, 사용자 기능, 거래·증권 어댑터, 빌드 진입점에서
호출되지 않는다. 삭제 대신 프로젝트 밖에 보존하며 SHA-256으로 변조·누락을
검사한다. 사용자 거래 데이터, 로그, API 자격증명은 격리 대상에 포함하지 않았다.

## 격리 결과

| 구분 | 수량 | 내용 |
|---|---:|---|
| 과거 충돌·패치 소스 | 2 | 버전 충돌본, 독립 대시보드 패치 조각 |
| 미사용·손상 위젯/업데이터 | 7 | 학습·리포트·차트·데모 변형과 과거 수동 업데이터 |
| 대시보드 미참조 메서드 보관본 | 1 | 정의 외 참조가 없던 23개 메서드의 원문 |
| 과거 `theme_system` | 6 | `utils.fixed_colors`로 대체된 팔레트·폰트·컴포넌트·스텁 |
| 합계 | 16 | 모두 활성 패키지와 PyInstaller 포함 범위 밖 |

`ModernDashboard`는 261개 메서드에서 연쇄 미참조 23개를 격리한 뒤 238개로
정리했다. 단순 이름 검색뿐 아니라 Python AST 속성 참조와 문자열 콜백까지
검사했으며, 제거 전 원문은 `ui/dashboard_modern_dead_methods_20260801.py.disabled`에
보존했다.

## 반복 검증

```bash
.venv/bin/python scripts/active_source_audit.py
```

PASS 조건은 다음과 같다.

- 활성 Python 소스 구문 오류 0건
- `Conflict`, `broken`, 미사용 변형 파일 활성 소스 0건
- 대시보드 정의 전용 메서드 0건
- 격리 16개 파일 존재 및 SHA-256 일치
- 증권사 공식 계약 4개 정본 일치
- 필수 감사·계약·테스트·마스터 문서 존재

광범위 예외 처리 수는 즉시 일괄 삭제하지 않고 회귀 추적용 기준값으로 기록한다.
UI 파괴 후 정리처럼 실패를 무시해야 안전한 최선 노력 경로와, 계좌·주문·체결처럼
오류를 화면과 호출자에게 반드시 전달해야 하는 경로를 구분해 단계적으로 줄인다.
